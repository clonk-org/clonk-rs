# Bundled zlib notice

`libz-sys 1.1.29` links a platform zlib when one is available, but compiles
its vendored `src/zlib/` sources on Windows and for static or fallback builds.
`LICENSE` is copied verbatim from `libz-sys 1.1.29/src/zlib/LICENSE`.
