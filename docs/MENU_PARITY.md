# C++ Menu Parity Inventory

This is the recursive completion checklist for the Rust menu port. The C++
classes in `../src/` are authoritative. A top-level screen is not complete
until every child sheet, context menu, confirmation/input dialog, callback,
scroll path, and transition reachable from it is complete.

Status meanings (the status applies only to the bounded row, not its parent
screen or an unwired library component):

- **Complete**: the reachable Rust path covers the C++ behavior and
  presentation with executable evidence and has no generic fallback.
- **Partial**: a classic-shaped implementation exists, but listed behavior is
  missing or indirect.
- **Fail-fast**: Rust logs and returns an error instead of drawing a generic or
  incomplete pane. This is preferable to false parity, but is not completion.
- **Dropped**: an explicit product decision excludes the bounded legacy feature
  from the Rust port. Any retained entry point must fail closed until its
  separate UI cleanup removes the affordance.
- **Missing**: no usable classic Rust implementation exists. A reachable
  generic, synthetic, status-only, or externally delegated substitute is still
  Missing, not Partial or Fail-fast.

The C++ source and shipped `content/`/`planet/` data are read-only oracle
material for this inventory. Implementations belong under the repo root; never edit
the oracle to make a Rust result appear correct.

Audit snapshot (2026-07-20): implementation through `2a12f0471`; M09 queue
state 154 done / 0 claimed / 42 todo / 0 blocked. Queue state is a discovery
lead only; current Rust code, executable evidence and the C++ oracle determine
the statuses below.

## Recursive census

- Startup graph: `C4Startup::SwitchDialog` plus every callback/subdialog from
  `C4StartupMainDlg`, `C4StartupScenSelDlg`, `C4StartupNetDlg`,
  `C4StartupPlrSelDlg`, `C4StartupOptionsDlg`, and `C4StartupAboutDlg`.
- In-game graph: `C4MainMenu`, `C4Menu`, `C4ObjectMenu`, player team selection,
  object context activation, and generic `C4GUI` dialogs.
- Overlay graph: `C4GraphicsSystem`, `C4UpperBoard`, `C4MessageBoard`, every
  `C4Viewport::DrawOverlay` child, `C4MouseControl::Draw`, network/debug status,
  screenshot feedback and eliminated/observer states. Non-dialog presentation
  is part of the literal recursive screen census.
- Rust-only graph: every app `StartupView`, retained generic renderer,
  status-state producer, environment/developer route and empty-state fallback
  must either be explicitly isolated or return a logged error before drawing.
- Script graph: 147 `CreateMenu` calls in 109 shipped `content/**/*.c` and
  `planet/**/*.c` files, plus 401 `AddMenuItem` calls. Styles comprise Normal,
  Context, Info, and Dialog.
  Full coverage comes from implementing the complete CreateMenu/AddMenuItem
  grammar rather than hardcoding individual scenario screens.
- Modal graph: at least 134 C++ message/confirmation/input/remove-dialog call
  sites across startup, networking, options, league, player, scenario, and
  update flows.
- Editor-only GUI is tracked separately at the end; it is never silently
  omitted from the literal full-GUI scope.

## Startup root

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Classic command-line bootstrap and file associations | `C4Application::DoInit`; `C4Game::ParseCommandLine` | **Partial** | Rust-only `--...` switches now coexist with case-insensitive classic file, URL and slash arguments; values belonging to the Rust switches are not reclassified, and unknown classic-shaped arguments are ignored instead of aborting in clap. A direct `.c4s` or `Scenario.txt` path starts without exposing the startup menu; absent a network-enabling switch it follows the offline path. First-occurrence case-insensitive `.c4p` arguments replace `General.Participants` when present, and configured definitions followed by ordered `.c4d` arguments seed the direct scenario loader. The effective seed/preset/fixed result is retained through prepared-host resource publication, dynamic `DefinitionFiles`, JoinData and synchronized client loading (`command_line_definition_selection_is_published_to_network_clients`). `/join:` and non-update `clonk:` URLs query the explicit reference endpoint, defaulting an omitted port to the selected config's `Network.PortRefServer`, then join the first returned reference. Network/signup/league/lobby/observe/runtime-join/port/password/comment/fair-crew switches are parsed in command order and retained as process-local launch state; backed host/client and game-option consumers apply those overrides without persisting the launch overrides. A direct network scenario proceeds through prepared-host staging without presenting its internal lobby unless `/lobby` or `/lobby:N` requests lobby/countdown presentation; command-line network flags without a direct scenario retain the normal startup screen, whose selected local/network dialog owns the eventual mode. `/config:` enters the established environment/explicit/default config-path selection before discovery, with ambient `LC_CONFIG_FILE` retaining precedence; `/verbose` selects a debug default while explicit `LC_LOG` then `RUST_LOG` filters retain priority; `/Language:` temporarily replaces the process-local `LanguageEx` resource/query sequence but not persisted `General.Language` scenario-title selection. `/console` selects the persistent scale-one developer window, defaults allowed debug on, and retains that shell across in-process Open/Close cycles. On an ordinary menu startup, standalone `/update`, `clonk:update` and incoming `.c4u` requests queue the launcher-owned hand-off below; `clonk:update` is not treated as a network address. Combining that update request with a direct scenario/join remains unhandled. A nonempty `/recdump:` now writes the fully decoded replay control record with C++'s case-insensitive `.txt` selection and canonical text/binary rewrite, and destination failures abort replay activation through its recoverable diagnostic path. Positional `.c4r` and nonempty `/stream:` inputs share the native inbound record-stream field: after boot, Rust inflates the classic zlib stream, retains its complete recoverable chunk prefix, opens the leading `RCT_File` as the initial save, copies `Scenario.Head.Origin`, overlays the initial save and later streamed files in order, removes file chunks, installs the remaining chunks as `CtrlRec.c4b`, writes the reconstructed `.c4s`, and enters ordinary replay activation. Clean control-record EOF without `RCT_End` and suffix bytes after End follow C++ tolerance. `/startup:` now selects the initial dialog before first rendering, mapping the seven case-insensitive `C4Startup::SetStartScreen` names — `main`, `scen`, `netscen`, `net`, `options`, `plrsel`, `about` — onto their counterparts, with `netscen` opening the scenario selector in network-host mode rather than the `net` browser; an unrecognized name leaves the remembered/default view untouched as C++'s `return false` does (`classic_startup_argument_selects_initial_cpp_view`). Remaining: combining an update request with a direct scenario or join is not implemented. |
| Configured user/save/record/screenshot paths | `C4ConfigGeneral::DeterminePaths`; `C4Config::{AtUserPath,AtScreenshotPath}`; `C4Game::QuickSave`; `C4Record::Start` | **Partial** | The already-selected config retains `LC_CONFIG_FILE` > explicit `/config` > default precedence; an `LC_CONFIG_FILE` selection bypasses default-location migration. Rust deliberately keeps its default config under the discovered user-data `Config` directory rather than the classic platform location. Only when that default destination is absent, startup performs a one-time migration: `LC_LEGACY_CONFIG_FILE` wins, Windows next tries `HKCU\Software\LegacyClonk Team\LegacyClonk`, and the first existing platform legacy file is the fallback. Once the destination exists, legacy sources are not read again, and later changes made by either engine are not synchronized. The launcher reapplies a just-migrated config before its first runtime launch. A nonempty `General.UserPath` is platform-expanded into the Rust user-data root unless `LC_USER_DATA_DIR` overrides it; selecting `UserPath` never relocates the config file. `SaveGameFolder` and `SaveDemoFolder` remain independent paths, with relative values rooted at ExePath/install root; relative `ScreenshotFolder` values remain ExePath-relative and a directory-creation failure falls back to ExePath. Sound discovery now takes the selected `AppPaths` like the music resolver already did, so an explicit `/config` whose `UserPath` names a different tree drives audio lookup and a pathless app walks no install media at all - the ambient `AppPaths::discover()` fallback is gone (`explicit_config_paths_feed_audio_and_live_user_root`). Remaining: C++ expands `UserPath` on every `AtUserPath` call while `AppPaths` still freezes it at discovery, so a runtime `UserPath`/environment change does not move later lookups; recording roots do not yet receive C++'s localized `Title.txt`; and C++'s single-directory screenshot mkdir/absolute-string concatenation quirks are not modeled exactly. |
| Persistent engine session log | `C4LogSystem::OpenLog`; `C4LogSystem::LogSink` | **Complete** | App startup truncates or creates `Clonk.log` in the selected logs directory (normally `Logs/` under the user-data root, while honoring `LC_LOGS_DIR`) and tees the filtered session stream to both stderr and the file. `/verbose` raises the default file and stderr level from info to debug, while explicit `LC_LOG` then `RUST_LOG` directives retain priority. |
| Main six-button screen | `C4StartupMainDlg.cpp:38-122`; `crates/clonk-frontend/src/startup_main_menu.rs` | **Partial** | Six-button activation, 10-step fade/back stack, Alt/Alt+Shift mnemonics and shared 500 ms tooltips are covered, including gamepad focus traversal in About. Retained button and bottom-right Players/fan-project geometry now follows fullscreen resizes that arrive while boot still owns `AppMode::Loading`; the reported 1152x723-logical/3456x2168-physical Scale=300 regression fixes the final C++ anchors at logical `(1101,640)` and `(1129,695)` before the one-row physical viewport crop. It remains Partial through common-input/cursor, deferred participant persistence and recursive rows below. **Intentional divergence:** the version string under the logo draws this port's release version (`clonk_core::version::PORT_VERSION`) instead of C++'s `C4VERSION` (`"4.9.11.0 [362] "`). Placement, font, colour, alignment and markup are unchanged, so an F9 capture diff of this screen will show a text-shaped difference at `(Wdt*39/40, Hgt/18 + 0.4*logoHgt)` that is expected, not a regression. The engine compatibility version still lives in `clonk_core::version::ENGINE_VERSION` and continues to drive definition gating, savegame `C4XVer` headers, config migration and protocol identification. |
| Process-global C4GUI resources | `C4GUI::Resource::Load`; `C4GraphicsResource::InitFonts` | **Complete** | Before startup, loading or running UI can mutate state, select a cache or draw pixels, one aggregate gate requires all five GUI fonts and the exact thirteen global sheets, including `GUIBigArrows.png` and `GUISpinBoxArrow.png`. Resolution preserves C++ base/`Extra.c4g` stem precedence; tooltip ownership uses its independent shadowless font. Active scenario/folder/definition `Graphics.c4g` groups rebind the running sheet set at the GraphicsResource refresh with C++ group precedence and group-id reload caching (a sheet reloads only when its winning source changes); the loader progress bar, reusable dialogs and script-menu graphics consume the rebound sheets, and teardown restores the pristine startup surfaces (`active_scenario_gui_overrides_reach_dialogs_and_script_menus`, `active_gui_sheet_overrides_rebind_only_when_the_winning_source_changes`, `real_hazard_scenario_gui_sheet_overrides_apply_and_reach_running`). Scenario `Head.Font`/overload fonts already resolve through the same registered set at the refresh and revert at teardown. Font specs cap every comma-delimited field at 30 native bytes and apply `%i` decimal/octal/hex prefix grammar after truncation (`font_specs_apply_c4maxname_and_percent_i_numeric_grammar`). Directory and packed vector-font catalogs retain native-byte filenames and faces through exact entry reads (`font_catalog_preserves_native_byte_vector_filenames_in_directory_and_packed_groups`). Unreadable optional vector/`Fonts.txt` entries are skipped, and matching vector faces are initialized newest-first until one succeeds; only an exhausted registered chain tries the requested filename and exact host-system face (`font_catalog_skips_bad_optional_candidates_and_falls_through_matching_faces`). `Fonts.txt` Size fields use the StdCompiler decimal/unsigned-`0X` numeric-prefix lexer, while a no-digit value keeps the definition at the field default of 1 (`fonts_txt_size_uses_stdcompiler_numeric_prefix_and_error_semantics`). Network client GO/runtime-join loading stages the same resolve→pending→apply refresh over the join-synchronized group set — combined scenario with Origin parents, the Extra root without pre-join definition children (C4Game.cpp:368-381), and the synchronized definition roots registered last at definitions priority (C4Game.cpp:2432-2441) — under the identical typed failure gate (`network_join_applies_active_scenario_gui_overrides`). A Graphics-bearing Definitions resource arriving mid-round re-registers into the active set and rebinds only sheets whose winning source changed, mirroring the re-callable `C4GraphicsResource::Init` overloading pass with its `idRegisteredMainGroupSetFiles` skip (`mid_round_graphics_group_arrival_rebinds_changed_sheets_only`). Missing, malformed, or exhausted final resources — including undecodable active-scenario overrides (`malformed_active_gui_sheet_override_fails_typed_before_pixels`) — retain the typed logged boundary instead of a default Rust pane. |
| Mandatory game graphics bundle | `C4GraphicsResource::Init`; `C4GraphicsResource::LoadFile` | **Fail-fast** | The selected `Liquid` bitmap is resolved and decoded through native group/extension precedence during every install-backed graphics initialization, even when `ColorAnimation` or shaders are disabled. Missing and malformed winning resources reach the typed startup boundary and active-game/preload error paths without falling back; only retaining the decoded surface and performing per-frame animation remain config-gated (`graphics_resources_validate_liquid_even_when_animation_disabled`). |
| Eager startup graphics/font bootstrap | `C4Startup::EnsureLoaded`, `C4StartupGraphics::Init`/`InitFonts` | **Fail-fast** | After the process-global gate, one deterministic aggregate preflight requires the oracle-ordered 16-image startup bundle (including the recursively used `StartupPlrPropBG.png` and `StartupPlrCtrlType.png`) and Caption/Main/Title/MainSmall shadowless RX fonts before view/model/child/status checks, cache lookup, or logical/native pixels. All seven Rust `StartupView` roots are covered; `NetworkLobby` retains the prerequisite because C++ reaches its lobby before `C4Startup::Unload`, although it is not a startup `DialogID`. Decoded surfaces/fonts are checked for basic usability without inventing atlas dimensions C++ never validates. At integer application scale above one, Main also refuses a missing or mismatched scale-native font atlas. |
| Main screen fixed-default resource failure boundary | `C4StartupMainDlg`; startup graphics resources | **Fail-fast** | For the currently supported shipped-resource path, the app logs the complete missing set and returns an error before bitmap-font, optional-image, blue-pane or solid-button substitutes can become player-visible. A missing `LoaderGoldmine1.png` now delegates to the same weighted `C4LoaderScreen::Init` search the scenario loader uses, ending in the general `Loader*` wildcard, so a classic pack without the named loader still boots; the boundary fires only when no eligible loader exists at all (`startup_main_uses_classic_loader_wildcard_when_goldmine_is_absent`). Font selection uses the shared catalog/file/system resolver above. |
| Dialog switch/fade/back stack | `C4Startup::SwitchDialog`, `C4StartupDlg::OnClosed` | **Complete** | Retain fade-timing (10-step, exact (100-iFade)*255/100 alpha), cross-fade compositing, input-suppression-until-frame-ten, and eLastDlgID-style back-stack regression tests, including that Back does not update the remembered dialog (mirrors C++'s pToDlg==pLastDlg skip) and the nested ScenSel-from-NetJoin Back target. |
| Common startup cursor/focus/hotkeys/tooltips | `C4GUI::Screen`, `Dialog`, `Control` | **Partial** | Shared 500 ms tooltip ownership is complete, including the retained joined-lobby controller, as are the resolution-selected `C4GUI::CMouse` Region cell (centered for new sheets and top-left for the legacy 13px sheet), its Region-hotspot mouse-help `(+5,-5)` Help composite, dialog -> cursor -> tooltip layering, startup-frame invalidation on every mouse move/leave/focus transition, Main/Player/About/NetDlg mnemonics, Options Alt+1..0 control-set mnemonics and per-control Tab/Shift-Tab traversal on every sheet, Player/About reverse traversal, About's configured-primary-gamepad Left/Right traversal, and persistent global frontend F3/Ctrl+F3 audio toggles. The joined lobby shares that one tracker across render and input frames: the stillness clock survives a drawn frame, non-pointer input suppresses until real integer motion, a wheel and a new hover target each call `ResetToolTipTime` rather than disabling the tip, and a covering modal draws none (`joined_lobby_tooltips_survive_frames_and_use_shared_delay`). |
| Process-global GUI feedback sounds | `C4GUI::GUISound`; `C4SoundSystem::NewInstance`, `Instance::Execute` | **Complete** | Startup, loading, lobby and running GUI feedback first requires `FESamples`, then uses the shared global sound-instance lifecycle: pre-game execution follows `FESamples`, running execution follows `RXSound`, same-resolved-sample globals are rejected without retriggering, and muted one-shots retain their logical instance only through the strict half-duration cutoff. Instances survive the startup/loading-to-game mode switch and unrelated in-generation catalog loads; a reloaded same-filename sample or the next PreInit sound-system generation invalidates them. Menu/loading frames sweep completed instances so a finished sound cannot suppress later feedback. Empty lobby chat retains its GUI-gated Error, while native direct effects—including the Sound-volume test, sync error and lobby countdown/loop—bypass the outer GUI gate with the same instance behavior (`running_gui_sound_requires_fe_samples_and_rx_sound`, `global_gui_instance_survives_transitions_until_its_sample_is_reloaded`, `repeated_gui_sound_uses_global_instance_dedup`). |
| Participants Add/Remove context and player submenu | `C4StartupMainDlg::OnPlayerSelContext*` | **Partial** | Context geometry, recursive Add/Remove trees, ordering, icons, activation, validation and focus suppression are covered. Rust eagerly persists participant mutations; missing paths and write failures reach the typed startup-status boundary. C++ mutates process Config here and defers persistence; configuration-lifetime parity is M10. |
| Rust startup model/child/status boundary | no C++ screen; `GameApp::preflight_startup_presentation` | **Fail-fast** | Resource preflight rejects missing controllers, unavailable Options audio and generic diagnostic status before any pixels. All six Options sheets, About Licenses and Network Chat render; no implemented child is rejected. |
| Startup base-frame reuse and nested overlays | C++ redraw/invalidation ownership; `GameApp::render_for_presentation` | **Partial** | Every presented startup frame is composed from scratch. The version-gated `MenuFrameCache` was removed on 2026-08-04: it could never engage under retained GPU capture, and the recompose it would have saved measures 25-269 µs. The static per-view backdrop layer is still cached (`StartupBackdropCache`), including under GPU capture, so the full-screen bilinear blit is not repeated. Scenario-selector raster chrome is cached separately from its scale-native `Start Game`/`Search:` commands, which are emitted on every frame just as C++ redraws its labels. Broader classic dirty-rectangle/redraw ownership remains unported. |
| First-run new-player properties modal | `C4StartupMainDlg::OnShown` | **Complete** | Retain the OnShown-equivalent trigger (search path, *.c4p extension check, hidden-file skip, name-only match without opening the group), its re-trigger on every Main show/Back-to-Main and on player deletion, and the Cancel/Submit/save-and-refresh-participants regression coverage. |
| Automatic/manual/incoming update flow | `C4StartupMainDlg::OnShown`, `C4UpdateDlg` | **Partial** | Showing the main menu now runs the OnShown sequence (cpp:258-276): an incoming package first, then a requested check, otherwise the automatic one when `Network.EnableAutomaticUpdate` is set — the incoming branch is not an `else if`, and both flags are consumed either way. `C4UpdateDlg::CheckForUpdates` is ported against the published manifest rather than the `?action=version` server: the once-a-day gate (cpp:264-265), the unconditional `LastUpdateTime` write (cpp:266-267), the cancellable `IDS_MSG_LOOKINGFORUPDATES` wait, the `IDS_MSG_ANUPDATETOVERSIONISAVAILA` Yes/No prompt, `IDS_MSG_UPDATEFAILED` with the transport's own message, and `IDS_MSG_UPTODATE` for manual checks only. Accepting a compatible release now retains every planned component and external release source, performs a preflight writable-space check, downloads with cancellation and aggregate progress, verifies each declared size and SHA-256, writes a durable `ApplyPlan`, and exits into a detached copy of `clonk-game`. That helper waits for the runtime and launcher, transactionally swaps data before binaries with journaled rollback/recovery for ordinary process failures, records the installed component release/digest before macOS signing, consumes its staged inputs and relaunches the installed launcher. Three deliberate divergences: an incoming `.c4u` is refused with `IDS_MSG_UPDATEFAILED` instead of applied, because `ApplyUpdate` (cpp:171-215) executes a program extracted from an untrusted group; an engine-tuple change gets its own `IDS_MSG_UPDATEINSTALLMANUALLY` message rather than being hidden as "no update available", since installing that content under the running engine would silently prune definitions; and the flow is captioned `IDS_DLG_CHECKFORUPDATES` and reports the up-to-date case as `IDS_MSG_UPTODATE` ("Clonk Rust %s is the latest version") rather than carrying `Config.Network.UpdateServerAddress` as every caption and denying an update with `IDS_MSG_NOUPDATEAVAILABLEFORTHISV` — this port's default server is a full release-download URL rather than C++'s bare hostname, and "no update available for this version" reads as the engine-change message above. Remaining: combining an update request with a direct scenario/join remains unhandled, and incoming legacy `.c4u` packages remain intentionally refused. Automatic checks are suppressed in test builds (`automatic_update_check_allowed`). |
| F6 editor launch from startup | `C4StartupMainDlg::SwitchToEditor` | **Complete** | Bare F6 is bound in the startup main dialog's own scope at control-override priority; a modified chord and any other startup view leave it alone. On Windows it validates `Editor.exe` beside the engine (`Config.AtExePath(C4CFN_Editor)`), and only then flags the launch and exits startup - a missing executable returns false so the key stays unconsumed. The spawn happens in the loop-teardown handler, after subsystem cleanup, mirroring `~C4Application` rather than the key handler. Every other platform skips the `#ifdef _WIN32` body and consumes the key without effect, exactly as C++ does (`startup_f6_launches_editor_when_available`). |
| Rust editor scenario/controller route | no C++ startup-list equivalent | **Fail-fast** | `ScenarioKind::Editor`, touch activation and `StartupMenuAction::EditEntry` return typed, logged boundaries; the external-editor substitute was removed. Keep the guard until the real developer UI exists. |
| Startup fatal/restart log presentation | `C4Application.cpp:438-449`; `C4Startup.cpp:257-308` | **Partial** | Host/join failures and ordinary fullscreen local load/recoverable activation failures reconstruct the remembered selector and localized Error Log after clearing partial game and loader state; fatal, ringbuffer and empty-log branches are covered without `StartupStatusOverlay`. Explicit command-line and developer-console failures remain partial because their native non-restart lifecycle is not yet modeled. |

## Scenario selection subtree

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Local scenario book chrome/list/info/buttons | `C4StartupScenSelDlg`; `startup_scensel.rs::ScenselDialogFocus` | **Partial** | Exact focus chrome/order, basic keyboard/gamepad traversal, disabled-control skipping and pointer/touch hit bounds are covered. F5 rediscovers the current folder in place, F2 provides inline rename with group/title persistence, Delete confirms and removes ordinary or packed children, and Alt+M updates the shared/persisted mission-access modules at their exact override priority. Map-style folders remain outside this book projection. |
| Recursive `.c4s`/`.c4f`/directory discovery | `C4ScenarioListLoader` | **Complete** | Recursive discovery and C++ ordering are covered in both modes. Physical and packed scenario/folder basenames retain their exact native bytes for filtering, ASCII-case-folded ordering, menu identity, and child opening; legacy decoding is confined to the fallback display title (`scenario_discovery_preserves_native_byte_names_for_directory_and_packed_children`). Selected `Desc{}.rtf` component data stops at its first native NUL before RTF parsing (`scenario_description_ignores_bytes_after_native_nul`). `Startup.AlphabeticalSorting` suppresses only folder-index and difficulty keys while preserving folder/icon/title/identifier order across discovery, reload and rename resort. |
| Enhanced catalog search (Rust product path) | C++ baseline: `C4StartupScenSelDlg::OnSearchBarEnter`, `UpdateList`; Rust: `MenuState::apply_enhanced_search` | **Partial** | **Intentional product divergence:** the player-facing path searches every loaded non-folder catalog entry live, including title, identifier, ancestor context, author, and description. It normalizes case, accents, punctuation, and whitespace; requires all query terms; applies deterministic relevance ordering with conservative title typo tolerance and stable catalog-order ties; shows folder context, result counts, a clear affordance, and query-aware no-result recovery; and restores the prior selection and scroll when cleared. The first Escape clears while retaining focus and the second leaves the edit. C++ remains the authority for the isolated submit matcher below. Remaining: expose a platform accessibility search role/name and announced result-status changes, support IME preedit/composition and candidate positioning, and localize the new result/count/recovery strings. |
| Folder navigation and folder metadata | `Folder::Start`, `FolderBack` | **Complete** | Folder layers push and pop one level, caption/right-page metadata follows the stack, Back reconstructs the parent, and F5 atomically rediscovers the current path while preserving live search and selection. |
| Asynchronous scenario-loader progress state | `C4StartupScenSelDlg::UpdateList`, `C4ScenarioListLoader::GetProgressPercent` | **Complete** | Interactive discovery runs on a cancellable worker with recursive 0–100 progress. The selector forces the book loading view with the localized `Loading... (%d%%)` label, then atomically restores the folder, live search, and selection when the completed catalog arrives (`scensel_f5_rediscovers_current_folder_and_applies_live_search`). |
| Folder-map view (`FolderMap.txt`) | `C4StartupScenSelDlg.cpp:47-404,1016-1023`, `C4MapFolderData`; shipped `content/Western.c4f/FolderMap.txt` | **Partial** | Direct `.c4f` folders honor map enablement, packed/logical traversal, localized data, resolution/image fallback, backgrounds, access overlays, scenario facets/titles, map-side info and classic activation. Map pictures and scenario facet buttons use `DrawXFloat`'s inward integer destination bounds with matching proportional source crops; fullscreen backgrounds retain the ordinary stretched blit. Invalid or disabled maps fall back to the book. Developer `ImageDump` now blits the scenario `Area` out of the background into a facet of exactly that size and saves it under `BaseImage` beside the map - pixels outside the background stay transparent, a failed dump only logs, and the ordinary base load is skipped as C++'s `continue` does. Title fonts come from `GetFontByHeight`/`GetBlackFontByHeight`'s ascending line-height ladder, including the `BookSmallFont` tier the port previously lacked, with the native `iHgt / lineHeight` zoom snapped to 1.0 inside the [0.8, 1.25) tolerance (`folder_map_image_dump_and_dynamic_title_font_match_cpp`). Remaining: the resolved zoom is not yet applied to glyph rasterization, so a title outside the tolerance band draws at its font's native size. |
| Classic search edit and oracle filter | `OnSearchBarEnter`, `UpdateList`, `KeySearch`, `C4GUI::Edit`; Rust oracle seam: `MenuState::submit_search` | **Complete** | The isolated oracle path preserves C++'s Enter-submitted, immediate-current-folder, title-only, markup-stripped, case-insensitive substring filter, stable ordering, and surviving-selection fallback; the player-facing window deliberately uses the enhanced product path above. Ctrl/Command+F select-all, caret/selection, word edits, platform edit shortcuts, plain caret arrows, mouse capture/double-click, horizontal scroll, blink, and render tests work. The exact state-dependent Cut/Copy/Paste/Clear/Select-all classic popup, right-down/Apps-key triggers, retained logical focus with suppressed focus drawing, clipboard mutation rules, activation-release capture, raw middle-down insertion from the native selection source (Linux PRIMARY, the platform clipboard on other Unix targets, and no Windows paste), and the clipped 1.5x `¦` caret glyph are covered. Consecutive ordered-native frames prove the static wooden `Search:` label and typed edit text both survive a pixel-backdrop cache hit. |
| Description `TextWindow` scrolling | `C4GUI::TextWindow`, `ScrollWindow` | **Complete** | Wheel, clipping, fixed pin, track jump-and-drag with capture, and held arrows match the C++ geometry/conversions. |
| Scenario list scrolling | `C4GUI::ListBox` | **Complete** | Selection-follow viewport, wheel, clipping, fixed pin, scrolled clicks, captured track drag, held arrows, end-stopping Up/Down, and fully-visible-row PageUp/PageDown/Home/End are covered. Scale-native row labels and inline rename text remain attached to the clipped list viewport instead of disappearing when semantic font capture is active. |
| Choose Definitions checkbox and definition selector | `StartScenario`, `C4DefinitionSelDlg`; `clonk-app/src/main.rs::PendingDefinitionSelection` | **Complete** | Selection/reset rules, LocalOnly, recursive focus/input, raw-order `*.c4d`, fixed/optional checks, exact dialog/list/preview/buttons, scrolling, title drag, 3-second 1px-per-frame bounce auto-scroll, shared-delay title/Close tooltips, F5 rebuild, nested error, modal/capture cleanup, cancel retention, ordered output and rooted loading are covered. Local versus `NetworkHost` mode survives refresh, cancel, error and accept. |
| C4Group child opening | `C4Group::OpenAsChild`, `WildcardMatch`; `clonk-resources::Group::open_child` | **Partial** | Successive packed/directory-backed components resolve independently; `*` rejects the whole request, while `?` consumes exactly one native byte, folds ASCII case, selects the first stored/native-order name before child validation, and retains the actual selected path (`open_child_question_mark_selects_first_child_like_cpp`; `open_child_question_mark_matches_one_native_byte_not_one_unicode_scalar`). Close-equivalent parent rewrites retain a complete directly unopenable child's state-0/1 CRC core and physical payload, stop CRC mutation at that entry in sorted order, apply the same `*` rejection and final-order `?` first-match rules recursively, promote a successfully opened child with deeper CRC failure to state-2/zero, and still reject an unavailable declared payload (`rewrite_preserves_unopenable_child_when_crc_calculation_fails`; `rewrite_crc_pass_stops_after_first_unopenable_child`; `rewrite_nested_child_crc_uses_open_as_child_wildcard_rejection`; `materialized_child_crc_uses_child_close_sort_for_question_match`; `rewrite_promotes_openable_child_with_nested_crc_failure_to_zero`; `rewrite_rejects_truncated_unopenable_child_payload`). Save-only and other raw-child rewrite fidelity remain tracked separately. |
| Explicit/folder-local definition order and packed parent resources | `C4Game::OpenScenario`, `FoldersWithLocalsDefs`, `InitDefs`, `InitMaterialTexture`; `C4GroupSet::RegisterParentFolders`; `C4GraphicsResource::RegisterMainGroups` | **Complete** | The startup handoff resolves the explicit global pack before folder-local packs, lets the later folder-local definition overload an ID collision, retains definition-root graphics, and traverses nested packed `.c4f` parents inner-first for graphics/materials and overload flags. Every definition load uses the native `CreateNameTree` hierarchy for the first root `DefCore`, its direct fields, dedents and the adjacent `Physical` follow-up (`def_core_main_fields_follow_create_name_tree_hierarchy`). An explicit empty `Name=` remains the definition's empty, script-visible name through direct and legacy scenario loading; only an absent key receives `Undefined` (`def_core_empty_name_overrides_undefined_default`; `empty_def_core_name_survives_both_engine_load_paths`). The direct compiler surface ignores the synthetic `CanBeBase`, combined `Shape` and `BurnTurnTo` aliases while retaining `Base`, `Width`/`Height`/`Offset` and `BurnTo` (`def_core_ignores_non_oracle_alias_keys`). Definition scripts traverse the fixed and localized native segments independently, reduce script and associated `StringTbl` language preferences to their first two native bytes, skip unreadable candidates within a segment, and retain empty-file, per-component newline and NUL semantics (`unreadable_definition_script_candidate_falls_through_to_next_language`; `readable_empty_definition_script_candidate_blocks_language_fallback`; `script_string_table_uses_two_native_language_bytes_before_nul`). `BurnTo` and `ConstructTo` pass through the same fixed four-byte `C4IDAdapt` before their live morph lookups, including short-token rejection, overlong truncation and native zero-ID handling (`def_core_turn_to_fields_use_c4id_adapt`). Packed System scripts and recursive child definitions wildcard-match, label, read and open exact native-byte entry names in stored order (`system_script_enumeration_preserves_non_utf8_group_entry_name_bytes`; `child_definition_enumeration_preserves_lossy_colliding_native_names_in_group_order`). Generic packed `LoadEntry`/`LoadEntryString` reads retain a child-marked entry's declared physical bytes, while `OpenAsChild` separately enforces child-group parsing (`child_marked_file_loads_physical_payload_like_cpp`). C4GraphicsResource and loader lookup now scan immediate raw entry names only, retain the selected entry's exact identity through decode, and ignore unrelated opaque names and unrelated malformed child payloads without inventing nested basename aliases (`graphics_lookup_is_top_level_and_ignores_unrelated_opaque_names`; `raw_graphics_and_loader_lookup_ignores_unrelated_opaque_names`). Additional graphics and portraits retain exact packed/directory filename bytes, clip suffix identities to the native 30-byte `C4MaxName`, keep PNG-before-BMP and first-match order, and resolve full raw suffix-matched overlays (`definition_graphics_names_preserve_legacy_bytes_and_c4maxname`). Custom `Rank*.txt` extensions apply fmt's static one-C-string flags, width, byte precision, positional reuse, no-op lengths, escaped percent and pointer form, while malformed formats remain dormant until their extended rank is requested and then fail at the native uncaught boundary (`custom_rank_extensions_apply_printf_width_and_precision_like_cpp`). `General.Definitions` and ordered command-line `.c4d` arguments form the seed; a nonempty scenario preset replaces it, while fixed selection stays authoritative. Network publication consumes the frozen staged rooted block, originals, duplicates and outer-to-inner folder-local packs; the host reload recomputes and verifies that same order (`scenario_preset_replaces_seed_while_fixed_selection_wins_publication`). The global-vs-folder-local collision and packed-parent resource regressions run in the default gate. |
| Scenario rename | `C4GUI::RenameEdit`, `ScenListItem::KeyRename`, `Entry::RenameTo`; `clonk-frontend::rename_edit` | **Complete** | The reusable inline controller hides the replaced label, preloads/selects its text, applies the exact C++ modifier masks, owns Escape and the enabled primary gamepad's raw AnyHigh abort, submits on Enter or focus loss, reselects invalid input, and restores the saved focus for generic finish/abort. Raw AnyLow follows the dialog path (abort rename, then OK), while only gamepad Left/Right attempt focus transfer. F2 wires it to scenario/folder persistence: valid commits regenerate the type extension, move ordinary or nested packed entries, replace `Title.txt`, rediscover/resort and reselect; that callback uses C++'s `RR_Deleted` ownership and explicitly focuses the rebuilt list. Empty/same-title input is non-mutating; collisions and storage failures remain modal and nonfatal. |
| Scenario delete | `KeyDelete`, `DeleteConfirm` | **Complete** | Unmodified Delete outranks Search when a row is selected, shows the type/title Yes/No confirmation and original-group warning, removes ordinary or nested packed entries, selects the next visible row, and reports storage failure through the nonfatal Delete error modal. A focused inline rename edit retains its higher-priority text Delete. |
| Mission access/password | `C4StartupScenSelDlg.cpp:1797-1843`; `KeyCheat`, `KeyCheat2` | **Partial** | Alt+M priority, case-insensitive module updates, shared `General.MissionAccess`, immediate rebuild, start refusal and FolderMap filtering are covered. Rust writes the file on acceptance and a failure reaches `StartupStatusOverlay`; C++ only mutates process Config here. Configuration-lifetime parity is M10. |
| Start validation/warnings | `Scenario::CanOpen`, `DoOK` | **Complete** | Local/network mission-access, replay, savegame min/max and player-count behavior is covered. Network replay and over-max rows are disabled; too-few remains enabled and opens the native warning. |
| Local Fair Crew/Record option buttons | `C4GameOptionButtons`; `clonk-frontend/src/game_option_buttons.rs` | **Partial** | Geometry, resources, tooltips, focus, all input modes, native Fair Crew/Record state and `ForcedNoCrew` are app-wired. Launch switches seed the same state without persisting. Rust eagerly writes accepted changes and failures enter the typed startup-status boundary; C++ mutates process Config and defers saving. Configuration-lifetime parity is M10. |
| Network scenario book | `C4StartupScenSelDlg(true)`; `ScenarioSelectorMode::NetworkHost` | **Partial** | Network `CanOpen` and row colors are exact, including replay/over-max disabled and too-few enabled with a warning. The dialog remains Partial through selector option/InputDialog configuration-lifetime paths. |
| Network selector option buttons | `C4GameOptionButtons`; `clonk-frontend/src/game_option_buttons.rs` | **Partial** | Internet, League, Password, Comment, Fair Crew and Record have classic layout, state, tooltips and input. Alt+M and modal suppression are covered. Rust writes normal values immediately; C++ defers saving process Config. Configuration-lifetime parity is M10; Password/Comment remains Partial below. |
| Password/Comment `InputDialog` from network selector | `C4GameOptionButtons::OnBtnPassword`, `OnBtnComment`; `clonk-frontend/src/input_dialog.rs` | **Partial** | Both callers use the classic modal with editing, context actions, limits and all input modes. Rust eagerly persists accepted values and write failures hit the typed startup-status boundary; C++ mutates process Config/Game.Network and defers saving. Configuration-lifetime parity is M10. |

## Startup network browser and IRC subtree

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Network browser chrome | `C4StartupNetDlg` | **Complete** | Exact layout, Games/Chat switching, Back/F5/Enter and Internet/Record buttons are app-wired; searches and timers freeze under a modal. The advertised captions are live on both registered Alt forms - Games, Chat, Internet, Record, Reload, Join and New Game - pressing the button directly like `Button::OnHotkey`; the four controls Chat mode removes stay inert, and a covering modal keeps the dialog beneath silent (`netdlg_alt_mnemonics_activate_visible_buttons`). |
| Legacy configuration adaptation and corruption recovery | `C4Config::AdaptToCurrentVersion`, `IsCorrupted`; `C4Application::DoInit`; `clonk-game::prepare_config` | **Complete** | First load applies C++'s exact 346/347/349 and through-359 migrations, including all legacy service addresses and graphics/audio resets, then stamps build 362 so later launches are byte-stable. Before any app config consumer runs, missing integrity fields retain the typed 42/800 defaults and present native scalars are canonicalized; a non-42 `ConfigResetSafety` or zero `Graphics.ResolutionX` triggers full typed-default replacement and path rediscovery for the standard or environment-selected config. A nonempty `/config:` or `--config` selection instead aborts with the classic custom-corruption error without installing defaults. |
| Retained NetDlg Internet refresh | `C4StartupNetDlg::OnShown`, `UpdateMasterserver`; `NetDlgController::sync_masterserver_signup_from_config` | **Complete** | Returning from the host selector resyncs the Internet icon/config and masterserver row; live query/status transitions and spawned references are implemented. A failed masterserver query stays in its native query presentation while up to two immediate retries run, and only a terminal failure enters the response-relative 30-second fallback (`masterserver_failures_retry_twice_before_periodic_interval`). |
| Live game list/query entries | `C4StartupNetListEntry`, `UpdateList` | **Complete** | Masterserver/LAN/direct queries, references, native status/MOTD/redirect rows, blue-underlined clickable MOTD links, scrolling, timeouts and one-second throttling are implemented. Reference endpoints use the first system hostname result, retain malformed entries through source-host fallback, and preserve address, protocol and reference order without disturbing legacy quoted/octal decoding (`reference_addresses_resolve_hostnames_and_keep_malformed_entries`). Modal coverage freezes search events and their expiry clocks; the masterserver worker preserves the entry-lifetime two-retry budget and keeps its 30-second deadline separate from LAN discovery, which re-probes every five seconds while a per-address gate holds each host's reference query to the native 30-second and 10-second cadences — a deliberate, approved divergence from the oracle's 31-second refresh (clonk-org/clonk-rs#306). |
| Hosted reference charset | `C4Network2RefServer::RespondReference`; `C4Config::GetCharsetCodeName` | **Complete** | Startup passes `General.LanguageCharset` through the exact case-insensitive CP932/949/1361/950, CP1250-1258, CP874 and UTF-8 mapping with CP1252 fallback. The retained summary path converts Unicode text before bytewise INI escaping, while exact host updates preserve already-native `LegacyCString` fields; every response keeps the configured canonical `Content-Type` (`reference_server_uses_configured_legacy_charset`). |
| Reload and direct-address two-stage query/join | `C4StartupNetDlg::DoRefresh`, `DoOK` | **Complete** | Reload restarts discovery/masterserver search with C++'s exact one-second throttle and preserved rows. Enter on a nonempty, focus-retaining, untrimmed address adds a direct-reference query row and refocuses the list; a second Enter/Join on that pending row joins the raw address directly. |
| Join validation/redirect/discovery modals | `DoOK`, `DoRefresh` | **Complete** | Empty selection, bad references, version mismatch, no-runtime-join confirmation, masterserver redirect accept/decline and discovery-start failure all open the exact classic OK/Error/YesNo dialogs with C++ ordering and text. |
| Create Game transition | `C4StartupNetDlg::CreateGame`; `clonk-app/src/main.rs::process_network_dialog_actions` | **Complete** | It opens `C4StartupScenSelDlg(true)`/`NetworkHost` before any host socket or `NetworkManager` exists. |
| Selected host scenario/definitions staging | `C4StartupScenSelDlg::StartScenario`; `C4Network2::LeaguePlrAuth`; `LeaguePlayerAuthContinuation::StartupHost` | **Complete** | Replay refusal, savegame/bootstrap projection, resource publication and sanitized identity match C++. Initial league players use native login/register, cancellable Auth wait, welcome confirmation and retry before finalization (`startup_host_auth_players_stay_with_their_connection_and_cancel_cleanly`; `league_signup_headless_login_registration_and_abort_match_cpp_auth_flow`). |
| Network group CRC failure projection | `C4Group::EntryCRC32`; `C4Network2Res::SetByGroup`, `SetByCore` | **Complete** | Every C4Group-backed resource publishes and locally compares the unsigned zero result when direct CRC calculation fails, without turning the failure into a host error or search miss. A successfully opened nested group contributes that zero while its parent continues XORing sibling CRCs (`nonplayer_group_crc_failure_publishes_and_matches_zero_like_cpp`). |
| Network directory standalone packing | `C4Group_PackDirectoryTo`; `C4Network2Res::GetStandalone`, `SetByCore` | **Complete** | Host publication and contents-identical local resolution use one exact directory packer with native entry bytes, literal zero timestamps, wrapped-versus-raw child classification, Linux-only executable metadata and player optimization after packing (`network_directory_standalone_matches_cpp_group_packer`). |
| UPnP host port mappings | `C4Network2IO::Init`, `Clear`; `C4Network2UPnPMiniUPnPc`; `clonk-network::upnp` | **Complete** | TCP then UDP mappings use the bound local ports as external suggestions. A zero request selects `AddAnyPortMapping` on every accepted IGD service, including v1, while an explicit port selects `AddPortMapping`; router failures remain nonfatal and later mappings continue. Rust preserves `NewReservedPort` for shutdown deletion as required by the mapping contract, while the current C++ source appears to overwrite that returned value with its suggestion (`upnp_zero_external_port_uses_add_any_on_igd_v1_like_cpp`). |
| Post-connect lobby boundary | `C4Game::NetworkJoin`, `C4GameLobby`; `GameApp::poll_startup_network_connection` | **Partial** | A staged ordinary host enters the exact lobby over the scenario loader background while retaining its manager. Live roster, team, resource, add-player and kick paths are wired; remaining unsupported children stay typed fail-fast. Every terminal post-worker host/join failure now tears down partial state, reconstructs the C++-remembered NetworkHost selector or fresh NetDlg, and presents its diagnostic in the native `Error Log` modal without a generic startup-status boundary (`retained_netdlg_refreshes_internet_and_staged_host_keeps_options_noninteractive`, `join_progress_names_target_and_dismisses_on_resolution`). |
| IRC login sheet | `C4ChatControl` | **Complete** | Nick/Password/Real name/Channel use centered classic geometry, localized labels, masked transient password, native focus/traversal and ordered validation. Errors preserve/focus the form; native config seeding, selective persistence and the public-server warning are covered by `chat_login_uses_cpp_centered_geometry_localized_strings_and_typed_validation` and `standalone_irc_validation_disconnect_and_window_close_use_classic_modal_ownership`. |
| IRC legacy-byte transport | `C4Network2IRCClient::Connect`, `OnPacket`, `Send`, `OnMessage` | **Complete** | Configuration, line framing/parsing, reducer state, channel/query identities, transcripts, local echo and socket output retain exact legacy bytes; only the app/frontend boundary projects them through Windows-1252. Registration clips `PASS` to the first 31 bytes without repairing a split UTF-8 sequence, C-string termination and native `CharCapital` identity folding are preserved, and CRLF framing remains exact (`irc_transport_preserves_legacy_bytes_and_password_cap`). |
| IRC server/channel/query tabs | `C4ChatDlg.cpp:181-425,677-857`; `C4ChatControl` | **Partial** | Sheets preserve topics, colors, sanitization, wrapping, wheel scroll, unread state, nick sorting/icons, queries and close semantics. Each transcript is now bounded by the `C4GUI::TextWindow(rcDefault)` constructor defaults its C++ counterpart takes - 100 lines and 4096 characters - evicting from the front on either bound the way `C4LogBuffer` does, and the channel nick pane retains and wheels a `ScrollWindow` offset instead of dropping the rows below the fold (`irc_chat_scroll_windows_match_cpp_pointer_overflow_and_limits`). The transcript scrollbar is pointer-operable: its two `C4GUI_ScrollArrowHgt` arrows step a line, its pin drags, and its bare track pages. Connection and protocol-status lines now come from the active language table rather than baked English: the twelve `C4Network2IRCClient` status templates travel with the connection and substitute their positional `%s` arguments in C++ order, and the server sheet's connect notice uses `IDS_NET_CONNECTING` with its native empty second argument (`irc_status_transcript_uses_active_language_resources`). Transcripts are non-markup `TextWindow`s, so `AppendLines` breaks on CR/LF alone and a `|` stays literal text; `Update` tests `MSG_Notice` before its empty-source Server fallback, keeping a source-less notice on the active sheet; and queries are keyed by the post-`!` ident, so a sender's nick change reuses and retitles the one sheet instead of splitting the conversation (`irc_transcript_literal_pipes_and_query_routing_match_cpp`). Remaining: the nick `ListBox` is wheel-only (no bar drawn or hit-tested), and the retained line cap counts source lines rather than C++'s wrapped display lines. |
| IRC command language and quit confirmation | `C4ChatDlg.cpp:250-273,899-1033`; `C4ChatControl::ProcessInput` | **Complete** | Commands, history, multiline-paste abort and quit/part routing are covered. Empty submission now answers with `DoError(nullptr)` - the error sound alone, with no transcript line and no back-buffer entry - command parameters keep every character after the first `SplitAtChar` delimiter, the invalid-nick error expands `IDS_ERR_INVALIDNICKNAME2` with its command token, and an asynchronous send failure is appended to the sheet the command was typed into rather than Server (`irc_process_input_errors_spacing_and_send_failure_match_cpp`). |

## Startup player subtree

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Player list/activation/info/portrait | `C4StartupPlrSelDlg` | **Complete** | List scroll/typeahead/Home-End/PageUp-Down, keyboard/mouse/gamepad routing, widget sounds, checkbox activation, participant-overflow rollback/dialog, delete/properties/crew dispatch and portrait/selection-info rendering are covered, including reconciliation after a properties save crosses a rename before failing. |
| Player row Properties/Delete context | `PlayerListItem::ContextMenu` | **Complete** | Both entries route through the shared context-menu system (touch/mouse/keyboard/gamepad, focus suppression, outside-down pass-through, exact popup shape) into real handlers: Delete opens the classic Yes/No warning, and Properties now opens the live C4StartupPlrPropertiesDlg port, not a stub action boundary. |
| New Player / Properties form | `C4StartupPlrPropertiesDlg` | **Complete** | Presentation is now a port of the native dialog rather than a wooden-skin substitute: the 365x400 `fctPlrPropBG` paper is centered unscaled, every child rect comes from the same `ComponentAligner` walk as `C4StartupPlrSelDlg.cpp:1116-1235` behind the 45/16/55/30 margins, and the widgets are the native ones — BookFont/BookSmallFont labels in `C4StartupFontClr`, a transparent-background `C4GUI::Edit` with its double `DrawFrameDw` border and select-all highlight, `fctBigArrows` arrow buttons, the `fctFlagClr` flag drawn as C++'s two independently filtered `CreateColorByOwner` quads, three `sfctBookScrollR/G/B` scroll bars whose pins travel `value * GetMaxScroll() / 255`, the `fctKeyboard`/`fctGamepad` control-set image under `C4Facet::Draw`'s aspect fit, `Ico_MouseOn/Off`, the big-icon picture button, and `fctPlrCtrlType` movement phases with Jump'n'Run left of Classic. OK and Cancel are `Ico_None` `CloseIconButton`s that draw nothing, because the "OK" word and the "x" are painted into the paper sheet. Verified against a live 1280x720 C++ F9 capture at 99.58% bit-identical with 99.999% of pixels within +-1 (`layout_matches_the_cpp_component_aligner`, `slider_pins_travel_over_the_native_scroll_range`, `aspect_fit_matches_the_native_facet_letterboxing`, `book_font_line_heights_match_the_layout`). Name, standard/RGB color cycling, 8 control sets, mouse toggle, Jump'n'Run/Classic movement (mirrors AutoContextMenu), tooltips, empty/duplicate-name validation, and the real nested portrait selector are implemented and tested. Name failures open the classic blank-caption error modal and retain the form; post-validation rename/group/image/core/close failures close the form, reconcile the underlying list, and open the separate screen-owned `ShowErrorMessage`-style dialog whose body names the failing step and path with C++'s own strings (`IDS_FAIL_RENAME`+`IDS_ERR_RENAMEFILE` with both filenames, `IDS_ERR_OPENFILE`, `IDS_FAIL_MODIFY`, `IDS_PRC_NOGFXFILE`, and the raw close-step detail; `startup_player_properties_post_validation_save_failure_opens_classic_error_dialog`, `startup_player_properties_save_failure_modal_names_step_and_path`). Packed creates and edits stamp the process-frozen raw `General.Name` maker only when it is nonempty, preserving `New C4Group` or an imported maker otherwise (`startup_player_properties_stamps_nonempty_configured_group_maker`). Picture/BigIcon materialization follows `CopyFromSfcMaxSize`'s offscreen software path: left-edge nearest sampling is independent of application scale and `PointFiltering`, transparent source RGB is blackened, owner tint uses legacy `/256` rounding, and a zero-truncated minor axis produces no saved replacement (`startup_player_resize_matches_cpp_copyfrom_sfc_max_size`). |
| Portrait selector | `C4PortraitSelDlg` | **Partial** | Picture opens the C++-ordered modal with LegacyClonk user/program/Home/Desktop locations, native ComboBox/ListBox/CheckBox/scroll/button chrome, wrapped 100px thumbnails, pixel scrolling, throttled progressive decoding, native title/Close/option/location-entry tooltips, file entries followed by a working No Portrait tile, independent Player image/Lobby-Icon options, Alt+O, F5 and OK/Cancel commit semantics. Parent form, selector dialog and open location ContextMenu use separate native painter batches so deferred text cannot bleed through later chrome (`scale_native_portrait_selector_keeps_dialog_layers_in_cpp_painter_order`, `portrait_selector_layout_matches_cpp_at_1152x723`, `portrait_selector_wraps_labels_and_uses_the_tallest_item_for_the_next_row`). The location dropdown now grows to its widest caption and never leaves the screen: `DoDropdown` seeds it at the combo's width (`C4GuiComboBox.cpp:116`), `UpdateElementPositions` widens it to the longest entry without shrinking below that (`C4GuiMenu.cpp:333-358`), and `Screen::DoContext` flips it above or left of the anchor — falling back to the screen edge when the flipped side is also short — rather than clipping (`C4Gui.cpp:877-892`, `a_long_location_caption_widens_the_dropdown_and_keeps_it_on_screen`). The dropdown also raises C++'s four context-menu sounds on their exact conditions: `DoorOpen` on open, `DoorClose` only for user dismissal, `Command` only for a user selection change on an entry, and `Click` on activation without an extra close sound (`the_location_dropdown_raises_the_native_context_menu_sounds`). The selector title now takes the classic bounce: after the three-second dwell it advances one pixel per drawn frame, reverses and re-arms the dwell at each end, and stays fixed when the caption fits (`a_long_selector_title_bounces_one_pixel_per_draw_after_three_seconds`, `a_short_selector_title_never_scrolls`). Every visible selector caption — title, `Location:`, `Import image as:`, both checkbox labels, the trailing `No Portrait` tile and the OK/Cancel buttons — now resolves through the active language table like `C4FileSelDlg` does (`C4FileSelDlg.cpp:142,439,535,568-571`), with the shipped `LanguageUS.txt` value as the per-key fallback. The Location label's box is sized from the measured caption as `pUseFont->GetTextWidth` sizes it (`:143`) rather than a frozen 57px, so a longer localized caption pushes the combo box along instead of running underneath it; the measurement reproduces 57 for English, exactly matching the pinned 1152x723 C++ capture (`every_selector_caption_comes_from_the_language_table`). Remaining: selector-title dragging; geometry derived from the active native font metrics; localized Alt hotkeys; C++'s process-global rather than per-dialog progressive-decode cadence. Thumbnail filtering now goes through the same `stdgl_blit_sampling` predicate the runtime sprite path uses (`StdGL.cpp:527`), so `PointFiltering=true` upscales nearest-neighbour at an application scale of exactly 1, an already-exact thumbnail is never filtered, and any other scale stays linear regardless of the setting (`point_filtering_upscales_thumbnails_without_blending_at_scale_one`). Non-simulation C++ defects are intentionally corrected: its visible null item cannot be accepted, its lobby checkbox copies the player-image default, a taller item beside the trailing null item can be clipped from the final row, resize reflow carries the tallest prior row into every later row, and a context-suppressed Space-up can leave a button visibly stuck down. Rust also preserves useful PNG decoder diagnostics instead of C++'s usual `No Error`, and rejects corrupt JPEGs rather than accepting a partially decoded surface. Windows shell folders use the Unicode API instead of reproducing the pinned ANSI path narrowing. |
| Crew mode and crew detail list | `SetCrewMode`, crew list classes | **Complete** | Enter/leave crew mode, direct-child discovery with C++'s descending type/experience sort, live participation checkboxes, portraits, stats/physical bars and promotion text, and the exact no-crew message dialog are implemented and tested end-to-end against real player/crew groups. Close-equivalent participation rewrites stamp only the packed crew child with the process maker while preserving its directly saved parent (`startup_crew_core_rewrite_stamps_child_maker_but_preserves_parent_maker`). |
| Persistent player/crew ExtraData load | `C4PlayerInfoCore::CompileFunc`, `C4ObjectInfoCore::CompileFunc`, `C4ValueMapData::CompileFunc`, `C4Game::CreateInfoObject` | **Complete** | Player files retain ordered nil/int/raw-bool/bool/C4ID/object values, native identifier/default and legacy numeric parsing, and install the exact player/crew maps before PreInitializePlayer and ready-crew Construction/Initialize/Recruitment. Every Player/Preferences/LastRound section and field name now has comprehensive exact-case regression coverage through the shared `StdCompilerINIRead`-style tree (`player_file_core_names_are_exact_case`); player-selection rendering and properties rewrites consume those same exact-case results instead of permissive duplicate parsing. Creation attaches the roster info/link/rank and persistent physicals before callbacks, preserving same-callback writes (`player_file_loads_player_and_crew_extra_data_into_join_state`). |
| Crew Rename/Delete/Death Message context | crew item callbacks | **Complete** | Delete (exact warning/removal) and Set-death-message (matches C++'s own modal C4GUI::InputDialog, same label/caption/icon strings) are faithful and tested, and Rename now uses the shared inline RenameEdit over the row label (pre-fill/select-all, Enter/focus-loss commit, Escape abort, invalid-name reselect) instead of a modal dialog. Rename/death-message core closes stamp the nested crew maker but preserve the direct-`Save(true)` parent; a packed delete stamps the parent when its pending mutation reaches the native `Close`, and an empty process maker preserves every prior header (`startup_crew_delete_stamps_parent_when_native_group_close_persists_it`). |
| Player/crew delete and validation dialogs | `OnDelBtn`, property close paths | **Complete** | Player and crew deletion share the exact warning/failure-dialog machinery and are tested against real packed and directory groups. Properties OK now distinguishes empty and duplicate names with the classic blank-caption error modal, preserves every entered field, and leaves the properties form open underneath after dismissal. |

## Startup options subtree

All six sheets (Program, Graphics, Sound, Keyboard, Gamepad, Network) now
render and route real behavior; no sheet returns an entry boundary anymore.
Shared Back/save failures now use the exact localized classic configuration
resources; remaining gaps are noted per row.

| Sheet or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Options chrome/tabs | `C4StartupOptionsDlg.cpp:609-674` | **Partial** | All six sheets and every visible per-control Tab/Shift-Tab path are reachable, with exhaustive tooltip targeting. The dialog now resolves its caption and all six tab captions from the active language table at construction, as the C++ constructor does, and the sheet layout measures that resolved text (`startup_options_visible_labels_follow_runtime_resources`). **Intentional divergence:** the third tab is captioned from the port-only `IDS_DLG_AUDIO` ("Audio") rather than C++'s `IDS_DLG_SOUND` ("Sound"), because the port also hosts the port-only voice-chat group on that sheet (clonk-org/clonk-rs#452). `IDS_DLG_SOUND` keeps its C++ value and its other consumer, the in-game `C4MainMenu` Options entry. Nothing else moves: `C4GuiTabular` sizes every tab from the 95px clip graphic and centres the caption inside it (`C4GuiTabular.cpp:110-118,393`), so an F9 capture diff of this screen shows a text-shaped difference at the third caption anchor `(300, 277)` at 1280x720 and nowhere else. |
| Program | program sheet controls | **Partial** | Language and font combos, white-chat, timestamps, preloading, fair-crew slider and Reset/Advanced buttons are wired and persisted; tooltip targeting is exhaustive. Remaining: visible labels such as `Language:`, `Font:` and `White Chat:` are hard-coded rather than loaded from runtime resources. |
| Graphics | `C4StartupOptionsDlg.cpp:794-919` | **Partial** | Display mode, scale controls and confirmation, toggles, particles and smoke are bound, tooltip-covered and included in per-control traversal. Visible labels come from the active language table, and the resolution confirmation uses `IDS_MNU_SWITCHRESOLUTION`/`IDS_MNU_SWITCHRESOLUTION_TEXT` with its `%u` countdown re-rendered every tick like `ResChangeConfirmDlg::UpdateText`. |
| Audio (C++ "Sound") | sound sheet controls | **Partial** | **Intentional product divergence:** the sheet carries a port-only **Voice chat** group — the `Config.Voice` opt-in, playback volume, push-to-talk key and activation mode, which until now were reachable only through Advanced Settings (clonk-org/clonk-rs#452, clonk-org/clonk-rs#422). `ActivationThreshold` and `ActivationHangover` stay in Advanced: they are set-once tuning, and a third row costs ~41px against 800x600's 91px of slack. It is placed in the vertical slack C++'s own 2x5 grid leaves unused below the Volume group (172 of 462 logical pixels at 1280x720), so every C++ rect on the sheet is unchanged and the group is *omitted* where that slack cannot hold it (640x480 leaves 50px); Advanced Settings stays the fallback there. Its checkbox and bar are appended to `SoundCheckboxId`/`SoundVolumeId` after C++'s own, so they inherit the exact pointer, focus and drag behaviour, and its key button reuses the classic `IDS_MSG_DEFINEKEY` capture modal. An F9 capture diff of this sheet therefore shows one extra group box below `y=398` and nothing else. The Frontend/Game groups, checkboxes and sliders route exact pointer, touch, keyboard and gamepad behavior. Live audio state, volume, tests, F3 synchronization and six-key close-save are covered; missing audio fails typed. Global startup/loading F3 and Ctrl+F3 now eagerly persist the exact `[Sound] MenuMusic`/`MenuSound` keys while Options-sheet changes retain their close-save lifecycle. Tooltips are exhaustive, and the group captions, checkbox labels and volume headings resolve through the active language table. |
| Keyboard | `C4StartupOptionsDlg.cpp:257-352` | **Complete** | The sheet is now the C++ `ControlConfigArea` walk rather than an invented grid: `ControlSheetLayout::from_sheet` replays the exact `ComponentAligner` sequence behind the dialog's own `caMain.GetWidth()/20` / `caMain.GetHeight()/40` margins (`:994`), so the selectors clamp to `fctKeyboard`'s 80px width with the slack redistributed as `iCtrlSetHMargin` (`:274-276`), the `C4GUI::HorizontalLine` sits between the two `ExpandTop`s (`:291-293`), and the key grid is float-scaled to fit with square `iKeyWdt` caps and `iKeyHgt*3` of label gutter outside each button (`:294-327`). `KeySelButton::DrawElement` is ported whole (`:216-250`): `fctKey` phase 0/1, the inset `fctCommand` under a `0x7f7f7f` blit modulation whenever the button is not focused/hovered/pressed, and the two `GetBlackFontByHeight(cgoDraw.Hgt/2 + 5)` labels — action name and `KeyCode2String` — drawn at that call's `fZoom`, turning `0xffff0000` while highlighted. The selected control set carries `IconButton::DrawElement`'s double additive `fctButtonHighlight` (`C4GuiButton.cpp:210-225`). Verified against a live 1280x720 C++ F9 capture at **98.46% bit-identical / 99.98% within +-1 LSB**, with the action label's ink box matching C++'s 58x10 exactly (`control_layout_matches_the_cpp_component_aligner_at_1280x720`, `control_sheet_dims_glyphs_and_draws_labels_and_the_separator`). |
| Key capture modal | `C4StartupOptionsDlg.cpp:160-200` | **Partial** | Device-specific capture rejects modified chords and the wrong pad, captures bare Escape ahead of Cancel and persists Config. The prompt and caption resolve `IDS_MSG_PRESSKEY`/`IDS_MSG_PRESSBTN`/`IDS_MSG_DEFINEKEY` with their positional `%s`/`%d` arguments. Remaining: the control description inside the prompt is still English, and audited-resolution render evidence is unverified. |
| Gamepad | `C4GamePadCon.cpp:295-387`; `C4StartupOptionsDlg.cpp:249-345` | **Partial** | Global enablement, GUI routing, configured legacy axes/dead zone, Windows calibration, device claims, bindings, Reset, tooltips and traversal are covered. `C4GamePadControl::FeedEvent`'s raw conversion is now ported whole: the classic key space holds every axis ordinal whose minimum extent still encodes inside `KEY_JOY_Axis1Min..=KEY_JOY_AxisMax` (0..=16, with ordinal 16 having no maximum extent and therefore no Left/Up/Right/Down alias either), hats become the axis pair `hat * 2 + 6` and balls the pair `ball * 2 + 12` — including the deliberate overlap from hat 3 onwards — and `amplify` places both exactly one step past the dead zone. Gilrs' semantic D-pad buttons are folded back into a hat bitmask so both representations run through that one conversion, and unnamed device axes are recovered from the backend event code (evdev `ABS_X..ABS_RZ` plus `ABS_HAT0X..ABS_HAT3Y`, IOKit generic-desktop `X..Rz` plus the hat switch). The set selectors now blit `fctGamepad` phases (its own image, 80px phase width, `C4GraphicsResource.cpp:229`) and the bindings use the shared key/command facets. The sheet is its own `ControlConfigArea` (`C4StartupOptionsDlg.cpp:997`), so its selector row is laid out for the live pad count -- one attached pad centres the lone selector at `iCtrlSetHMargin = (iCtrlSetWdt - iCtrlSetBtnWdt)/2` -- and the `IDS_CTL_GAMEPADFORMENU` checkbox consumes the bottom strip from the left before the reset button is taken from the right (`:332-347`). Measured 98.17% bit-identical / 99.69% within +-1 LSB against a live 1280x720 C++ capture. Remaining: gilrs exposes no trackball events on any backend, so the ported ball conversion has no live source, and its Windows backends are XInput/WGI, which have no raw ordinals at all. |
| Network | `C4StartupOptionsDlg.cpp:920-1033` | **Partial** | Network controls, alternate-server warning, update, UPnP, edits, port sanitation and collision validation are wired with tooltips and per-control traversal. Remaining: visible labels are hard-coded English rather than runtime resources. |
| Reset confirmation/quit | `C4StartupOptionsDlg.cpp:1073-1091` | **Partial** | Reset opens the classic Yes/No modal; Yes preserves reset-protected keys and requests clean exit, while No leaves Config untouched (`options_reset_confirmation_replaces_config_and_requests_clean_exit`). Remaining: audited-resolution render evidence is unverified. |
| Advanced warning/config editor | `C4StartupOptionsAdvancedConfigDialog` | **Partial** | show_options_advanced_warning reproduces the OK/Abort IDS_MSG_ADVANCED_SETTINGS_WARNING gate; AdvancedConfigController builds dynamic section tabs from a typed field/order/default/blocked-key table mirroring C4Config::CompileFunc and round-trips Save/Cancel preserving unknown keys/comments (advanced_options_click_save_and_cancel_round_trip_typed_config). `Developer.ConsoleScriptStrictness` is a typed `NonStrict`/`Strict1`/`Strict2`/`Strict3`/`MaxStrict` enum whose absent/255 sentinel resolves to current max strictness and whose explicit levels reach console-script packets. Its ordinary Dialog caption now drags with a retained offset, bounce-scrolls after 3 seconds, and exposes the title/Close tooltips through the shared 500ms mouse clock. Remaining: not every C4Config field/section has been cross-checked against the typed table. |
| Timed scale confirmation | `C4StartupOptionsDlg.cpp:107-125,1103-1148` | **Partial** | Apply opens a 12-second Yes/No dialog, updates each second, reverts on timeout/No and persists on Yes (`options_scale_test_times_out_reverts_and_yes_commits`). Remaining: caption/message are hard-coded English rather than runtime resources; audited-resolution render evidence is unverified. |
| Validation/restart/font errors | options callbacks | **Complete** | Network Back validates TCP/Ref and UDP/Disc collisions with the exact localized `IDS_ERR_CONFIG` caption and `IDS_NET_ERR_PORT_*` message before saving. Config I/O failures show the formatted `IDS_ERR_CONFSAVE` feedback and still return to Main. Font-face/size failure shows 'Error initializing fonts' and leaves prior fonts/config intact, including across a restart and a blocked config write; Reset shows the restart-required warning text. |

## About/update subtree

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Credits page/chrome | `C4StartupAboutDlg.cpp:262-307` | **Partial** | TextWindows, scrolling, focus/gamepad traversal, tooltips and classic button skin are covered. The dialog now resolves its Back/Check for updates/Licenses captions from the active language table while it is constructed, as C++ does; the mnemonic markers travel with the resolved text, so `Button::OnHotkey` follows the translation rather than the English original (`about_chrome_uses_runtime_resource_strings`). The title strip already read `IDS_DLG_ABOUT`. |
| Licenses list/text page | `C4StartupAboutDlg.cpp:188-218` | **Partial** | The second page implements classic list/text geometry, selection, independent scrolling, arrow repeat and captured scrollbar dragging. Rust includes only `COPYING`: the trademark page was deliberately removed, and it does not ingest the dependency-license corpus generated through `deps/licenses.cmake`. |
| Compatible release component installation | Rust release manifest; `clonk-update` transaction | **Partial** | The ordinary operational path is implemented: accepting a compatible release preserves its complete component/source plan, preflights space and write access, downloads with cancellation and aggregate progress, verifies declared sizes and SHA-256 digests, and hands a durable plan to an out-of-process helper. The helper waits for the runtime and launcher, applies data before binaries under a locked journal, records installed component digests inside the signed transaction, and starts an installed finisher; ordinary process failures are rolled back or recovered and reported to the relaunched app, after which the finisher removes the staged inputs and copied helper. It remains Partial because this is not the legacy `.c4u` installer, the exact `C4DownloadDlg` speed/time and relaunch-countdown presentation is absent, and hard-power recovery is not complete: recovery identifies a bundle by pathname rather than stable install identity, and the engine two-rename window can temporarily remove the executable that would start recovery. Release-owned versus user-added `content/` entries are also not yet distinguishable. These gaps are tracked in the issue tracker; the row must not be read as an unqualified rollback-safety claim. |
| Update controller / button hand-off | `C4UpdateDlg` | **Partial** | Activating Check Updates by mouse or focused keyboard runs a manual `CheckForUpdates` (`C4StartupAboutDlg.cpp:377-380`): the classic update-icon wait dialog opens over the retained About screen captioned `IDS_DLG_CHECKFORUPDATES` (C++ uses the update server address) and Abort as its only button, closing it by button, Escape or title-close abandons the check, and every verdict is a classic update-icon dialog. The verdict itself comes from `clonk-update`'s manifest comparison on a worker thread, polled from the ordinary application pass. Accepting the verdict opens a cancellable aggregate-progress dialog, and verified staging, detached apply, journaled ordinary-process rollback/recovery, installed-state recording and relaunch are wired through `clonk-game`. Remaining: the download surface does not yet mirror `C4DownloadDlg`'s per-file speed/time presentation or C++'s relaunch countdown, and the hard-power gaps are listed in the component-installation row and the issue tracker. |

## Network start/lobby subtree

The generic lobby pixels are gone. Staged ordinary hosts retain a persistent
`ClassicGameLobby`; joined clients and unstaged prepared hosts use
`NetworkLobbyState` as a state adapter that projects into and retains the same
resource-validated classic controller. Players, Teams, Resources and Scenario
are live on both paths; core Options are live only for the persistent host.
The joined-client adapter retains and routes roster, chat, Exit/right-caption
chrome and game-option-strip interaction with tooltip ownership, but still
lacks the client Options sheet.

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Initial network start wait | `C4Network2Dialogs.cpp:114-173,552-586`; `C4Network2.cpp:558-608` | **Complete** | Host start-wait owns live acknowledgements, Kick, Restart/Cancel/Close, sounds, scrolling and focus; non-host waits use the small cancel-only NetWait dialog. Both close on Go acknowledgement (`network_start_wait_tracks_only_matching_accepted_status_acknowledgements`; `selected_network_scenario_installs_prepared_host_before_admission`). |
| Password challenge and connection progress | `C4Network2.cpp:281-442,818-833`; network join callbacks | **Complete** | Password prompts and WrongPassword retries retain ordered attempts; raw connections show localized abortable NetWait and return to NetDlg on Cancel/Escape/title-close. Each dialog owns a unique cancellation signal and launcher join: abort drops the result receiver, interrupts in-flight mesh setup/dial/handshake work, releases the TCP connection and client mesh TCP/reliable-UDP resources, and completes both worker teardowns before a later attempt can start (`network_connection_progress_cancel_interrupts_inflight_transport`). |
| Host/join app entry | `C4GameLobby::MainDlg`; `GameApp::poll_startup_network_connection` | **Partial** | Staged ordinary hosts retain the persistent classic lobby; successful joined clients and unstaged prepared hosts enter the classic renderer through the state adapter. Classic `/join:` and non-update `clonk:` launch URLs query the explicit reference endpoint, default an omitted port to the selected config's `Network.PortRefServer`, select the first returned game reference and enter its reference-backed client path without showing the startup selector. `/network` on a direct scenario requests start in the same update and never presents its internal staging lobby unless `/lobby` or `/lobby:N` requested presentation. Process-local TCP/UDP bind, password, signup/league, comment and runtime-join overrides feed their existing host/client consumers. Prepared-host TCP and reliable UDP bind independently, and publication fails only when neither configured transport survives. Prepared hosting freezes the selected definition semantics, resolved resource vector and DefinitionPath spellings together; the host reload recomputes and verifies that same order before the dynamic Scenario, JoinData and client preload consume it. Remaining limitations are the recursive adapter gaps below, not an entry failure or generic fallback. |
| Joined-client/unstaged `NetworkLobbyState` adapter | no C++ state authority; classic visual authority is `C4GameLobby::MainDlg` | **Partial** | One persistent `ClassicGameLobby` survives every joined-lobby render/input projection, retaining roster focus/selection/scroll, tooltip state, chat edit capture and `TextWindow` scrolling exactly like the single native `MainDlg`. It is the single emitter for chrome interaction across pointer, touch and keyboard: sheet tabs, external chat, resource save, preload, Ready and Exit all route through its typed actions (the adapter's legacy `LobbyButton` pointer dispatch is retired), alongside the locally authorized roster actions and the right-caption context. All four sheets — including the read-only Options list — are now live on that one controller. The option strip remains in its dedicated row below. |
| Exact lobby frontend resource boundary | `C4GameLobby.cpp:141-314`; `clonk-frontend/src/game_lobby.rs::LobbyResources` | **Complete** | The validated image-sheet/font boundary is live for both render paths and fails typed before pixels. Child availability is tracked separately below. |
| Scenario/team/parameter metadata projection | `C4Scenario`, `C4TeamList`, `C4GameParameters`, `StdCompilerINIRead`; `clonk-engine/src/scenario.rs` | **Partial** | Recursive INI hierarchy, case/duplicate/default/string semantics, compound-array recovery, teams, clients, savegame/replay rules and ordinary definition resolution are projected and tested. Runtime loader resources, current roster/script names and config/network/league adjustments are owned through preparation and activation. Old exact saves with a `Game.txt [DefinitionFiles]` override remain an explicit typed boundary: C++ replaces the whole effective vector there, and Rust does not publish the pre-override selection (`old_save_definition_files_override_remains_a_typed_host_boundary`). |
| Headless record control execution | `C4Control::Execute`; `C4Playback::ExecuteControl`; `clonk-engine/src/ffi.rs` | **Complete** | The record comparator projects and executes every C++ `PC_Control` family in recorded frame/list order, including Set, client lifecycle, Vote/VoteEnd, internal team-init/surrender and opaque no-op `CID_DebugRec` packets. Independently, every outer record type at or above `0x80` consumes and retains the exact `C4PktDebugRec` raw-i32/StdBuf boundary, including mismatched embedded types, before normal playback skips the diagnostic chunk (`cpp_debug_chunks_do_not_block_later_record_controls`, `cpp_debugrec_control_round_trips_as_noop`). Host/owner authorization, observer/disconnect PlayerInfo history, approved vote flags and saved client seeding follow the replay-mode C++ branches; only genuinely unknown CIDs fail loudly with their packet name, numeric ID and frame (`ffi_record_replay_executes_all_known_control_packet_variants`). This bounded row does not claim the still-missing app-level `.c4r` launch/presentation path. |
| Application CtrlRec boundary decoding | `C4Playback::{ReadBinary,ReadText,ReWriteText,ReWriteBinary}`; `clonk-network::control_record` | **Complete** | Replay activation prefers a readable `CtrlRec.txt` entry over `CtrlRec.c4b` without falling back after a text parse failure, then shares the same complete typed chunk list with playback and `/recdump:` (`replay_prefers_and_executes_cpp_ctrlrec_text_over_binary`). Binary loading accepts clean EOF without `RCT_End` (including C++'s ignored lone header byte), stops at the first End without inspecting its suffix, and still rejects a complete known control header with a partial payload; EOF exhaustion intentionally does not synthesize End-driven replay completion (`cpp_record_parser_accepts_clean_eof_and_ignores_after_end`). `/recdump:` emits C++'s signed-frame CRLF INI grammar or canonical binary payload/frame encoding from that authoritative text-or-binary source, preserves diagnostic chunks and validated names, and reports serialization or destination failures before activation (`classic_recdump_writes_cpp_text_and_binary_outputs`). Classic compressed `.c4r` stream conversion remains tracked separately. |
| `PID_Status` / `PID_StatusAck` barrier foundation | `C4Network2Status`, `C4Network2::ChangeGameStatus`; `StatusBarrier`, `HostHandle`, `ClientHandle`, `NetworkManager` | **Complete** | Exact signed payloads, host barrier transitions, stale/duplicate rejection, client acknowledgement and real-TCP tests exist. The classic host lobby now initiates the Lobby->Go transition itself: `start_network_game_now` (reached from Start / countdown-zero) calls `NetworkManager::change_status`, gated to the host with no generic fallback. |
| Live typed-packet suffix tolerance | `C4PacketBase::unpack`, `CompileFromBuf<StdCompilerBinRead>`; `C4IDPacket::CompileFunc` | **Complete** | PostMortem, Status/StatusAck, ClientActReq, ExecSyncCtrl, JoinData, PlayerInfoUpdReq, Control and ControlPkt accept unread bytes only after their declared value or terminated typed list has compiled. Truncation, malformed declared lengths and unknown control IDs still fail; `live_cpp_packet_decoders_accept_trailing_bytes` covers every family. |
| `PID_LobbyCountdown` transport foundation | `C4PacketCountdown`, `Countdown`; `LobbyCountdown`, host/client session APIs | **Complete** | Exact codec, abort value, cadence predicate, host-only broadcast and real-TCP round trips are tested. The classic host lobby now owns the timer: `tick_network_lobby_countdown`/`HostLobbyCountdown::advance` mirror `Countdown::OnSec1Timer`'s sparse-broadcast cadence exactly, and `apply_countdown_packet` applies every transition to chat log, sounds (Elevator/Pshshsh/Fuse/Blast3/Command) and Start/Ready UI state. |
| `PID_ReadyCheck` / lobby-ready foundation | `C4PacketReadyCheck`; `ReadyCheck`, `HostHandle::set_lobby_ready`, `ClientHandle::set_lobby_ready`, `NetworkManager::set_local_ready` | **Complete** | Request/reply codec, role-correct host/client APIs, origin validation and real-TCP tests exist. The app lobby now binds them: the checkbox submits `set_local_ready` via `apply_classic_lobby_ready_change`, the roster reflects Ready/Client status, and `request_lobby_ready_check_at`/`handle_lobby_ready_check_request` implement the host cooldown, forced non-host reset and the client's Yes/No ready-check modal. |
| Main transparent chrome and responsive layout | `C4GameLobby::MainDlg::MainDlg`; `game_lobby.rs` | **Complete** | Both render paths use the loader background, responsive classic layout, fonts and sheets without generic pixels. Host and joined-client controllers retain focus, selection, scrolling, chat, option-strip and tooltip state across every sheet the native dialog builds. |
| Chat log, scrolling and input edit | `C4GameLobby.cpp:272-280,475-766,1018-1056` | **Complete** | Host and joined clients share the persistent classic controller and app-owned edit mutations: captured pointer/touch selection (including release outside and cancellation), word double-click, middle-primary insertion, exact conditional Cut/Copy/Paste/Clear/Select-all context actions, history/submission, wheel and retained scrollbar state all survive frame projections. Each appended lobby line repins the `TextWindow` bottom like C++; wheel routing is limited to its inner `ScrollWindow` (`joined_lobby_chat_routes_pointer_context_and_log_scroll`). General forwarded running-chat commands remain tracked in the in-game row below. |
| Lobby chat `C4ControlMessage` / `CID_Message` | `C4ControlMessage`, `CID_Message` (`C4Control.cpp:1071-1260`); `clonk-engine/src/control.rs::MessageControlData`; `clonk-app/src/main.rs::{parse_lobby_message_control, process_classic_lobby_chat_request}` | **Complete** | Exact codec and NORMAL/ME/TEAM/SOUND/ALERT send/receive behavior are covered, including authentication, team visibility, colors/markup/timestamps, sound/mute and attention behavior. Private messages correctly remain running-game-only under C++'s `Game.IsRunning` gate. As a presentation-only extension, Rust hosts retain a 100-packet/4096-encoded-byte FIFO of authenticated NORMAL/ME lobby controls and replay it after JoinData for late joiners; those conservative transport limits borrow the native lobby TextWindow's numeric ceilings rather than claiming identical rendered-text accounting. Audience-sensitive TEAM and one-shot SYSTEM/SOUND/ALERT controls are excluded, and the FIFO is cleared when leaving the lobby. Because replay reuses legacy controls, timestamps reflect receipt during join and ordinary nick-mention attention behavior still applies (`joining_client_receives_prior_lobby_chat_after_join_data`, `delayed_join_data_is_followed_by_prior_lobby_chat`). |
| Chat input context popup | `C4GUI::Edit::OnContext`; `LobbyChatRequest::OpenContextMenu` | **Complete** | Host and joined-client edits open and dispatch the exact conditional Cut/Copy/Paste/Clear/Select-all popup through the shared chassis. Right-click preserves the current caret/selection and uses its pointer anchor; Apps uses the focused edit center even without a live pointer. |
| Direct lobby Exit | `MainDlg::OnExitBtn`, `OnClosed` | **Complete** | Host and joined-client Exit close directly, tear down lobby/network state, return to Main and restore menu music. The retained joined controller is the single Exit emitter: pointer and focused Space/Return activation preserve native ArrowHit/Click ordering, dialog-level Escape aborts silently from any non-chat focus (the chat default keeps the adapter's silent route), and the Alt+Exit mnemonic stays silent (`joined_lobby_chrome_routes_exit_and_right_tab_context`, `joined_chrome_focused_button_activates_on_confirm_keys`). |
| Ready checkbox/loading lock | `MainDlg::OnReadyCheck`, `UpdatePreloadingGUIState`, `RequestReadyCheck` | **Complete** | `apply_classic_lobby_ready_change` submits `set_local_ready`, updates authoritative client/roster state and triggers host auto-start checks; `request_lobby_ready_check_at`/`handle_lobby_ready_check_request` implement the forced non-host reset, the host's 2s+cooldown-config rate limit and the client Yes/No modal; `sync_classic_lobby_resource_ready` gates the checkbox on live resource completion, matching `UpdatePreloadingGUIState`. Joined-client checkbox interaction (the pointer square and focused Space, with Return rerouting to the chat default) flows through the retained controller's `ReadyChanged` with the native single ArrowHit and the `OnReadyCheck` cooldown, submitting exactly once; the disabled checkbox is skipped by focus traversal. |
| Host Start/Cancel and synchronized countdown | `MainDlg::OnRunBtn`, `Start`, `OnCountdownPacket`; `Countdown` | **Complete** | `start_network_lobby_countdown_with`/`tick_network_lobby_countdown` broadcast the exact sparse cadence (mirrors `Countdown::OnSec1Timer`), apply `LobbyCountdownPacket` to UI/sounds, validate the league minimum-5s and unassociated-savegame-player warning (`Start`), and reach `NetworkManager::change_status` at zero; `/start`/`/abort` chat commands and the Cancel button share the same path. |
| Bottom lobby game-option strip | `C4GameOptionButtons`; `game_option_buttons.rs` | **Partial** | Persistent-host context, focus, sounds, locks, Internet lifecycle, Password/Comment, Fair Crew, Record and league display policy are covered. The joined adapter retains one role-synced strip (league/Fair-Crew/countdown control state, lobby bounds, render/tooltip projection) and routes its controller-emitted strip inputs: pointer press/release with chat focus preserved on clicks, touch, Tab dialog-focus stops across all strip buttons, Space/Enter with the `KeyFocusDefault` chat fallback and refocus-and-insert characters, Alt hotkeys, and gamepad traversal/Select. Enabled Record toggles config while League/Fair Crew stay inert, and the live lobby render matures the strip's 500 ms tooltip clock (`joined_lobby_game_option_strip_routes_input`). Remaining: the joined Internet/Password/Comment host controls stay host-only by role, and non-strip commands remain with the reduced adapter. |
| Password and Comment lobby input dialogs | `C4Network2Dialogs.cpp:718-776` | **Complete** | The host opens the exact shared InputDialog; an existing password clears in one click, otherwise LastPassword is preset. Validated password/comment submissions commit only after their live admission/reference side effects succeed, retain LastPassword, invalidate advertisement, log comment changes and preserve Connect-sound ordering; rejected text leaves the prior lobby state intact. |
| Right tab icon buttons | `C4GameLobby.cpp:255-264,922-998` | **Complete** | Players/Teams, Resources, Options and Scenario are live for host and joined client alike. The conditional IRC icon opens the shared standalone dialog on both paths. |
| Right-caption tab context popup | `MainDlg::OnRightTabContext` (`C4GameLobby.cpp:844-866`) | **Complete** | Both lobby paths route the flat native-order popup through the shared context controller, and both now expose the full native set: Players, optional Teams, Resources and Options. Scenario correctly remains direct-button-only (`joined_lobby_chrome_routes_exit_and_right_tab_context`). |
| Players/clients roster layout and scrolling | `C4PlayerInfoListBox.cpp:39-488,1237-1607`; `game_lobby.rs` | **Complete** | Authoritative client and PlayerInfo controls rebuild both projections through retained classic roster state. Semantic selection, roster focus (including its team/Add child) and roster scrolling deliberately survive joined-client render frames, row reordering and authoritative refreshes under the retained-controller contract shared with the persistent-host path. |
| Player row context root | `C4PlayerInfoListBox.cpp:490-533` | **Complete** | Persistent-host and joined-client rows open the exact root, including empty unauthorized roots, with conditional Take Over, Remove and New Color ordering, permissions, tooltips, icons and accelerators. |
| Nested Take Over submenu | `C4PlayerInfoListBox.cpp:535-572` | **Complete** | Both lobby paths enumerate the local client's PlayerInfo packet in order, filter ineligible entries, present exact `Using %s` children and submit a fresh full-packet association without optimistic mutation. The children fill at submenu-open through the deferred `OnSubcontext` callback and refill on every re-open, so PlayerInfo updates arriving while the root menu is open are reflected live (`takeover_submenu_fills_live_at_open`). |
| Player Remove control | `C4PlayerInfoListBox.cpp:574-600` | **Complete** | Host-manageable and joined local-owner contexts revalidate ownership, swap-remove from a cloned full packet, submit without confirmation and clear league authentication; a host removal also aborts its locked/final countdown first. |
| Player New Color action | `C4PlayerInfoListBox.cpp:602-616` | **Complete** | Persistent-host and joined local-owner contexts revalidate ownership and submit a cloned full packet with only the original color restored. |
| Player team combo/dropdown | `C4PlayerInfoListBox.cpp:618-646` | **Complete** | Host and joined-client popups apply native ownership, distribution, capacity, join/savegame and countdown gates, submit a full PlayerInfo update with only Team changed and wait for its authoritative echo. |
| Restart player-team restoration | `C4PlayerInfoListBox.cpp:170-181`; `C4Game.cpp:2390-2397` | **Complete** | A same-scenario Restart preserves the full-game PlayerInfo name/team snapshot and raw `SetRestoreInfos` mask into the next host lobby. Newly constructed, exact-name-matched User rows submit a cloned full owner packet when their recorded team differs, while the mask/type/equal-team gates remain inert; actual Next Mission clears the snapshot like C++. |
| Client rows/status/ping/sound/add button | `C4PlayerInfoListBox.cpp:737-916` | **Complete** | Live rows, transient sound icon, ping routing, chunk-weighted progress and locally authorized Add Player activation work on both lobby paths; local/disconnected telemetry suppression and completion text match C++. The ping column shows `getLag()`: the route registries mirror the connection's dispatched/answered ping counters, so an unanswered ping reports its growing wait once it exceeds the measured round trip, while `getPingTime()` consumers (debug status text, activation requests) keep the raw measurement (`route_ping_lag_mirrors_cpp_get_lag_branches`, `classic_lobby_ping_prefers_positive_message_connection_over_data`). |
| Client context Mute/Kick/Activate/Info | `C4PlayerInfoListBox.cpp:918-979` | **Complete** | Real host and joined-client rosters expose the exact ordered, iconless actions: remote Mute/Unmute, host-only Kick and Activate/Deactivate for every registered network client (including the local host row), then Info. Mute is local-only, activation submits `CLIENT_UPDATE_ACTIVATE` and waits for the authoritative echo, the local-host Kick callback remains safely inert, and Info opens the modal per-client detail panel. |
| Add Player selector | `C4PlayerInfoListBox.cpp:981-985`, `MainDlg::OnClientAddPlayer` | **Complete** | Host and joined-client Add Player activation refreshes the shared C4PlayerSelDlg files, preserves physical/wire-relative names, publishes the resource and waits for authoritative PlayerInfo; the host also aborts a locked/final countdown before submission. |
| Team header rows and move-local-players action | `C4PlayerInfoListBox.cpp:989-1110` | **Complete** | Normal/random headers, icons, names and tooltips are shared by both projections. Double-click applies native local-team permissions and submits one full local PlayerInfo packet on either lobby path; the joined path shares the host's roster-scoped click pairing plus the press-classified SDL LeftDouble channel (`joined_roster_double_click_is_roster_scoped`). |
| Free-savegame player group | `C4PlayerInfoListBox.cpp:1112-1143` | **Complete** | Host and joined-client paths have localized restore projection, Take Over root, filtered nested chooser with live submenu-open fill (tracked above) and full-packet association. Offline `RestoreSavegameInfos` now reports its weaker passes the way C++ does: any assignment made past `PML_PlrName` logs `IDS_MSG_PLAYERASSIGNMENT` on every path, and a graphical non-replay session additionally shows one `IDS_MSG_FREESAVEGAMEPLRS` modal per wild match whose checkbox persists `Startup.HideMsgPlrTakeOver`. The warning is informational - the association is already made when it appears - and exact filename/player-name matches stay silent (`offline_wild_takeover_reports_only_matches_past_player_name`; `offline_wild_takeover_logs_and_presents_hideable_warning`). |
| Script-player group and Add action | `C4PlayerInfoListBox.cpp:1146-1210` | **Complete** | The live projection gates the Add row by `MaxScriptPlayers`, selects the first unused configured script name, falls back through `SafeRandom` to that same list when every name is active, and submits the host PlayerInfo creation request with three fresh `SafeRandom(302)` color-channel draws. |
| Replay-player group | `C4PlayerInfoListBox.cpp:1212-1235` | **Complete** | Replay-flagged staged scenarios construct the localized record-icon group in global player-ID order, retain removed-but-visible rows, move active visible script players into the following script group, and preserve that exact projection across live roster synchronization. |
| Teams-filtered sidebar mode | `MainDlg::OnTabTeams`, `C4PlayerInfoListBox::PILBM_LobbyTeamSort` | **Complete** | The roster switches to team/member order, filters invisible or deactivated-client players, retains empty-team headers, skips unused generated teams and builds the lazy Random header. Both lobby paths preserve focus, scrolling and surviving semantic selection across switching and authoritative refreshes. |
| Resources list/progress | `C4Network2ResDlg.cpp:32-87,158-216` | **Complete** | Resource IDs reconcile in order with basename/icon rows, integer chunk progress and hidden-at-100 labels. Per-chunk backend telemetry updates only the active sheet's visible snapshot; reopening forces the retained latest state. |
| Resource Save/overwrite/success/error branch | `C4Network2ResDlg.cpp:88-157` | **Complete** | Resource rows expose the exact conditional phase-13 Save button from explicit local/remote completion provenance and the native Scenario/Definitions/AllowPlayerSave type gate. Client and host actions re-resolve the resource ID, preserve the raw WorkPath guard and `.c4p` PlayerPath routing, then use the shared MessageDialog stack for overwrite decline/accept and localized success/error outcomes. |
| Blocking network resource retrieval | `C4Network2::RetrieveRes` (`C4Network2.cpp:1852-1937`) | **Complete** | A reusable classic `ProgressDialog` presents `GUIProgress` percentage, NetWait icon and the native localized Cancel while client startup resolves scenario, dynamic and ordered game resources or a runtime `JoinPlayer` waits for its player file. One active-resource clock resets on every changed percentage, emits the native fatal timeout after 100 seconds of unchanged progress and releases the appropriate startup/player caller; completion and transfer failure dismiss the wait (`progress_dialog.rs`; `BlockingResourceWait`; `headless_client_join_tracks_slow_resource_then_cancel_aborts`; `unknown_loadable_resource_join_stalls_until_resource_completion`). |
| Runtime-join synchronized dynamic regeneration | `C4Network2::SendJoinData`, `OnGameSynchronized`, `CreateDynamic`, `RemoveDynamic`; `C4GameSaveNetwork(false)` | **Complete** | A stale/missing runtime dynamic coalesces waiting clients behind the exact synchronized control seam, captures a fresh native C4 save from live engine/player state, publishes its dynamic resource and updated JoinData at that control tick, then retires the superseded resource; delayed already-satisfied join events do not repeat the full capture. Creation failures remove every waiting client with the classic error reason instead of leaving occupied slots. The serializer and restore path cover exact/diff landscape state, material enumeration, identity-bearing loaded/runtime `C4String` enumeration and value serialization (including unenumerated `S-1` and noncanonical bool payloads), active/inactive objects and pointer order/default caches, independent player view state, Game/player sections, teams, round results, departure-time-frozen modified scenario sections/components, restore infos, fresh embedded `.c4p` groups in C4Group sort order, and ordered player recreation with current client/control rebinding. |
| Preload button, automatic preload and failure log | `C4GameLobby.cpp:231-245,766-842,1001-1016` | **Complete** | Staged classic hosts, catalog/CLI hosts, and synchronized clients share the non-player resource-completion/one-shot eligibility gate. Manual mode reserves the native 32px Resources-sheet strip, reveals the button only while eligible, retains it after a setup/thread-launch rejection, and restores the list height after a successful launch; automatic mode fires once on the same edge. The owned, named background worker caches raw-load-key/effective-definition-matched game graphics plus material textures/render metadata, and catalog hosts or clients also hand their parsed scenario to startup; host Start or client GO waits for the worker. Client preload composes `Combined<client>.c4s` through an owned staging file, commits it only for matching JoinData, retires the dynamic resource asynchronously, and removes unconsumed owned files on lobby teardown. That composition overlays top-level names by ASCII-folded legacy bytes, carries ordinary child-group images opaquely, and reproduces the native colliding-`Material.c4g` quirk: scenario contents survive while that child alone is unpacked and rebuilt. Extracted ordinary group-looking files use top-level C4Group envelope classification, so raw unwrapped images remain files while wrapped groups are promoted (`retrieve_scenario_classifies_extracted_group_files_like_top_level_cpp`). Setup/thread-launch rejection appends localized `IDS_ERR_PRELOADING` in red without an error sound; unmatched activation uses the normal load path. |
| Options list container | `C4GameOptions.cpp:28-310` | **Complete** | Host and joined client share the exact scrollable stacked-dropdown list, `Activate` forced update, one-second refresh cadence, inset, focus/selection/traversal and context-menu input on one retained controller each. A joined client's rows are all read-only, so its ComboBoxes never open, matching `ComboBox::SetReadOnly` (`joined_lobby_options_sheet_projects_read_only_rows`). |
| Options / Control mode | `C4GameOptionsList::OptionControlMode` | **Complete** | Present and read-only for host and joined client alike with correct order and refresh; the lobby list is never runtime, so `!IsRuntime()` makes the row read-only for both roles. The joined value follows the committed network status. |
| Options / Control rate | `OptionControlRate` | **Complete** | Persistent-host values 1-9, edit gate, relative `CID_Set` submission and authoritative echo are covered, and the joined client shows the same row read-only off the control host, tracking the synchronized rate. |
| Options / Runtime join | `OptionRuntimeJoin` | **Complete** | The host-only editable row exposes prohibited/allowed, persists the inverse `Network.NoRuntimeJoin` value and updates the prepared admission policy. Lobby exit applies the Go status and admission gate in one acknowledged host-loop command, preventing a late join from interleaving between them; visibility, persistence, atomic transition and worker-error propagation are tested. |
| Options / Team distribution | `OptionTeamDist` | **Complete** | The persistent host projects the scenario-gated native choices and submits synchronized `C4CVT_TeamDistribution` controls. A joined client shows the same row read-only, projected from the `C4TeamList` JoinData carries verbatim, and `HasTeamDistOptions()` is `fActive` on both sides. |
| Options / Team colors | `OptionTeamColors` | **Complete** | The persistent host projects enabled/disabled and waits for the synchronized `C4CVT_TeamColors` echo; a joined client shows the same row read-only from its `C4TeamList` copy, gated on `IsMultiTeams()` exactly as the host is. |
| Options / Random team count | `OptionRandomTeamCount` | **Complete** | Host-only random/random-invisible visibility, Automatic/2..max ranges, direct team recreation, JoinData publication and dependent row refresh match the native lobby. |
| Scenario-description sheet | `C4GameLobby.cpp:64-137`, `ScenDesc` | **Complete** | Host/client Scenario tabs use the undecorated scrolling TextWindow geometry, refresh localized loading percentage only while active, load exact `Desc{}.rtf` through the language sequence as plain text (or title/load-error fallback), stop component input at the first native NUL, and stop after the terminal result. Covered by `*_scenario_description_*`/`scenario_sheet_uses_text_window_geometry_and_scrolls_overflow` and `lobby_scenario_description_ignores_bytes_after_native_nul` frontend/app tests. |
| Optional IRC/chat window button | `MainDlg::OnBtnChat`, `C4ChatDlg` | **Complete** | The lobby icon, runtime HUD Chat button and Alt+C open or raise one standalone classic dialog without conflating it with synchronized game/lobby chat. UI close destroys only dialog state while the process IRC connection survives (`standalone_irc_entry_points_share_the_singleton_dialog_and_alt_c_toggles_it`; `active_irc_runtime_hud_chat_button_opens_ui_without_disconnecting_transport`). |
| Per-client information dialog | `C4Network2Dialogs.cpp:42-110`, `C4Network2ClientDlg` | **Complete** | The shared runtime/lobby panel uses the reusable configurable-line-count InfoDialog scrolling chassis: wheel/scrollbar (plus Rust keyboard accessibility) reaches vertical overflow, and the scheduler-owned one-second snapshot refresh preserves its absolute pixel offset. The dialog binds to a client *id* like `UpdateText` does: a stale or departed id renders the active-language `IDS_NET_CLIENT_INFO_UNKNOWNID` line and keeps the dialog open, while a network host appends the active-language acknowledgement marker to the native format for an unready remote. Status/role/name, address, message/data connection ordering and every body branch resolve active-language `IDS_MSG_*`/`IDS_NET_CLIENT_INFO_*` resources with English fallback (`client_info_text_shows_unknown_ids_and_the_host_ack_marker`, `client_info_ack_marker_needs_a_host_and_an_unready_net_client`, `client_info_dialog_shows_unknown_id_and_host_unacknowledged_marker`, `client_info_uses_active_resources_for_native_arguments`). |

## In-game main-menu subtree

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Player/observer main page | `C4MainMenu.cpp:643-715,734-948` | **Complete** | Native visibility uses live player, network, league, fullscreen and team state, and every entry routes to its child/action. Every visible caption and tooltip in the whole in-game menu subtree now resolves through the active language table at page-construction time, the way `LoadResStr` does, with the shipped `LanguageUS.txt` value as the per-key fallback C4ResStrTable uses (`ingame_menu_uses_active_language_resources_for_all_pages`). |
| Goals list | `ActivateGoals` | **Complete** | Rows use the definition description, fulfilled goals carry the scaled Captain overlay, and selection queues CID_ActivateGameGoalRule locally or networked while failure to resolve a live object reaches the distinct `ClassicIngameMenuChild::GoalInfo` typed boundary. Production definition collection now crops symbols only from the decoded root base Graphics bitmap: missing base or malformed recognized base/additional Graphics log and omit the individual definition, while a shape-zero fallback or fully transparent crop remains visually blank. A defensive checked accessor rejects impossible dangling menu IDs before icon-cache mutation. The recursive `Activate(player)` subtree is now covered by a representative per family. The shipped inventory is 138 Goal/Rule definition directories carrying 106 distinct C4IDs — the same id ships in several scenarios with an identical script — of which 131 directories (100 distinct ids) define `Activate`. Every one falls into one of six families, each pinned by a test in `goal_rule_activate_families`: 115 defer to the shipped `MessageWindow` global and open a one-column Info menu on the cursor; 10 build their own five-column Normal chooser whose command object is the *goal*, not the cursor showing it; 3 guard on `GetPlayerByIndex()` and answer non-hosts with `PlayerMessage`; 1 hangs an effect on the cursor and opens nothing; 1 asks the scenario through `GameCall` and removes the crew only when it declines; 1 issues `SetCommand(clonk, "Context", ...)` and lets the command layer raise the menu, carrying its target in Target2 rather than Target. The remaining 7 define no `Activate` at all and must stay selectable and do nothing, because `ActivateGoals` builds its rows from the goal ID list without asking whether the definition implements anything. |
| Rules list | `ActivateRules` | **Complete** | Rows use the definition description, selection queues the shared CID_ActivateGameGoalRule dispatch, and failure to resolve a live object reaches the distinct `ClassicIngameMenuChild::RuleInfo` typed boundary. Goal/rule symbols share the production graphics rejection and defensive cache-invariant handling described above. The recursive `Activate(player)` subtree is shared with the Goals list and is covered by the same six family representatives described in that row; `MessageWindow` itself is not an engine function but a shipped global in `planet/System.c4g/Helpers.c:5-16`, compiled and driven by those tests rather than trusted. |
| Hostility | `ActivateHostility` | **Complete** | Native-order visible-player rows, directional captions/tooltips, cached BigIcon or owner-colored Crew symbols, and the hostile overlay refresh on Tick35 in the permanent normal-grid page; toggles submit authenticated `CID_ToggleHostility` controls and honor `IsHostilityChangeAllowed()`. |
| Initial team selection/team switch | `C4Player.cpp:1797-1807`; `C4MainMenu.cpp:175-232,909-918` | **Complete** | Initial selection and synchronized mid-round switching use live ordered teams, sole-team bypass and the native `IconSpec` → occupied Crew → generic icon chain. `ActivateMenuTeamSelection` passes no `iStyle`, so both pages are `C4MN_Style_Normal`: the five-column 35px grid, the selected row's caption installed as the title, and `COM_MenuUp`/`Down` stepping whole grid rows. Only the alignment differs — the mid-round switch anchors `Left|Bottom` while the initial selection leaves `Alignment` at zero and is centered in the viewport. Both pages now take `C4Menu::Execute`'s Tick35 refill, rebuilding membership captions and the generated-team row in place through `ClearItems(false)` + `AdjustSelection`, so the dragged position, scroll, tooltip age and numeric selection survive and only an out-of-range row is clamped (`team_switch_menu_refills_membership_and_preserves_selection_like_tick35`, `team_pages_use_the_normal_grid_and_only_switching_anchors_bottom_left`). |
| Observer target/free view | `ActivateObserver` | **Complete** | The NO_OWNER page lists free view followed by every non-invisible runtime player in player-list order, preselects the current target, and uses one retained-viewport transition for keyboard/hover live preview and Enter. The viewport's durable ownerless identity survives temporary player targets. Covered by `observer_menu_lists_players_and_live_previews_selection`. |
| Runtime player join | `ActivateNewPlayer` | **Complete** | DirectoryIterator-order discovery skips in-use/directory groups and closes when full (`activate_new_player_lists_cpp_eligible_files_in_source_order_and_closes_when_full`). Network Join dispatch works for hosts and clients (`active_network_host/client_runtime_join_*`). Offline `CtrlJoinLocalNoNetwork` now applies resource-free AddPlayers/PlayerInfo and performs a locally controlled filename join; unreadable files fail through a typed menu boundary without status-text mutation (`offline_runtime_join_player_local_no_network`). |
| Ten save slots | `ActivateSavegame`; `C4StartupScenSelDlg::OnShown`, `SubFolder::Start` | **Complete** | The configured `General.SaveGameFolder`, classic old-style numeric-parent/new-style trailing-digit `ScenName.c4f/ScenNameN.c4s` layout, localized folder `Title.txt` metadata, packed native live-save C4Groups, numbered core icons, descriptions and embedded 200×150 `Title.png` are implemented. Startup discovery retains the configured save root as one classic folder row—even at an absolute external path—so its per-scenario folders and individual native saves can be opened and resumed through the ordinary offline loader. Slot occupancy requires a valid packed or folder C4Group. Covered by `save_to_slot_writes_native_c4group_savegame`, `savegame_slot_probe_uses_c4group_validity` and `configured_native_savegames_folder_is_browsable_and_selects_a_resume`. |
| Options | `C4MainMenu.cpp:553-580` | **Complete** | Music and Sound toggle native runtime state, persist classic keys and reopen at the prior selection. Mouse control is scoped to the requesting player with native shown/on/off rules. Every visible caption and tooltip in the whole in-game menu subtree now resolves through the active language table at page-construction time, the way `LoadResStr` does, with the shipped `LanguageUS.txt` value as the per-key fallback C4ResStrTable uses (`ingame_menu_uses_active_language_resources_for_all_pages`). |
| Display | `ActivateDisplay` | **Complete** | Player/Clonk-name toggles now compose and draw the classic crew label, Clock/FPS feed the upper-board right-side rows, fullscreen-only entries follow the live window mode, and Hide/Small/Mini use their native board geometry. Captions, the `IDS_MNU_UPPERBOARD: <mode>` composition and both name tooltips are runtime-localized. The five persisted Display values remain process-local until an explicit C++ save site (`C4Application.cpp:351-367`, `C4StartupOptionsDlg.cpp:1182-1183` or `C4StartupNetDlg.cpp:312-315`), then reopen with the prior selection and preserve unrelated config entries. |
| Host disconnect/client kick | `ActivateHost` | **Complete** | The permanent client-ID-ordered page lists every client with the native Host/Client/ObserverClient icon and selectable host no-op; league clients with live players submit `VT_Kick`, while other remotes submit the `IDS_MSG_KICKBYMENU` removal and close the page. |
| Client disconnect | `ActivateClient` | **Complete** | The Yes/No page is captioned `IDS_MENU_DISCONNECTFROMSERVER` ("Disconnect from host?") with `IDS_BTN_YES`/`IDS_BTN_NO` rows. Part is fully wired: ordinary Part records the first localized `NR_NetError` result before graceful teardown and the ChangeToLocal transition, including league observers without a local player; league Part with a local player submits an authenticated self-kick vote (`non_league_network_part_continues_the_running_round_locally`, `league_observer_part_uses_ordinary_network_clear_path`, `league_network_part_submits_authenticated_self_kick_vote`). |
| Surrender | `C4MainMenu.cpp:538-551,790-795`; `C4Player.cpp:930-979` | **Complete** | Offline and network paths queue authenticated `CID_SurrenderPlayer`; surrender is idempotent, marks the player inactive, retires after 60 frames and enters ordinary evaluation. The confirmation page is captioned `IDS_MENU_SURRENDER` ("Are you sure?"), not the main-menu row's `IDS_MENU_CPSURRENDER`, and its buttons are `IDS_BTN_YES`/`IDS_BTN_NO`. Covered by `surrender_from_menu_ends_local_round`, `network_surrender_menu_queues_the_next_authenticated_control_tick` and `surrender_control_requires_the_runtime_player_owner`. |
| Abort/restart/no | `C4Game.cpp:4257-4279`; `C4GameDialogs.cpp:33-128`, `C4AbortGameDialog`; `C4FullScreen.cpp:556-566` | **Complete** | Bare or Logo+Escape, the in-game Abort action, and a running window close open the unique screen-centered exclusive dialog with Exit icon, localized body/caption, focused Yes, native 300px Yes/No or control-host/Film2 400px Yes/Restart/No geometry, title Close and keyboard traversal. Offline opening owns one counted `HaltCount` lease, so stacked pause state and the Pause overlay survive every normal/silent close; network opening owns none. No/Escape/Close clear every local player's pressed commands. Yes and Restart release the lease before routing through the exact hard-abort/league matrix: hosts start `VT_Cancel`, non-hosts with a local player start self-`VT_Kick`, and all other cases hard-abort after callback-free player removal. Restart preserves restore state and schedules the same scenario across rejected voting until the later hard quit consumes it (`abort_dialog_uses_stacked_halt_and_preserves_prior_pause`; `league_abort_confirmation_routes_cancel_and_self_kick_votes`). |
| Mouse hit testing/scrollbars/tooltips | `C4Menu.cpp:433-523,751-789`; `C4GuiContainers.cpp:309-470` | **Complete** | Keyboard navigation, title Close, selection-follow scrolling, wheel input and title drag are covered. Disabled mouse control is gated before world mouse-down. Overflow scrollbars are now drawn and operable: `clonk-app-menus::scrollbar` is the single `C4GUI::ScrollBar` model — arrow/pin/track hit regions, proportional pin placement, drag-to-scroll and the three-cell facet draw — shared by the in-game chassis (Normal and Context), the `engine_script_menu_layout` path (Info and the engine object menu) and the evaluation dialog. An overflowing in-game menu is widened by `C4GUI_ScrollBarWdt` as `C4Menu::InitSize` does (`C4Menu.cpp:776`), which the port previously omitted. Dialog-style menus deliberately have no bar: `C4Menu.cpp:765-771` auto-enlarges them vertically and sets `fBarNeeded = false` (`overflow_menu_scrollbar_arrows_track_and_thumb_match_cpp`, `engine_menu_overflow_scrollbar_routes_arrows_track_and_thumb`, `ingame_menu_overflow_widens_for_the_scroll_bar`). Arrow auto-repeat matches `ScrollBar::DrawElement`'s one-unit-per-drawn-frame step, held by `press_scrollbar` and cleared by `release_scrollbar` (`C4GuiContainers.cpp:446-457`, whose own comment notes there is no OnIdle or timer). Clicking the bare track **jumps** the thumb to the pointer and begins dragging rather than paging (`:414-423`). A held arrow draws from its pressed cell — `fctScrollDTop` at column 16 row 0 and `fctScrollDBottom` at column 16 row 32 (`C4Gui.cpp:209,214`; `C4GuiContainers.cpp:459-461`) — and the `ArrowHit`/`Command` sounds are raised on the same transitions C++ uses: `ArrowHit` only when the held state changes, `Command` when a pin or track press starts a drag (`:421,427`). |
| In-game/script menu font and sheet boundary | `C4GUI::Resource`, `C4Menu::Draw`; `clonk-app-menus/src/ingame_menu.rs`, `clonk-app/src/main.rs` | **Complete** | Render and pointer paths preflight the current font/sheet set and every requested item picture across all four styles, including non-TextSpec recipes and Captain. Failures are typed before pixels or world input. |
| HUD resource boundary | `C4GraphicsResource.cpp:199-232`; player HUD; `clonk-frontend/src/hud.rs` | **Partial** | Running presentation now rejects every mandatory game/HUD facet `C4GraphicsResource::Init` loads - Control, Fire, Background, Flag, Crew, Score, Wealth, Player, Rank, Captain, SelectMark, Menu, Logo, Construction, Energy, Magic, UpperBoard, Arrow, Exit, Hand, Gamepad, Build and EnergyBars - through one logged typed boundary before pixels or pointer regions, reported in the native load order; none of them can reach the frame as an absent `HudGraphics` option (`running_hud_rejects_each_missing_mandatory_graphics_facet`). Because that gate is the earliest, the Player/Score/Crew facets the evaluation dialog also draws are reported by it rather than by the game-over dialog's own recursive inventory, which keeps its GUI-sheet entries. `Options`, the cursor atlas and `Liquid` retain their separate gates. The cursor's energy/magic/breath group is now transient like C++: `C4Object::ViewEnergy` is a `// NoSave //` timer refreshed to `C4ViewDelay` (100) by `DoEnergy`, `DoBreath`, the asphyxiation arm and `FnDoMagicEnergy`, decremented once at the end of each `C4Object::Execute`, and `DrawCursorInfo` skips the whole group unless it is live or `Config.Graphics.ShowPlayerHUDAlways` is set. It is live object state, so it is excluded from the serialized `EngineState` and from the compared `ObjectSnapshot` alike (`view_energy_triggers_for_energy_breath_and_magic_and_expires_after_100_ticks`; `cursor_bars_require_view_energy_when_always_hud_is_disabled`). |

## Object-menu subtree

Construction, Activate, and Get requests now open real engine-owned
`C4ObjectMenu` instances rendered through the shared classic presentation, not
rejected at creation. `ClassicObjectMenuBoundary` retains only Activate/Get
variants and both are now unreachable dead code in `handle_menu_requests`; the
formerly described Inventory/Container/Build boundary modes no longer exist in
the enum. Buy/Sell/Contents/Info/Context were already engine-owned.

| Menu identification/style | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Construction (`C4MN_Construction`) | `C4Command::Construct`; `C4Object::ActivateMenu` | **Complete** | Knowledge-driven Structure rows, custom components, costs, symbol and drag-out site placement are covered. CanConstruct gates the request, and shared persistent scrolling, wheel input and title drag are live without an app boundary or generic pane. |
| Activate (`C4MN_Activate`) | `C4Command::Activate`; `C4Object::ActivateMenu`; `C4ObjectMenu` | **Complete** | Implicit Take and explicit Target2 open the engine-owned menu with RejectContents, grouped pictures, both commands, ID-preserving selection and refill. Shared persistent scrolling, wheel input and title drag are covered. |
| Get (`C4MN_Get`) | `C4ObjectMenu` | **Complete** | RejectContents, carryable get/activate choice, entrance-forced Get, grouping, both commands, refill and shared scroll/wheel/title-drag behavior are covered. |
| Buy (`C4MN_Buy`) | `C4ObjectMenu` | **Complete** | HomeBaseMaterial order, script-aware pricing, BASEFUNC_Buy availability, refill and shared scroll/wheel/title-drag behavior are covered. |
| Sell (`C4MN_Sell`) | `C4ObjectMenu` | **Complete** | NoSell, script-aware pricing, Hostile(Base), picture grouping, refill and shared scroll/wheel/title-drag behavior are covered. |
| Context (`C4MN_Context`) | `C4ObjectMenu` | **Complete** | C++-ordered context functions and native rows are covered. The retained C4Aul function ledger now preserves reverse linked-list order across local declarations, high-priority appends, low-priority includes and engine globals for every ActionContext/Fx/Attach/Context consumer (`linked_context_menu_functions_preserve_cpp_func_list_order`), including newest exact-name overload suppression. Free click anchors, one-time clamping, retained pixel scrolling, wheel input, title drag and resize reset match the shared menu behavior. |
| Info (`C4MN_Info`) | `C4Object.cpp:2008-2027,6133-6159`; `C4ObjectMenu.cpp:366-414` | **Complete** | Target picture/title, target-relative placement, wrapped `InfoCaption`, and ordered static plus `Fx*Info` text render. The effect walk is live rather than a clone: `object_effect_info_lines` resolves each successor from the host context *after* the callback ran, matching `for (C4Effect *pEff = pEffects; pEff; pEff = pEff->pNext)` (`C4Object.cpp:6140-6158`), so an `Fx*Info` hook that adds an effect behind the cursor is visited in the same pass. There is deliberately no dead-effect filter — `C4Effect::DoCall` (`C4Effect.cpp:439-457`) has no `IsDead` gate and `GetInfoString` adds none, unlike `DoDamage` — and insertion order follows `C4Effect::C4Effect` (`C4Effect.cpp:80-93`), which puts a new effect at the head unless `Abs(pPrev->iPriority) < iPrio`, so only a higher-priority addition lands after the cursor (`object_info_menu_walks_effects_added_during_an_info_callback`). Markup carries no advance of its own, so wrapping a row in `<i>`, nesting it or coloring it leaves every line width, wrap point and row hit rectangle exactly where the plain text put them, while an inline `{{image}}` does occupy width and reflows the row (`info_markup_leaves_row_geometry_alone_while_inline_images_move_it`). |
| Contents (`C4MN_Contents`) | `C4ObjectMenu` | **Complete** | CollectionLimit/RejectCollection downgrade, entrance-forced Get, grouping, refill and shared scroll/wheel/title-drag behavior are covered. Retained `C4OS_INACTIVE` entries participate in `Contents.GetObject`/`ObjectCount`, mass and collection-limit decisions. `Contents.Find`/`ObjectCount` skip status-zero or missing holes, while native menu-row traversal follows raw links through `AssignRemoval`'s pre-unlink callback window. Command, action and container references retain their raw C++ pointer identity until `ClearPointers`, including detached callback continuations; live and script-preview Put/Get/Activate/Drop/Throw paths keep that pointer-vs-list distinction (`command_references_accept_inactive_status_and_contents_like_cpp`). Script-time Contents refill grouping and script `ShiftContents` share the exact live `C4Object::CanConcatPictureWith` predicate, including `APS_*` exceptions and same-call appearance changes; ordinary engine grouping uses the equivalent exact comparator (`shift_contents_skips_only_cpp_picture_concat_equivalents`). |
| Selection-follow scrolling/pointer drag | `C4Menu.cpp:433-523,751-789`; `C4GuiContainers.cpp:309-470`; `C4ObjectMenu.cpp:448-503` | **Partial** | Construct drag-out, retained pixel scrolling, wheel clamping, selection visibility, title drag and resize reset are covered. Remaining: overflow object menus reserve scrollbar width but do not render or hit-test native arrows, track or draggable thumb. |

## Script-created menu grammar

| Feature | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Script function declaration arity | `C4Aul.h:54`; `C4AulParse.cpp:1624-1648` | **Complete** | New-style declarations allow at most ten syntactic comma-delimited parameters. Duplicate names still consume the cap; the native `)`/limit/`...` check order preserves ten names, nine names plus ellipsis and a trailing comma while rejecting an eleventh name or ten names plus ellipsis (`function_declaration_rejects_eleventh_parameter`; `function_parameter_limit_preserves_cpp_boundary_order`). |
| Script parameter type grammar | `C4AulParse.cpp:1659-1677` | **Complete** | Declarations recognize exactly the case-sensitive C++ set `int`, `bool`, `id`, `object`, `string`, `array`, `map` and `any`; Rust-only proplist/effect/nil/union annotations are rejected, while the pre-STRICT2 `|nil` spelling retains native identifier tokenization without becoming a union. A recognized type without a following name becomes the native warning and untyped parameter below STRICT2, but rejects the declaration at STRICT2+ (`nonlegacy_parameter_type_extensions_are_rejected`; `strict_two_rejects_parameter_named_only_by_type`). |
| Script strict-level syntax admission | `C4AulParse.cpp:666-699,2941-2955,3069-3103,3130-3148` | **Complete** | The shared script parser treats `nil` as an ordinary identifier below STRICT3 and as the reserved literal at STRICT3, rejects every array literal/index/append form in NONSTRICT, and admits map literals and dot access only at STRICT3. The exact boundaries are pinned by `nil_is_contextual_below_strict_three`, `array_syntax_requires_strict_one` and `map_and_dot_require_strict_three`; bracketed function descriptions, concatenation dots and STRICT1+ string-key indexing retain their separate grammar paths. |
| Script static-constant initializers | `C4AulParse.cpp:3396-3439` | **Complete** | The preparser admits only a direct nil, signed decimal integer, unsigned integer (including hexadecimal), bool, string, C4ID, or previously registered script/engine constant. Calls, grouping, operators and containers recover as declaration diagnostics, while unresolved names stop registration with a link diagnostic instead of silently becoming nil; built-ins and declarations resolve left-to-right (`static_const_rejects_nonliteral_nonconstant_initializer`; `prior_static_constants_resolve_in_declaration_order`). |
| DirectExec trace/profiler diagnostics | `C4AulExec.cpp:65-105,137-179,1526-1605,1658-1705` | **Complete** | Console, menu-command and `eval` execution owns a balanced temporary diagnostic frame named `<context> in <receiver ScriptName>`, using the full loaded host name and live object data string. Trace entry/anonymous-return depth and lifetime match the C++ context across nested success, parse failure and runtime-error unwind; errors snapshot the temporary frames before unwind so fail-safe `by:` output renders the raw DirectExec context without a fake call or source suffix. The singleton profiler preserves native start/stop/error and nested-timestamp behavior and aggregates completed DirectExec time into one target-independent `Direct exec` row (`direct_exec_arms_trace_and_reports_direct_exec_profile_entry`; `eval_direct_exec_frames_use_cpp_names_and_balance_on_errors`; `direct_exec_stack_display_and_targeted_profiler_match_cpp`; `failsafe_runtime_error_logs_cpp_call_stack_frames`). |
| Legacy `@` identifiers | `C4AulParse.cpp:616-671` | **Complete** | The lexer admits `@` as the initial byte of an identifier only below STRICT2, consumes the ordinary alphanumeric/underscore continuation, and retains the modern hard error at STRICT2+ (`at_prefixed_identifier_is_legacy_only`). |
| Inherited-call strictness | `C4AulParse.cpp:2775-2798` | **Complete** | Unqualified `inherited` and `_inherited` emit the native recovered diagnostic in NONSTRICT origin scripts and remain available from STRICT1 upward; safe no-parent calls still evaluate discarded arguments. Arrow and `global->` methods with those names remain ordinary calls. All shipped content users already declare strict mode (`nonstrict_inherited_is_rejected`; `safe_inherited_without_parent_evaluates_discarded_arguments`). |
| Legacy NONSTRICT `goto` return | `C4AulParse.cpp:2193-2199,2240-2248` | **Complete** | A statement beginning with the bare lowercase `goto(...)` call returns that call's value immediately only for a NONSTRICT origin function, including before a parsed expression suffix. STRICT1+ and prefixed, parenthesized or embedded calls retain ordinary continuation (`nonstrict_standalone_goto_returns_immediately`). |
| Script execution value-stack ceiling | `C4AulExec.cpp:62-63,182-221,330-363,866-904,1216-1305` | **Complete** | One dynamic 1,024-slot budget is shared by active parameter/local frames, suspended aggregate and expression operands, call arguments, foreach state, cross-host dispatch and nested DirectExec/Eval. Raw string indexes retain C++'s embedded AB_MAPA operand while parenthesized strings consume a dynamic slot. Exact normal-function, DirectExec, Eval, map, nested-expression, surplus-argument, unwind and recursion boundaries report `internal error: value stack overflow!` (`vm_enforces_cpp_1024_value_stack_limit_for_large_array_literal`; `direct_exec_uses_all_1024_value_stack_slots`; `eval_reentry_shares_the_suspended_callers_value_stack`; `only_a_direct_string_index_uses_cpp_embedded_operand_optimization`; `map_and_nested_expression_operands_share_the_value_stack`; `recursion_beyond_the_value_stack_limit_errors_cleanly`). |
| Array concatenation size cap | `C4AulExec.cpp:594-636`; `C4ValueList.cpp:50-74` | **Complete** | Array `..` and `..=` preflight the checked combined length before extending either value or identity storage. A result of exactly 1,000,000 elements remains valid; a larger result reports `out of memory` and leaves the assignment target unchanged (`array_concat_rejects_result_over_max_size`). |
| Fail-safe runtime call-stack diagnostics | `C4AulExec.cpp:65-108,1318-1339` | **Complete** | Runtime errors snapshot the singleton active script-context stack before RAII unwind and every fail-safe engine logger emits native-style `by:` records from innermost to outermost with arguments, object context, original script name and zero-based declaration line. Recovery still yields nil, later calls retain no stale frames, and re-entrant VMs share the same stack (`failsafe_runtime_error_logs_cpp_call_stack_frames`). |
| Native `Explode` fallback | `C4Script.cpp:238-243,6601`; `C4Object.cpp:1673-1680`; `C4Effect.cpp:877-929` | **Complete** | Bare and inherited calls reach the four-slot engine native while ordinary script definitions retain precedence. The fallback snapshots container/cause before `AssignRemoval`, uses the removed object's live post-callback position, layer and creator controller, walks `ContainBlast`, applies absolute object/landscape blast effects, and preserves custom/default particle plus partial-construction effect fallbacks. It also retains C++'s raw captured-container blast, zero-construction removal lifecycle, full-to-partial initial `DoCon` idle transition and synchronous FLAM retry behavior (`native_explode_is_registered_and_matches_object_default_effect_arguments`; `native_explode_builds_the_effect_id_when_no_particle_is_loaded`; `native_explode_initial_partial_effect_idles_after_full_construction`; `native_explode_script_overload_shadows_and_inherited_reaches_the_host`; `native_explode_contain_blast_suppresses_visuals_and_blasts_contents`; `native_explode_blasts_a_captured_container_after_an_effect_removes_it`; `native_explode_zero_level_runs_effect_destruction_without_activate`; `native_explode_retries_incineration_after_flam_removes_itself`). |
| `GetMaterialVal` compiled-core reflection | `C4Script.cpp:3936-4068,4131-4147,4283-4300`; `C4Material.cpp:170-226` | **Complete** | `GetMaterialVal` reflects exact compile names from the compiled `C4MaterialCore`, including post-load/default values and native int/bool/string/C4ID types. `Color` and `ColorX` share the final overridden color array, fixed arrays expose one compiled primitive per nonnegative index through their last nonzero element, and negative/out-of-range indices or unknown names return nil (`get_material_val_reflects_compiled_material_core`). |
| Normal style | `C4Menu.cpp:359-365,642-753`; `C4Script.cpp:1420-1450,1465-1727` | **Complete** | Grid geometry, selection, scrolling, clipping, wheel/title drag, images, decoration, progressive text, failure and callbacks are covered by `engine_script_normal_menu_uses_cpp_grid_geometry_and_selection_color`, `normal_menu_render_rejects_an_unresolved_non_textspec_item_picture` and `engine_script_menu_pointer_selects_enters_and_closes_like_cpp`. |
| Context style | `C4Menu.cpp:359-365,642-753`; `C4Script.cpp:1465-1727` | **Partial** | Layout, command strip, anchor, scrolling, wheel/title drag, targeting, image recipes, TextSpec and object/scenario callbacks are covered. Remaining: ObjectRank snapshots use fixed 35px symbols instead of the Context menu's live item height. |
| Info style | `C4MN_Style_Info` | **Complete** | Classic geometry, retained multi-row scrolling, pictures, markup-aware wrapping, complete TextSpec grammar, pointer targeting, no highlight/tooltip and close-only footer render in production; unresolved pictures fail typed. All shipped paths create one row; active-scenario GUI sheet/font overrides reach the shared menu resources through the rebound process-global set. Italic is a cumulative shear, not a flag: each tag applies `mat[1] -= 0.3` (`StdMarkup.cpp:24-28`), so a nested tag leans exactly twice as far. That is pinned at scale 1, where the projection correction is the identity and a wrong constant or a dropped scale term has nowhere to hide, and at an anisotropic fractional scale; glyph-only, image-only and mixed text-image rows all shear together and by the same amount, and measurement is unaffected (`native_italics_shear_cumulatively_at_scale_one`, `fractional_native_italics_shear_glyphs_and_images_without_changing_metrics`). |
| Dialog style | `C4Menu.cpp:363-424,642-753`; `C4Script.cpp:1420-1450,1465-1727` | **Complete** | Variable-height layout, portrait column, selection, decoration, TextSpec, images, callbacks and crew portraits render without fallback. Covered by `dialog_layout_matches_cpp_portrait_and_variable_row_geometry`, `real_last_will_assets_render_a_decorated_progressive_dialog` and `get_portrait_distinguishes_permanent_and_requires_info`. |
| EqualItemHeight flag | `C4MN_Style_EqualItemHeight` | **Complete** | The raw style bit is preserved independently of base style and Dialog symbol rows use the C++ equalization/restacking rule. |
| Rank/Indexed/ObjRank/Object/TextSpec/Color symbols | `C4Script.cpp:1598-1727`; `C4Game.cpp` text-image resolver | **Partial** | Definition, indexed/color, rank, object snapshots and TextSpec recipes are implemented, including current/permanent portraits and shipped corpus. Active-scenario GUI sheets and fonts now resolve through the rebound process-global set and the active HUD bundle. A Context `ObjectRank` row is sized by the menu's resolved `GetItemHeight()` rather than the add-time `GetSymbolSize()`, keeping the native `H * 2` by `H` facet with the object left and the rank right; every other style still uses the square add-time size, and the add-time object/rank visual state is untouched (`context_object_rank_snapshot_uses_runtime_item_height`). |
| TextSpec grammar/corpus | `C4Game::DrawTextSpecImage`, `C4DefList::GetFontImage` | **Complete** | Parser tables cover C scanf prefixes, raw portrait IDs/colors, case rules and icon prefixes; recursive shipped team census covers 39 `IconSpec` values in 19 files. Keep the Goldrush Context render/hit-test regression current. |
| Footer extras (`C4MN_Extra_*`) | `C4Menu.cpp:823-930` | **Complete** | All variants and blank Info/unknown strips are covered by `engine_buy_menu_draws_cpp_title_symbol_and_value_footer`, `menu_value_footer_runs_shipped_tendon_calc_def_value`, `engine_magic_menu_draws_cpp_spell_cost_and_available_energy_footer` and `engine_components_menu_draws_cached_requirements_right_to_left`. |
| Explicit size/permanence/alignment | `C4Menu.h:180,203`; `C4Menu.cpp:298-319,516-523,635-640,713-721,755-780,947-970`; `C4Viewport.cpp:780-803,983-1013,1482-1494`; `C4Script.cpp:1420-1450,4483-4491` | **Complete** | Alignment, Context anchors, clamping, drag, resize reset, permanence and close-before-command are covered. `SetMenuSize` stores both axes; the column count has always driven the grid, and the row count now does too, with the native lifetime: `SetSize` assigns `Lines` and reruns only `InitSize`, which applies no viewport clamp, while `InitLocation` recomputes `Lines` from the item count whenever it runs. A row count set before the menu's first draw is therefore discarded by that draw, exactly as in C++, and one issued while the menu is on screen constrains the client height, the scrollbar, `VisibleCount` and item hit geometry (`set_menu_size_rows_control_visible_grid_and_scrollbar`). Refill growth clears the retained location for every style; shrink does so for Context menus, and viewport `ResetLocation` clears it for the next draw. The pre-first-draw, stable-live, growth-refill, context-shrink and viewport-reset lifetimes are covered by `script_menu_pre_first_draw_discards_explicit_rows`, `script_menu_explicit_rows_survive_stable_live_draws`, `script_menu_growth_refill_recomputes_explicit_rows_and_visible_grid`, `context_menu_shrink_refill_recomputes_explicit_rows_and_scrollbar` and `script_menu_viewport_resize_recomputes_explicit_rows_and_hit_regions`. |
| Primary/secondary commands | `C4Menu.cpp:433-523`; `C4Script.cpp:1513-1597` | **Complete** | Enter/left click dispatches `Command`; right click dispatches `Command2`. Object and scenario paths honor nonpermanent close-before-command semantics (`cursor_script_menu_consumes_controls_before_gameplay_like_cpp`; `scenario_script_menu_routes_enter_close_and_selection_callbacks_like_cpp`; `engine_script_menu_pointer_selects_enters_and_closes_like_cpp`). The port's own `planet/System.c4g/MenuRangeRow.c` spends this pair on the two directions of a ClonkMars Menu2 range, which is a deliberate content divergence rather than an engine one. **One engine divergence sits beside it:** in a one-column menu, where `COM_MenuLeft`/`Right` compute the same deltas as `Up`/`Down` (`C4Menu.cpp:433-457`), the port first offers the com to a script menu's command object as `~OnMenuStep(iDelta, pMenuObject)` and only moves the selection when the script declines. Inert for content that does not implement it (`a_one_column_script_menu_offers_left_and_right_as_a_step_before_moving`; `an_unclaimed_step_still_moves_the_selection_exactly_as_it_did`; `a_multi_column_menu_never_offers_a_step`). |
| Menu decoration | `SetMenuDecoration` | **Partial** | `SetByDef` snapshots callback-derived color/borders and all eight `FrameDeco*` facets immediately; every classic style applies packed alpha, tiled/truncated edges, protruding corners, clipped out-of-bounds source facets, and captured margins. Background-only decoration is valid; unsafe geometry and unresolved drawable facets fail fast. Real LastWill assets and the natural Western Goldrush dialogue path are covered. Remaining: definition hot-reload refresh/clear behavior when Rust gains that API. |
| Progressive text | `SetMenuTextProgress` | **Complete** | Per-row byte offsets, portrait exclusion, markup-skipping shared budgets, selectable-row reveal, late-added rows, one-byte menu ticks, explicit show-text, local-only first-input conversion, and byte-prefix Dialog rendering are modeled and tested. |
| Selection/close/command callbacks | `C4Menu.cpp:433-523`; `C4ObjectMenu.cpp:57-104,479-541` | **Complete** | Object/scenario callbacks, close ordering, permanence and cleared command-object ownership are covered by `cursor_script_menu_consumes_controls_before_gameplay_like_cpp`, `scenario_script_menu_routes_enter_close_and_selection_callbacks_like_cpp` and `cleared_object_menu_callback_does_not_become_scenario_callback`. |
| Script-menu resource/font fallback boundary | `C4Menu::Draw`, `C4GUI::Resource`; `ingame_menu.rs` | **Complete** | Render preflight uniformly fails typed for every unresolved requested picture across Normal, Context, Info and Dialog, including non-TextSpec recipes. Pointer targeting uses the same global-resource and inline-image preflight before hover or clicks can reach the world. |

## Other game-visible dialogs and overlays

| Screen | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Game over/evaluation | `C4GameOverDlg.cpp:115-258`; `C4RoundResults.cpp:52-73,338-344`; `C4PlayerInfoListBox.cpp:260-450` | **Partial** | Lifecycle/resource gates, frozen goals/players, Player fallback, league/network result refresh, and exact control-host-or-cinematic-Film Restart/Next construction plus base/expanded dialog geometry are covered. BigIcons are now copied on the native event: `C4RoundResults::EvaluatePlayer` freezes `C4Player::BigIcon` while the player is still alive, so an eliminated, retired or disconnected player keeps its final icon after its player record, file and network resource are gone, and dialog construction consumes that snapshot (`game_over_uses_elimination_time_big_icon_after_player_resource_departure`). The copy stays independent of the viewport-only ShowPortraits switch. Every evaluation row field is now projected and drawn: `UpdateScoreLabel`'s five league/settlement text variants in branch order, including the red admin-discrepancy form and the league-over-settlement precedence; the league rank icon in its own square right-hand column, whose left edge is what the score and playing-time labels are measured against; the joined-savegame `fctCrewClr` overlay with its `iSizeMax/2` quad, modulation-1 shadow at `X=2` and coloured pass at `X=0`; one `TeamListItem` header per filtered list in the two-team layout, coloured by `C4Team::HasWon()`; and the `C4GUI::ScrollBar` for custom evaluation text that overflows a third of the dialog (`evaluation_score_label_matches_the_native_league_and_settlement_variants`, `league_rank_icons_claim_their_own_column_and_move_the_score_edge`, `joined_savegame_crew_overlay_uses_the_native_half_size_geometry`, `two_team_evaluation_lists_lead_with_one_native_team_header`, `overflowing_custom_evaluation_text_reserves_and_travels_the_native_scrollbar`, `league_evaluation_score_draws_the_extended_facet_not_settlement`). Goal pictures follow `C4GoalDisplay::GoalPicture`: the goal is resolved as a live object through `Game.Objects.FindInternal` and drawn through that object's graphics when one exists, falling back to the definition picture otherwise, with the unfulfilled grayscale applied afterwards (`game_over_goal_picture_includes_live_goal_object_overlays`). League score rows now resolve the native `GUIIcons2.png` phase 8 facet independently from the settlement `Score.png` facet, and the evaluation resource gate rejects a sheet that cannot provide it. |
| Game-over goal-display child | `C4GameOverDlg.cpp:32-108` | **Partial** | Definition pictures, fulfilled stars, grayscale, responsive multi-row ComponentAligner geometry and localized tooltips are covered (`classic_evaluation_content_layout_matches_cpp_at_audited_resolutions`). Remaining: C++ passes the live goal object to definition drawing; Rust always renders a definition-only picture. |
| Game-over chat input | `C4GameOverDlg.cpp:317-321`; `C4MessageInput` | **Complete** | Bare Return/F2 opens All chat and Shift+Return opens Allies through the ordinary running-chat controller, including its edit, history, completion, paste/clipboard, submit/cancel and network lifecycle. Exact Alt/Alt+Shift first dispatches the localized visible-button mnemonic at control priority; an unmatched Alt+Return then falls through to Say chat. With signed `[Controls] GamepadGuiControl` enabled, raw gamepad-0 `AnyLow` opens All chat whenever no actionable close/button owns focus, so East remains chat despite its later Cancel alias; disabled GUI control and every nonzero physical pad ID remain inert for the exclusive evaluation GUI stack. Logo remains outside the C++ modifier mask and keypad Enter is not silently aliased on the macOS SDL route. Shared command-language descendants are inventoried below. |
| Game-over focus and keyboard/gamepad lifecycle | `C4GUI::Dialog`; `C4GameOverDlg.cpp:115-382` | **Partial** | No-focus opening, focus order, stacked-dialog suppression, press/release latches, mnemonics, pointer behavior, quit latch and gamepad ownership are covered. `SetNextMission` now records argument omission rather than baking English in: the host resolves the two omitted slots through the active `IDS_BTN_NEXTSCENARIO`/`IDS_DESC_NEXTSCENARIO` at the command boundary as `FnSetNextMission` does, while an explicit empty string stays empty - `C4String::Data.getData()` is non-null for it - and a custom string is verbatim (`set_next_mission_omitted_labels_use_active_runtime_resources`). Presentation text stays out of the serialized `EngineState`. |
| In-game chat/script query input | `C4MessageInput` | **Partial** | `C4ChatInputDialog` ports ordinary running chat (All/Allies/Say, history, nick completion, multiline paste, Ctrl/gamepad forwarding, and team/private/me/sound/alert/say classification) and Tick35 fObjInput script queries, including exact prompts, submission-time uppercase handling, synchronized answers, and empty-answer cancellation. Its case-sensitive `ProcessCommand` router now dispatches help/clear, host/debug/league-gated kick and client-state changes, central/decentral/async control modes, set comment/password/faircrew/maxplayer, script/nodebug, mute/unmute, `/msgboard`'s exact 0..20 mode/line-count and persistence behavior, `/fast`'s league-gated 1..500 frame skip, `/slow`'s normal-pacing reset, the live `/chart` dialog, and exact registered `AddMsgBoardCmd` commands; unknown commands retain the native error path. `/script` reads exact native `[Developer] ConsoleScriptStrictness` tokens, including numeric-prefix parsing, C integer narrowing and both uint8/0..3 clamps; missing, malformed and `MaxStrict` values resolve to `Strict3`. Outside the independent league gate, replay `CID_Script` and `EMMO_SCRIPT` execution now uses native `General.AllowScriptingInReplays`: the replay flag does not gate host-authored packets, while non-host packets—including replay-time `/script` injection—require it, and malformed/missing values default off. `/netgetscen` is ported with its exact gates - network enabled, not the host, and outside the lobby, which has its own Resources tab - copying the transferred scenario resource to `Config.AtExePath(GetFilename(Game.ScenarioFilename))` under the scenario's own base name rather than the resource's transfer name, overwriting an existing target the way `CreateItem`'s `EraseItem` does, and logging `IDS_MSG_CMD_NETGETSCEN_SAVED` with the destination; every missing link and the copy failure itself fall through to the native unknown-command error (`running_chat_netgetscen_saves_client_scenario_resource_like_cpp`). |
| Scoreboard dialog | `C4Scoreboard`, `C4ScoreboardDlg`; `StdFont.cpp:817-929` | **Partial** | Dialog lifecycle, placement, title/close interaction, stacking, matrix layout, markup, custom images, logical italic shear, KeyConfig and fail-closed resources are covered. Remaining: native-scale italic capture and parent-atlas/facet edge sampling, including C++ packing order and transparent-white padding. Console native-child presentation is M10. |
| Upper board modes and right-side indicators | `C4UpperBoard::Init`, `Draw`, `Height`; `clonk-frontend/src/hud.rs` | **Complete** | Hide/Full/Small/Mini reserve and draw their native geometry; Small scales its background tiles and logo, Mini carves the right text strip from the message-board width, and game time, optional wall clock and sampled FPS retain the classic right-slot order. Missing `UpperBoard.png` is rejected by the shared typed HUD-resource boundary before output pixels; no solid-brown substitute remains. |
| Player and cursor HUD subtree | `C4Viewport.cpp:884-970,1281-1325`; `clonk-frontend/src/hud.rs` | **Partial** | Portrait/mask/per-bar gates, transient ViewWealth/ViewValue, crew/inventory, command regions, help/menu/chat buttons and startup facets are implemented. Energy, magic and breath retain their raw levels plus live effective physical ranges through presentation; all three use the C++ `BoundBy`/multiply/divide order, including magic's separate pre-render level/range `/1000` conversion (`energy_bar_uses_effective_physical_range_and_cpp_integer_threshold`; `live_temporary_physicals_feed_all_integer_hud_bar_ranges`; `focused_crew_draws_partial_breath_in_the_next_cpp_bar_slot`). Missing: the outer `ViewEnergy || ShowPlayerHUDAlways` transient bar-subtree gate, i.e. `C4Object`'s ViewEnergy bar timing. Additional coverage includes `player_overlay_projects_transient_hud_flags`, `fixed_item_visibility_combines_global_and_script_requests`, `focused_crew_hud_masks_hide_inventory_and_compact_bars` and `overlay_state_feeds_the_hud_render`. |
| Definition-based newgfx particles | `C4Particles.cpp:30-56,118-191,578-598,727-808`; `C4Object.cpp:2238-2283`; `C4Viewport.cpp:1066-1079` | **Complete** | Production definition traversal parses exact `Particle.txt`/`Graphics.png` resources in native order, validates the built-in procedures and preserves successful name overloads. Smoke and Std rendering now carries native facets, phase/aspect, parallax, attachment, velocity rotation, packed modulation, additive blending and Y-offset clipping through object-back, object-front and global layers. The engine's own burning-object emitter is ported too: `FnFxFireTimer` spawns `2 * iCount` particles per execution, the first quarter `Fire` and the remaining three quarters additive `Fire2`, dealt to the object's back list three times out of four, off the unsynchronized `SafeRandom` stream. `FireParticles=false` reproduces `SetDefParticles` leaving pFire1/pFire2 null - it silences that emitter without unregistering or hiding script-created Fire/Fire2 particles - pinned by `switching_fire_particles_off_still_lets_script_create_them` - and the simple object Fire facet remains unconditional. A burning object also trails `SmokeRate` smoke from ExecFire on both feeder paths. The separate presentation-detail governor may suppress the flame *draws* to reclaim their unbatched draw calls, which is a renderer decision and never reaches the engine (`burning_object_smokes_on_the_defs_smoke_rate_cadence`; `an_inherited_fx_fire_timer_smokes_like_the_native_path`; `the_fire_detail_rung_skips_only_a_burning_objects_own_flames`; `burning_object_emits_its_fire_particles_on_every_fourth_execution`; `object_fire_consumes_safe_random_draws_in_cpp_order`; `stock_particle_groups_render_std_smoke_and_layer_order`; `load_install_definitions_discovers_mixed_case_objects_group`; `definition_load_skip_ladder_matches_cpp`). |
| Runtime gameplay sprite filtering | `CStdDDraw::Blit`; `CStdGL::PerformBlt`; `C4Surface::CreateTextures` | **Complete** | Object faces, TopFace construction signs, fire facets and graphics overlays now share the native exact-blit/`PointFiltering` selector. Non-exact scale-one draws default to linear, explicit point filtering keeps them nearest, and every non-100% application scale forces linear after the native half-texel source correction. Filtering clamps within each physical texture tile and retains float base/owner samples through modulation, FoW, gamma and framebuffer composition; resize and Advanced Options reloads preserve the live policy. Covered by `runtime_sprite_filtering_matches_stdgl_exact_and_point_modes`, `runtime_sprite_linear_filtering_uses_native_tile_edges_and_float_pipeline` and `runtime_point_filtering_reloads_after_advanced_config_save`. |
| Advanced renderer raster switches | `CStdDDraw::Blit`/`SetBlitMode`; `CStdGL::RestoreDeviceObjects`/`PerformBlt`/`DrawQuadDw` | **Complete** | One immutable device snapshot now carries `NoAlphaAdd`, `NoBoxFades`, `TexIndent`, `BlitOffset`, `AllowedBlitModes`, `Shader`, `UseShaderGamma` and `DisableGamma` through startup, Advanced-save reloads, renderer rebuilds and draw submission. Requested modes are masked after Parent resolution, including HUD inventory and owned-menu recipes; ordinary sprites, graphical PXS, landscape, generic/custom GUI and ClonkFont glyph/image blits use their native offset/texture-indent rules in both CPU fallbacks and retained GPU commands; box and ClrModMap fades select native normalization/flat provoking vertices; and `NoAlphaAdd` changes only fixed-function-equivalent ordinary alpha modulation. Shader gamma remains a pre-blend fragment lookup, monitor gamma runs once after complete raster/native composition and feeds both retained readback and presentation, and disabled gamma uses a continuous identity bypass without mutating the process-global monitor (`advanced_renderer_config_changes_frame_like_stdgl`; `configured_glyph_blit_applies_blit_offset_and_tex_indent`; `advanced_renderer_config_loads_native_device_snapshot`; `graphical_pxs_honors_advanced_renderer_snapshot`; `retained_gpu_gamma_mode_matches_native_device_switch_matrix`). |
| Viewport world-object motion for zero-vertex shapes | `C4Object::DoMovement`, `C4Shape::ContactCheck`, `C4Shape::Attach` | **Complete** | Vertexless objects now use the ordinary per-pixel translation, attachment-loss and per-degree rotation paths. Empty contact loops pass through terrain, clear the contact latch and preserve velocity/material state, while configured side/top/bottom/layer bounds still clamp and dispatch their native directional callbacks (`zero_vertex_objects_use_vertex_contact_and_border_bound_semantics`, `zero_vertex_attached_movement_runs_attachment_loss_action`, `zero_vertex_rotation_uses_empty_contact_checks`). |
| Live nested object mass | `C4Object::UpdateMass`, `Enter`; `C4ObjectList::MassCount` | **Complete** | Runtime force, splash and collision consumers plus section `Objects.txt` serialization include every descendant in arbitrarily deep acyclic contents trees. `OwnMass` and construction scale only each object's base term, `NoComponentMass` suppresses that node's cargo, and `Enter` rejects ancestor cycles before mutating links; malformed loaded graphs retain a path-local recursion guard (`effective_object_mass_has_no_nesting_depth_cutoff`; `section_object_mass_has_no_nesting_depth_cutoff`). |
| Signed DefCore gameplay fields | `C4DefCore::CompileFunc`, `C4DefCore::Load`; `C4Object::SetOCF`, `ExecLife`, `TargetBounds`; `ConstructionCheck` | **Complete** | `Value`, `CollectionLimit`, `ContactIncinerate`, `Grab`, `Rotate`, `BorderBound`, `UprightAttach`, `Basement` and `ConSizeOff` retain their raw signed compiler values through both definition-loading paths; only negative `Mass` is clamped to zero after load. Native signed/nonzero behavior now reaches contents capacity, OCF and rotation, incendiary material, border masks, upright attachment and construction-site height (`negative_defcore_values_preserve_native_runtime_semantics`). |
| Loaded object runtime fields and player-view links | `C4Object::CompileFunc`; `C4GameObjects::AssignInfo`, `AssignPlrViewRange` | **Complete** | `Objects.txt` restores the remaining signed simulation caches, base/mass/damage/breath/fire and crew flags in native default order. Delayed `Info` binding selects the saved live roster entry and physical/name state after players exist; the following full active-list pass reconstructs owned and ownerless FoW-view memberships. A bare old-style `OnFire` receives the callback-suppressed dead compatibility node and no fire tick (`legacy_objects_restore_remaining_runtime_fields_and_info_links`). |
| Loaded inactive object solid-mask lifecycle | `C4GameObjects::Load`; `C4Object::StatusActivate`, `StatusDeactivate` | **Complete** | `Objects.txt` rows with `Status=2` skip initial mask staging and baking, activation constructs and puts the mask through `UpdateFace(true)`, and later runtime deactivation retains that existing mask (`legacy_loaded_inactive_object_does_not_put_solid_mask_until_activated`). |
| Solid-mask placement for definitions without `Shape` | `C4DefCore::Default`, `C4Shape::CompileFunc`; `C4SolidMask::Put` | **Complete** | A valid bitmap-bounded `SolidMask` remains active when Width, Height and Offset are all omitted: authoritative and callback-time placement project the native zero shape offset without synthesizing a resource shape. Explicit shape offsets, rotation and bitmap validation remain unchanged (`solid_mask_without_shape_uses_zero_shape_offset_like_cpp`). |
| Solid-mask landscape persistence bracket | `C4GameObjects::RemoveSolidMasks`, `PutSolidMasks`; `C4SolidMask::Remove` | **Complete** | Save/state/section landscape captures remove active-list masks in native master order while repairing every linked overlap newest-instance-first. Runtime-inactive mask survivors—including partial masks—remain baked; loaded-inactive maskless rows do not. Non-overlap and all-active captures retain clean background bytes (`section_save_preserves_overlapping_inactive_solid_mask_like_cpp`). |
| Loaded `Objects.txt` command stacks | `C4Object::CompileFunc`; `C4Command::CompileFunc`, `DenumeratePointers` | **Complete** | Contiguous `Command1..N` nodes restore in executable head-to-tail order with tagged `Tx`, active/inactive/missing object denumeration, signed scheduler flags and counters, retries, permit, arbitrary base-mode words and comma-bearing text. Section loads also resolve against retained objects (`legacy_objects_restore_ordered_command_stack`). Runtime command/action/container fields keep raw pointer identity until `ClearPointers`, while `At`, `GetEntranceArea` and object-list operations retain their own nonzero-Status gates; cached `ControlTransfer` and `Contained*` direct calls do not inherit `C4Object::Call`'s receiver gate. Restored callback events rebind their omitted runtime identity before scripts run, so same-kind replacement commands cannot inherit a prior native command's result; callback-detached `GetTryEnter` continuations freeze their exact Target state, distinguishing clear-before-detach from detach-before-clear ordering. |
| Film/replay overlay suppression | `C4Viewport::DrawOverlay` | **Complete** | Film plus Replay suppresses HUD, menus, command/mouse overlays and cursor marks while retaining game messages; ordinary film and replay states keep their independent C++ behavior. |
| Eliminated/surrendered viewport message | `C4Viewport::DrawMenu:970-975` | **Complete** | Every retained eliminated viewport draws the active-language `IDS_PLR_ELIMINATED` or `IDS_PLR_SURRENDERED` FontRegular text at its exact centered, two-thirds-height anchor in `0xfaFF0000`. The draw-time early return suppresses generic, cursor-script and player/fullscreen menu resource checks, progression, pixels and pointer regions for that viewport while leaving the surrounding player HUD, game messages and control overlays intact. Ordered-native capture covers both localized variants, exact color/alignment/clip geometry and zero menu contribution. |
| Object-independent `NO_OWNER` observer viewport | `C4Viewport.cpp:1155-1278`; `C4FullScreen::ViewportCheck` | **Complete** | Zero-object/no-local play uses object-independent centered/clamped camera state, border clipping, ViewOffs projection, edge/arrow free-scroll and audio mixing. Covered by `no_owner_viewport_stays_centered_without_free_scroll_input`, `upper_board_relayout_clamps_no_owner_camera_at_large_world_edge`, `observer_scroll_queued_before_projection_moves_the_first_rendered_camera` and `script_view_offset_applies_after_camera_smoothing`. |
| Sound sample load graph | `C4SoundSystem::{C4SoundSystem,LoadEffects}`; `C4Def::{Load,C4DefList::Load}` | **Complete** | Startup opens only the configured `Sound.c4g`; scenario activation retains the exact native LoadEffects event stream independently of surviving definitions, including direct scenario/folder-root sounds and DefCore-less, particle and invalid-ID `.c4d` groups. Traversal is preorder through direct `*.c4d` children only, each event scans only direct WAV/OGG/MP3 entries, and reversed resolver precedence preserves the native later-load overload bank. Each candidate is read and eagerly decoded before it may replace the prior same-name sample; a failed candidate logs and preserves the old handle, catalog position and live instances, while only decoded samples are advertised for speech. SkipDefs, OLDGFX, pre-sound resource failures, ordinary nested directories and arbitrary `Sound*` siblings are excluded. Covered by `external_pure_c4d_sound_folder_without_defcore_is_playable`, `nested_non_definition_audio_is_not_admitted`, `global_sound_discovery_uses_only_native_sound_c4g`, `definition_sound_groups_follow_native_load_effects_gates_and_preorder`, `invalid_sound_override_keeps_previous_decoded_sample` and `undecodable_speech_sample_does_not_suppress_text_fallback`. |
| Message-family speech admission and text fallback | `FnMessage`, `FnPlayerMessage`, `FnPlrMessage`; `C4SoundSystem::NewInstance` | **Partial** | Speech suppresses segment-zero text only after the frontend creates the same logical sound instance as C++. Missing or undecodable samples still fall back synchronously; resolved-sample nearby duplicates, the 20-instance cap and initial mixer-channel failure now return their ordered fallback to the engine, while successful, muted and inaudible logical instances suppress it. Deferred outcomes retain the original message-operation position and lifetime, and definition-commanded calls preserve their null `cthr->Obj` speech anchor (`message_speech_falls_back_when_new_instance_is_rejected`; `message_family_speech_keeps_null_definition_caller_anchor`; `deferred_speech_fallback_expires_while_admission_is_pending`). Remaining: an advertised speech request with an invalid fallback format cannot reproduce C++'s synchronous script abort when the frontend later rejects the instance; that requires a synchronous audio host or resumable script continuation across the engine/frontend boundary. |
| Audio output initialization fallback | `C4AudioSystem::NewInstance`; `C4AudioSystemNone` | **Complete** | A concrete CPAL device/stream initialization error is logged before production installs a distinct inert None-compatible backend. Its sound/music files and channel objects are non-null placeholders; sound duration is zero, play-state queries are false and controls are no-ops. It performs no decode and starts no realtime/null worker. Explicit active and deferred null constructors remain separate test/headless facilities (`device_failure_uses_logged_inert_none_backend`). |
| Audio output negotiation | `C4AudioSystemSdl::C4AudioSystemSdl`; `Mix_OpenAudioDevice` | **Complete** | The default CPAL device starts from the native 44.1-kHz signed-16 stereo request, clamps unavailable rates to the nearest advertised boundary and retries each viable advertised PCM candidate before falling back. The game mixer remains stereo while mono hardware receives SDL's `(L+R)/2` conversion and 3–8-channel hardware receives the front L/R pair plus silent extra speakers. DSD and physical channel counts outside SDL2's 1–8 conversion range remain ineligible (`cpal_negotiation_prefers_cpp_request_and_converts_viable_fallbacks`). |
| SDL_mixer volume and pan controls | `C4AudioSystemSdl::{SetMusicVolume,SoundChannelSdl::SetVolumeAndPan}` | **Complete** | Music volume is rounded through SDL_mixer's 0–80 integer domain, sound volume on its 100-step scale (then SDL's 128 ceiling), and each panning side through the clamped 0–192 byte domain before those integer controls become mixer gains. Exact half steps use `std::lrint`'s default ties-to-even behavior (`fractional_volume_and_pan_match_sdl_integer_steps`). |
| SDL-compatible WAV decoding | `C4AudioSystemSdl::SoundFileSdl`; SDL2 `SDL_LoadWAV_RW` | **Complete** | Sound effects and streaming music share one checked RIFF decoder for conventional and extensible 8/16/24/32-bit PCM, 32-bit float, A-law, mu-law, Microsoft ADPCM and IMA ADPCM. Extensible subtype GUIDs, compact codec headers, SDL coefficient/nibble/index rules, stereo and multichannel IMA groups, exact SDL channel-count downmix matrices, per-block state reset and malformed block geometry are preserved; ADPCM music retains only one bounded decoded block before the existing stereo/rate converter. Covered by `decodes_cpp_supported_wav_encodings`, `malformed_or_unsupported_wav_encodings_remain_typed_errors`, `compressed_wav_music_streams_in_bounded_blocks_and_resamples` and `sdl_wav_multichannel_downmix_uses_count_layouts`. |
| Tracker music decoding | `C4MusicSystem::Play`; `C4AudioSystemSdl::MusicFileSdl` / SDL_mixer `MUS_MOD` | **Complete** | IT, MOD, S3M and XM bytes are detected after the native WAV/Ogg/MP3/MIDI precedence and decoded incrementally through maintained libxmp. Malformed recognized modules retain a typed tracker-decode error; executable/app-bundle/system discovery supports an explicit `LC_LIBXMP_LIBRARY` override. `decodes_and_plays_cpp_tracker_music_formats` executes synthetic non-silent fixtures for all four families and their control lifecycle; `malformed_tracker_data_returns_typed_decode_error` covers rejection. |
| Music streaming/playback | `C4MusicSystem::{Play,Stop,ClearPlayingSong}`; `C4AudioSystemSdl::{CreateMusicFile,PlayMusic}` | **Complete** | Loaded music retains its compressed source and a pull decoder instead of a complete stereo-f32 render. OGG, MP3, WAV, MIDI and tracker modules feed one fixed 4,096-frame mixer block with stateful streaming resampling; loop/restart, immediate replacement, live volume, fade completion and source lifetime match the single native music channel. Shared sound/music MP3 opening first attempts the original bytes, then retries decoder-validated Layer III headers only within the native 5,761-byte prefix while preserving the original failure if every candidate fails (`mp3_leading_junk_recovers_first_valid_layer3_frame`). MIDI keeps parser/event safety bounds but has no duration-based playback ceiling, synthesizes scheduled events incrementally and drains voice release plus the two-second tail. Covered by `long_music_streams_with_bounded_memory_and_no_prerender_limit`, `decodes_and_plays_cpp_tracker_music_formats` and the mixer loop/fade/gain regressions. |
| Legacy-byte music names | `C4MusicSystem::{LoadMoreMusic,LoadDir,FindSong,SetPlayList}` | **Complete** | `MoreMusic.txt` is split and ASCII-trimmed as raw bytes, with native C-string NUL termination at path opening. Catalog records retain exact group-entry basename/full-path bytes and exact-read packed payloads; named `Music`, playlist wildcards, default exclusions, extension probing and the script-visible `SetPlayList` count all use reversible C4 string bytes rather than lossy UTF-8. Covered by `more_music_and_playlist_preserve_non_utf8_track_name_bytes` and `set_playlist_count_preserves_legacy_music_name_bytes`. |
| Frontend music PreInit handoff | `C4Game::Clear`; `C4Application::PreInit`; `C4Startup::DoStartup` | **Complete** | Game teardown still requests the native 2s fade, but the next PreInit-equivalent reconstruction immediately hard-stops that generation, cancels stale asynchronous loads, reopens global `Music.c4g` and rereads the current `MoreMusic.txt` directives while discarding the prior scenario catalog, playlist and recent-song identity. A normal startup entry then requests exactly one non-looping `Frontend.*` catalog song without polling the old fade; Restart/Next Mission rebuilds without playing frontend music, and the separate frontend-to-scenario fade remains intact. Covered by `return_to_menu_recreates_music_before_teardown_fade_finishes_like_cpp` and `frontend_preinit_reloads_changed_music_and_more_music_catalog`. |
| Music replacement ordering | `C4MusicSystem::Play`; script `Music`/`SetPlayList` | **Complete** | Default, named and direct replacements enter one outcome-ordered start queue while decoding remains asynchronous. Each default request retains its command-time catalog and playlist but defers SafeRandom selection until the preceding start has succeeded or failed. An unresolved request leaves playback untouched; once a catalog record resolves, its processing hard-stops the current generation before reading, so read/decode/play failure leaves silence while only a successful identity becomes the next exclusion. Explicit stop/fade cancels deferred work and stale generations cannot play or overwrite the marker. Covered by `back_to_back_music_commands_exclude_the_prior_selected_track`, `resolved_unreadable_music_stops_current_playback_before_failure`, `deferred_unreadable_music_stops_predecessor_and_preserves_recent_marker`, `queued_default_selection_observes_a_failed_prior_start`, `queued_playlist_restart_uses_its_command_time_filter` and `stop_music_cancels_deferred_starts_and_rejects_the_stale_worker`. |
| Scenario music source isolation | `C4GroupSet::CheckGroupContents`; `C4MusicSystem::PlayScenarioMusic`, `LoadDir` | **Complete** | Each observed direct `Music.c4g` marks its scenario or registered `.c4f` parent as local before the child is opened. The global/old catalog is therefore replaced even when that child is malformed; child-open and child-catalog enumeration failures log and skip only that source while valid scenario-root, later parent, Extra and definition sources retain their established inter-source precedence. Covered by `malformed_scenario_music_child_clears_global_and_keeps_valid_root_tracks` and `local_music_source_enumeration_failure_is_isolated`. |
| Positional and object-bound sound spatialization | `C4SoundSystem::Instance::Execute`, `DetachObj`; `C4Object::GetAudibility`; `C4GraphicsSystem::GetAudibility` | **Complete** | Ordinary attached sounds and `PlaySoundAt` share the native active-viewport mix: `ViewCursor` -> `ViewTarget` -> `Cursor` supplies each player listener, volume takes the maximum, and live viewport centers sum pan without averaging. Draw-time caching follows the rendered call order: non-parallax lines submit both absolute live endpoints and the last replaces volume/pan, while parallax objects and lines use each exact content/full or nested-overlay facet, replace volume per call and accumulate immediately clamped pan. Successful presentations replace the cache and remix attached channels; skipped/failed draws retain it, object movement falls back to the current origin, and detached one-shots always freeze that origin mix. Every nonzero signed custom falloff uses the native integer transform. Channel updates and post-render release/restore follow sample-list then instance-insertion order, including deterministic capacity failures. Covered by `object_bound_mix_uses_only_active_viewport_listener_and_pan`, `line_object_sound_uses_rendered_endpoints`, `parallax_sound_uses_rendered_target_position`, `rendered_object_audibility_cache_retains_until_render_and_falls_back_after_motion`, `post_render_attached_mix_releases_then_restores_channel_capacity`, `sound_preserves_negative_custom_falloff`, `channel_restore_at_capacity_follows_sample_then_instance_order` and the positional/custom-falloff regressions. |
| Dynamic sound levels and maximum wind | `SoundLevel`; `C4SoundSystem::Instance::Execute`; `C4AudioSystemSdl::SoundChannelSdl::SetVolumeAndPan`; `C4Weather::Execute` | **Complete** | Every positive signed `SoundLevel` survives script dispatch, ordered audio transport and live/fallback instance updates without the former percentage clamp. Level 100 retains SDL's 100/128 sound headroom, higher levels rise to the backend's 128/128 volume ceiling, and maximum absolute wind emits the native level 140 before that saturation (`sound_level_preserves_positive_values_above_100`; `sound_level_above_100_reaches_app_instance_unchanged`; `sound_level_above_100_reaches_sdl_max_gain`; `maximum_wind_preserves_level_140_gain`). |
| F1 two-column game-help overlay | `C4GraphicsSystem.cpp:666-725`; `C4Game.cpp:3378` | **Partial** | Toggle priority, two-column layout, language parsing, FontRegular rendering with cumulative italic shear and live definition images, draw order, KeyConfig ownership and fail-closed resources are covered. Displayed key labels now come from the live registration like `GetKeyboardInputName`: a `KeyConfig` override changes the shown chord as well as the dispatch, modifiers precede the key in `C4KeyCodeEx::ToString` order, the shared `format_key_label` supplies the backend-specific names, and a gamepad-only override renders the empty name an unresolvable code yields. Both columns are built lazily from the same process-global language table the rest of the UI reads, so a language or key change simply drops the memo and keeps the native draw order (`runtime_f1_help_displays_live_remapped_key_names`). |
| Pause/hold overlay | `C4Game::TogglePause`, `Pause`, `Unpause`; `C4GraphicsSystem::DrawHoldMessages` | **Partial** | Bare or Logo+Pause is a nonfatal runtime-global key before ordinary running dialogs and menus; releases are consumed, repeated downs toggle again, and the shown game-over dialog consumes every edge without mutation. A modeled per-game `FullscreenPauseToggle` KeyConfig list replaces the default chord, while an unavailable configuration safely suppresses the unknown physical binding instead of exiting. Offline Pause assigns the integer `HaltCount` counterpart to 1/0, while the abort dialog owns a balanced increment/decrement on that same stack; any nonzero count returns from `update()` before control/simulation ticks while event polling, input and rendering stay live. Script `PauseGame` halt/toggle requests drain through the same controller at their direct/control/tick execution boundaries. Unevaluated league hosts and clients submit `VT_Pause` with data 1/0 through the shared vote gate; a non-league/evaluated host requests PAUSE at `getNextControlTick` or GO at the current control tick through the synchronized status barrier; non-host clients and inconsistent runtime roles are safe consumed no-ops. Once local/network control is actually halted, the frozen frame draws literal FontRegular `Pause` at the exact centered `ResY/2 - 2*line-height` layer between Help and Flash. A player control outranks a pause chord rebound onto its key: the pause key registers with no explicit priority and so takes `C4CustomKey`'s default `PRIO_Base` (1) against `PRIO_PlrControl`'s 7 (`C4Game.cpp:3429`, `C4KeyboardInput.h:344-354,362`). Only a rebound chord can collide, since the default Pause key is not a player control, which is why the wrong order was invisible in the shipped configuration (`a_rebound_pause_chord_loses_to_the_player_control_on_the_same_key`). Remaining: other native stacked `HaltCount` owners and their label timing (notably evaluation), replay/record/editor interactions. The console Play/Halt buttons are strictly complementary and derived from the live halt count rather than latched from the last click, so a pause from any other route moves them too (`C4Console.cpp:709-730,1711,1720`; `console_play_and_halt_track_the_live_offline_halt_count`). |
| Timed flash-message overlay | `C4GraphicsSystem::FlashMessage`, `DrawFlashMessage` | **Complete** | Replacement/clear storage, legacy byte lifetime, draw countdown, placement, resize, cumulative italic shear, live FontRegular definition images, markup/gamma/Unicode, layer order and localized music/speed/debug producers are implemented. Script `SetPreSend` now matches the local client wildcard, updates both adaptive presend and async-wait pacing, and emits exact `TargetFPS:` / changed-`PreSend:` flashes in native order. Covered by `runtime_flash_storage_uses_classic_bytes_and_snapshots_placement`, `runtime_help_and_flash_resolve_fontregular_images`, `runtime_flash_draws_above_f1_help_and_below_recursive_context_gui`, `network_set_pre_send_applies_matching_local_target_fps_and_flash_without_fatal_boundary` and `adaptive_presend_uses_live_target_and_emits_the_exact_classic_flash`. |
| In-game mouse-control/cursor overlay | `C4MouseControl::Draw`; `C4Viewport::DrawMouseButtons` | **Complete** | All defined `C4MC_Cursor_*` atlas phases use the native cells and hotspots, including the legacy 13px Region/Select/Dig rules plus Help, action, jump, throw, vehicle, construction and eight edge-scroll cursors; production bootstrap rejects a partial eight-sheet cursor set before hiding the OS pointer. World cursors retain the physical-viewport clip and native primary -> throw Point -> Shift Add -> caption order; carryable throws preserve the first successful probe's left/right direction and exact landing point, passive ownerless viewports draw Region, and Add obeys the Region/Select/Jump/passive exclusions. Independent C4GUI/world ownership reproduces the reset interval where both cursors are active, shown menus retain GUI ownership even when film/replay suppresses their pixels, F1 help does not masquerade as a dialog, and the last real dialog closes with the native retained-point handoff only when world ownership was absent; the GUI Region/Help cursor remains above dialogs and below tooltips. Selection marks/frame, construction-drag preview, FoW-gated help targeting, Time-on-Target caption delay and Help/Player-menu/Chat regions remain covered. The OS pointer is hidden while focused inside the client area in Menu, Loading and Running, then restored on leave or focus loss without losing a stationary pointer's position. |
| Debug and network-status viewport overlays | `C4Viewport::Draw`; `C4GraphicsSystem::ToggleShow*`; `C4Network2.cpp:1135-1180` | **Partial** | Debug geometry, paths, solid masks, key priority/flags/flashes and detailed live network text are covered. The protocol line's `i`/`o`/`bc` fields now read the same cached per-protocol accumulator the network chart samples, through a read-only accessor so drawing the overlay cannot consume the interval `GenerateStatistics` owns; the combined `Msg/Data` and split `Msg`/`Data` formatting follows the native NetIO comparison (`network_status_overlay_displays_live_protocol_rate_samples`). KeyConfig gamepad codes, debug-log sink and console default-on behavior are M10. |
| Rust-only FRAME/POS/VEL debug HUD | no C++ visual authority; `LC_APP_HUD_DEBUG`, `GraphicsSystem::draw_hud` | **Complete** | `LC_APP_HUD_DEBUG=1` is accepted only in debug builds for an interactive launch. Classic compatibility arguments, test/headless/capture switches, and `LC_APP_DUMP_FRAME`/`LC_APP_DUMP_OBJECTS` fail closed; the parity and compatibility constructors force the HUD off before their first update or render. The HUD is additive developer diagnostics only: classic help, flash, network status, dialogs and errors retain their own presentation layers. `debug_hud_requires_developer_interactive_launch`, `debug_hud_is_suppressed_for_parity_and_compatibility_launches`, `debug_hud_launch_classification_is_fail_closed` and `overlay_state_feeds_the_hud_render` pin the contract and normal-off state. |
| Scenario script component assembly | `C4CFN_Script`; `C4ScriptHost::Load`; `C4ComponentHost::LoadAppend` | **Complete** | Scenario hosts concatenate the top-level `Script.c`, first readable `Script<language>.c`, and first readable `C4Script<language>.c` segments in template order. Language priority restarts per segment with native two-byte codes shared by string-table lookup; successful components—including empty ones—receive one leading LF and truncate independently at NUL, unreadable candidates fall through, and unrelated scripts remain excluded (`scenario_script_assembly_uses_all_c4cfn_script_segments_in_language_order`). |
| Native `.c4m` compiler grammar | `C4MaterialCore::CompileFunc`, `C4MaterialReaction::CompileFunc`; `StdCompilerINIRead` | **Complete** | Real material-group loads use exact case-sensitive `Material`/`Reaction` trees and first-match/default behavior, preserve section tails and scalar shadowing, consume signed/unsigned numeric prefixes with native narrowing, fill fixed comma arrays positionally, and keep material integer flags distinct from exact native reaction booleans (`material_parser_matches_stdcompiler_section_key_and_value_grammar`). |
| S2 map point-after-operator parser bug | `C4MCParser::ParseTo`; `C4MCOverlay::RenderPix` | **Complete** | The parser preserves the native state-order bug that accepts a `point` after an overlay operator. The point stays in the child list but is skipped as an overlay operand without consuming the pending operator, so the next overlay completes the chain and later declarations continue; a point used as the left operand still fails its native `SetOp` check (`point_keyword_after_operator_is_ignored_and_chain_reaches_next_overlay_like_cpp`). |
| Basic dynamic-map surface precision | `C4MapCreator::Create` | **Complete** | Amplitude, Phase, Period and Random narrow through native single-precision floats before promotion into the double surface formula; MapPlayerExtend also multiplies Period at float precision. Ordinary map goldens and the large-value rounding boundary are pinned by `flat_map_fills_surface_material_below_the_level_line` and `basic_map_quantizes_surface_parameters_through_f32_like_cpp`. |
| Exact-landscape format and PNG presentation load | `C4Landscape::Load`; `PixCol2MatOld`, `PixCol2MatOld2`; `CSurface8::Read` | **Complete** | Exact indexed landscapes convert format 0 through the old three-colors-per-material/IFT mapping, convert format 1 through numeric material order (including all three Vehicle colors), and reject the first invalid nonzero live-format byte before applying `DiffLandscape.bmp`. The shared indexed loader retains CSurface8's fixed 256-entry palette, positive-height bottom-up rows and forward pixel offset while deliberately ignoring the file signature and compression tag before consuming the payload as raw indices (`indexed_bitmap_ignores_signature_and_compression_like_csurface8`). A case-insensitive `Landscape.png` sidecar fills the full packed Surface32 presentation plane with native inverted alpha while leaving Surface8 authoritative; its mere presence suppresses format-0 conversion and decode failures remain nonfatal. Duplicate-name materials retain their own numeric `DefaultMatTex`, and a PNG-backed surface renders without material-texture assets (`exact_landscape_honors_new_style_version_png_and_invalid_bytes`; `surface32_landscape_renders_without_material_resources`). |
| Legacy SkyFade channel projection | `C4SLandscape::CompileFunc`; `C4Sky::SetFadePalette`; `C4RGB` | **Complete** | Without a sky bitmap, the native signed zero-sum `SkyFade` predicate (including all-zero) retains the game-palette gradient; every explicit signed channel—including negative and oversized values—projects to its low byte instead of clamping (`legacy_sky_fade_channels_wrap_to_low_byte_like_c4rgb`). |
| Signed material-flag truthiness | `C4MaterialMap::CrossMapMaterials`, `mrfCorrode`; `C4Landscape::Incinerate`; `FnFxFireStart`, `C4Object::ExecFire` | **Complete** | Natural reactions, all five flag-based `TargetSpec` categories, landscape ignition, fire-start denial and both native/inherited fire timers treat every nonzero signed material flag as true. Corrosive strengths and custom `CorrosionRate` remain raw signed probability thresholds with native short-circuit draw order (`negative_material_reaction_flags_are_truthy_like_cpp`; `can_incinerate_respects_inflammable_materials`; `incinerate_object_matches_cpp_start_semantics`; `fire_timer_extinguishes_in_extinguisher_material_and_kills_the_effect`; `incinerate_landscape_spawns_one_flam_on_inflammable_material`). |
| Material and texture native-byte identities | `C4MaterialCore::CompileFunc`; `C4MaterialMap::Load`; `C4TextureMap::{LoadMap,LoadTextures,GetTexture}` | **Complete** | Material sources and TexMap tables retain every pre-NUL native byte through a reversible string projection. Compiled material names and admitted texture basenames truncate to `C4M_MaxName`'s first 15 bytes with C++ `CharCapital` matching, while TexMap pairs remain raw and unbounded. Same-load material prefix collisions keep both numeric slots with first-name lookup; texture admission preserves full-name-before-truncation asymmetry, PNG-before-BMP order, duplicate fixed identities and latest long-collider shadowing. MatMap/TexMap serialization emits native bytes, raw TexMap pairs round-trip, and indexed BMP surfaces remain landscape-only patterns (`material_and_texture_names_use_c4m_max_name_bytes`; `presentation_texture_long_name_collisions_keep_the_latest_surface`; `indexed_material_patterns_select_the_native_color_triplet`). |
| Custom material reaction type dispatch | `C4MaterialReaction::CompileFunc`; `C4MaterialMap::CrossMapMaterials` | **Complete** | Only the exact compiled strings `Script`, `Convert`, `Poof`, `Corrode`, and `Insert` select callable reactions. Unknown, absent, trailing-decorated, and mis-cased entries whose `TargetSpec` selects a pair install the native user-defined `NoReaction` sentinel across every event slot, suppressing built-in fallback without consuming a script callback index (`custom_reaction_type_names_are_case_sensitive_like_cpp`; `unknown_reaction_types_install_overriding_no_reaction_like_cpp`). |
| Loader/progress/log screen | `C4LoaderScreen.cpp:126-177`; `C4Game.cpp:351-536,2452,2598,2613,2619-2635,2661-2714`; `clonk-frontend/src/loader_screen.rs`; `clonk-app/src/main.rs` | **Partial** | The resource-validating frontend and app wire classic group priority/order, weighted loader selection, extension-directed decode, title and GUI-resource precedence, user gamma, resource refresh, logical chrome and scale-native text. Valid italic markup now receives the shared centered transform; inline images in the providerless FontTitle/FontTiny title and log paths are consumed unresolved like C++, and the loader's only FontRegular text is its synthesized numeric percentage. Interactive loading overlays include the classic GUI cursor between dialog bodies and their delayed tooltip pass in both logical and ordered-native composition. At every positive nonidentity scale with representable output and native-font geometry—including `Config.Graphics.Scale=50`—the ordered compositor preserves C++'s loader-text-before-GUI order for host start-wait and client message-dialog layers (`scale_fifty_host_and_client_waits_keep_ordered_loader_composition`, `scale_three_host_start_wait_renders_after_native_loader_text`, `fractional_client_wait_and_upper_dialog_keep_native_layer_order`); the raw accepted percent also survives Options and shutdown persistence. Legacy scenario workers now publish monotonic, C4Game-numbered coarse checkpoints while Rust decodes scenario metadata, definition/script/material resources, landscape state and object records, and the live loader retains their cumulative synthetic phase-status history. Main-thread activation owns 94–99, and 100 is committed only after successful offline activation or network GO finalization (`app_loader_keeps_progress_monotonic_and_retains_phase_status`, `real_legacy_worker_updates_live_loader_through_activation`, `accepted_loading_reaches_100_only_after_successful_activation`, `client_go_combines_scenario_once_and_defers_100_until_final_init`). Unsupported Extra/language-pack ambiguity fails closed. Arbitrary global tracing output does reach the loading screen while it is up: `loader_log_sink` retains exactly the events the message board takes, so an ordinary `tracing` event lands in the loader buffer with no explicit push while an engine-internal target with no C++ `Log()` counterpart does not (`a_live_tracing_event_reaches_the_loader_without_an_explicit_push`). The **process text is dead code in the shipped engine**, not a port gap: `C4LoaderScreen::Draw` declares `int Process = 0` (`C4LoaderScreen.h:38`) and its only call site passes three arguments (`C4MessageBoard.cpp:251`), so the `if (Process)` suffix never draws. The port keeps the field and its draw path but leaves it unset, which is what C++ does. Remaining: nonpositive scale and unrepresentable output/font geometry remain typed boundaries rather than C++ config sanitation. |
| Loader generic-fallback boundary | `C4LoaderScreen`; `clonk-app/src/main.rs::render_loading` | **Fail-fast** | Every missing selection, resource, font, invalid/nonpositive scale and render failure is logged and returned as a typed error before the old generic pane can draw. This guard remains required for the unresolved Partial states above; it is not evidence that live progress/log behavior is complete. |
| Object/global game messages (`C4GameMessage`) | `C4GameMessage.cpp:38-231`; `C4ObjectCom.cpp:353-361` | **Complete** | Global/player-global messages use C++ viewport-relative geometry, wrapping, portrait resolution, snapshotted FrameDecoration, clipping and insertion order; high-DPI native rendering and Tutorial01's exact pixels are pinned. Target/TargetPlayer messages now resolve each physical viewport's exact parallax/shape-offset anchor, logical bounds, Target-only `IsVisible`, adjusted-point FoW and `C4D_IgnoreFoW` gates before native wrapping, clamping and drawing. Inline `{{image}}` text works in every variant; remote-player, missing-target, offscreen and hidden messages are silently skipped. Failed direct, auto-stop and queued Dig attempts emit the active-language `IDS_OBJ_NODIG` once through exact `GameMsgObject` target/replacement semantics (`object_com_dig_failure_emits_no_dig_object_message`; `queued_dig_routes_action_rejection_through_object_com_dig`). |
| Message board modes/history | `C4MessageBoard` | **Complete** | All three ChangeMode states, dynamic viewport reservation, continuous multi-line history, Shift+Up/Down scrolling, the 1000-line/30000-byte LogBuffer, player-colored text, and per-presentation Fader/ScreenFader text transitions are implemented and tested. `/msgboard` reproduces C `atoi` bounds 0..20 and the native bool-typed `Graphics.MsgBoard` persistence quirk: a runtime multi-line count reloads as ordinary one-line mode. Missing `Background.png` is rejected by the same typed HUD-resource boundary before output pixels; no solid-black substitute remains. |
| Runtime per-client information dialog | `C4Network2ClientDlg` | **Complete** | F4 and lobby Info share retained modal ownership, scrolling, title drag, tooltips and one-second offset-preserving refresh (`standalone_client_info_starts_on_the_requested_row_and_closes_as_info`; `standalone_client_info_routes_wheel_and_keyboard_to_overflow`). Both resolve the native body templates through the active language table, retain the selected client and scroll offset across refresh, and show the localized unknown-client-ID and host acknowledgement branches described in the row above. |
| Runtime client list | `C4Network2ClientListDlg` | **Partial** | Host mute/activate/kick/disconnect (league-gated), connection rows, live Tick/Behind/Rate/PreSend/ACT footer, pointer hover/click row and info-panel interaction, wheel scroll, Escape/toggle lifecycle, retained title drag, 3-second bounce auto-scroll, and shared-delay title/Close tooltips are implemented and tested. The live runtime `C4GameOptionsList` exposes authority-gated control-mode/rate and host-only runtime-join dropdowns, with committed/echoed state, one-second refresh, clipped-list scrolling and native option tooltips. Running fullscreen matches C++'s nonexclusive shared-screen scope: Tab/Return/arrows and ordinary gamepad traversal do not enter the dialog, while its dedicated fullscreen Escape/gamepad-0 AnyHigh binding closes it; the frontend controller retains full focus traversal for a future exclusive console-window route. The exact 500 ms name-label tooltip contains only active player names. Remaining: console-mode native-child presentation. |
| Runtime ready-check timed dialog/toast | `C4Network2::ReadyCheckDialog`, `C4Network2.cpp:129-198,1625-1690` | **Partial** | `MessageDialogContinuation::LobbyReadyCheck` reproduces `C4Network2::ReadyCheckDialog`: exact 'Are you ready?' caption, `IDS_DLG_READYCHECKTEXT` per-second countdown, 15s timeout closing as not-ready, `CanBeReady`/resource-loaded gating, and Yes/No wired to `PID_ReadyCheck` (`complete_client_ready_check_opens_one_exact_fifteen_second_prompt`, `ready_check_prompt_counts_down_and_times_out_not_ready_at_fifteen_seconds`). Unfocused clients now receive one desktop notification through WinRT on Windows or the freedesktop notification service on Linux when `Toasts.ReadyCheck` is enabled; initialization and delivery failures warn and continue, and unsupported platforms safely no-op (`unfocused_ready_check_queues_one_enabled_desktop_notification`, `desktop_notification_delivery_failure_is_nonfatal`). Remaining: Yes/No notification actions, activation behavior and early dismissal when the dialog resolves. |
| Network vote dialog | `C4VoteDialog`, `C4Network2::Vote`, `AddVote`, `EndVote`, `OpenVoteDialog` | **Complete** | Every production vote start (Part, Pause/Unpause, client/lobby/menu kicks and both `/kick` routes) now shares the exact `(Type,Data)` subject gate: active subjects bypass the strict global 120s original-vote cooldown, existing local ballots stay silent, and other blocked subjects emit the native timeout log. A rate-limited inactive Cancel or local-self Kick immediately opens the same singleton default-No `LeagueSurrender` confirmation as a rejected own VoteEnd. The remaining lifecycle covers 10s host timeout->reject, ordered pending votes, host pause-for-vote, `IDS_VOTE_*`-matching text and Yes/No dispatch. |
| League surrender confirmation | `C4Network2::OpenSurrenderDialog`, `LeagueSurrender` | **Complete** | The default-No Yes/No text matches `IDS_VOTE_SURRENDERWARNING` and is reachable after a rejected own kick/cancel vote. Accepting first records the active-language `IDS_ERR_YOUSURRENDEREDTHELEAGUEGA` as `NR_NetError` and synchronizes the presentation snapshot, then hard-clears the network and returns to the menu. Matching C++'s `LeagueSurrender`, the surrendering client intentionally sends no disconnect report and leaves that notification to the other clients. |
| Network statistics chart | `C4ChartDialog`; `C4Network2Stats` backing model | **Complete** | A per-game NetworkStats now samples active objects, FPS, transport rates/pings, and drained player control/APM counts. Reliable-UDP wire traffic is attributed independently to each real connection route (including handshake carryover, 32-byte datagram overhead, close/reopen cleanup and per-peer high-level broadcast sends) instead of a synthetic whole-socket key; low-level multicast broadcast remains a separate aggregate. `/chart` plus configurable default-unbound ChartToggle own a singleton 400x300 Statistics dialog with native `oc`/FPS/NetIO/(network-only Pings)/Control/APM tabs, live scrolling graphs, tab input, and stronger Escape close. Headless network-sandbox and frontend regressions pin lifecycle, samples, conditional tabs, and toggling. |
| League signup/register/auth form | `C4LeagueSignupDialog`, `C4League.cpp:548-749`; `C4Network2::LeaguePlrAuth` | **Complete** | The app-wired classic modal now prompts exactly when `LeaguePassword` is empty or `LeagueAutoLogin` is disabled, while usable auto-login credentials submit silently. Server `Status=Register` switches the same player into the native Account plus optional-password/confirmation form, including Nick/player-name fallback, collapsed/expanded geometry, masked classic edits, title drag/scroll, clipboard/context behavior, initial focus and byte-exact account validation in C++ order. Login persists only `LeagueNick`; the password remains process-local. Server refusals and empty-AUID replies show the native error modal, clear the process password after dismissal and retry the same player/form. Abort shows the native notification before swap-removing that player and sends no Auth request. App regressions cover initial-client, later local-player and staged-host continuations; cancellable waits and welcome confirmation are covered in the separate row below. |
| League authentication/end waits and retries | `C4League`, network dialogs | **Complete** | `LeaguePlrAuth` now runs as a pollable request with the exact Abort wait, optional auto-login, server/derived OK-or-Abort welcome confirmation, cancellation notice, password clearing and error retry across initial-client, staged-host and runtime-player ownership. C++'s distinct `LeaguePlrAuthCheck` remains intentionally blocking with no cancellable wait; transport failures and refusals use its Error surface. `LeagueEnd` likewise has no native in-flight dialog: request-start/transport failures use Retry/Abort for at most ten attempts, server rejections are Abort-only, and console/headless cleanup retains the native ten-attempt loop. Final success/failure packets preserve legacy text and result rows, always evaluate/broadcast/latch (including teardown), and suppress later Update/End uploads. The credential/register form and unregistered-account handoff are covered in the separate row above. |
| Reusable URL/download progress dialog | `C4DownloadDlg` | **Partial** | A reusable transport-agnostic controller now drives the existing classic NetWait/status/progress/Cancel surface from overflow-safe HTTP byte callbacks, emits an abort request on Cancel/title-close, closes silently on completion and presents caller-formatted transport failures in the separate Error/OK modal used by C++. Compatible-release downloads are now a production caller of the byte-progress/cancellation contract, aggregating component transfers into one progress surface before size and digest verification; synthetic-transfer tests cover progress, abort and terminal presentation. An unknown reported length now suppresses the bar rather than freezing the last real percentage, including a transfer that starts with a known length and then stops reporting one. Cancelling ends in the same terminal modal a transport failure does: `UserClose` fails the transfer with `IDS_ERR_USERCANCEL` rather than dismissing it, and the wrapper composes `IDS_PRC_DOWNLOADERROR` naming the file and the error, appending the not-found message after a `|` when the composed text mentions 404 (`C4DownloadDlg.cpp:128-131,167-196`; `cancelling_ends_in_the_download_error_modal_naming_the_user_abort`, `a_404_failure_appends_the_not_found_message_on_its_own_line`). The update download is a second, independent implementation of the same byte-progress rule and now honours the unknown-length case too, dropping the bar instead of reporting a stuck 0% for a transfer that never announces a length (`an_unknown_update_length_suppresses_the_download_bar`). Remaining: a failed update download still ends in the generic `IDS_MSG_UPDATEFAILED` notice, where C++ routes it through `C4DownloadDlg::DownloadFile` and therefore through the `IDS_PRC_DOWNLOADERROR` modal with `IDS_MSG_UPDATENOTAVAILABLE` as the 404 suffix (`C4UpdateDlg.cpp:165`); closing that means making the update flow a real caller of the reusable controller. Also remaining: per-file speed/time presentation, which has **no counterpart in the pinned C++** — `C4DownloadDlg::OnIdle` shows only the filename and a percentage, and no download speed or ETA is computed anywhere in the engine — so it is a divergence to decide rather than a port gap to close. Lobby resource status/save is the separate `C4Network2ResDlg` subtree above. |
| File/player/definition/portrait selectors | `C4FileSelDlg` family | **Partial** | Definition, Player and the core Portrait selector are app-wired with classic scrolling, focus/input, location jumps, progressive image grids, None tile, Picture/BigIcon options and native tooltip targets. Portrait still lacks selector-title drag/bounce auto-scroll, active-font-derived geometry, runtime-localized selector strings and hotkeys, and long-caption ContextMenu expansion and viewport clamping; the detailed Portrait row above records its intentional non-simulation C++ corrections. Covered by `geometry_matches_the_classic_component_aligners`, `keyboard_focus_toggle_accept_cancel_and_refresh_contract`, `player_mode_is_single_selection_and_accepts_the_full_path` and `portrait_sel_dialog_none_item_clears_portrait`. |
| Rust-only F5 quick save | no C++ menu equivalent; C++ uses `C4MainMenu::ActivateSavegame` | **Complete** | The unsupported bare F5 binding is absent; supported saving remains in the classic `ActivateSavegame` ten-slot flow. |
| Rust-only F9 quick load | conflicts with C++ `C4Game.cpp:3373-3374` | **Complete** | Resolved: F9 no longer quick-loads or fails typed. handle_runtime_global_key now queues the real C4GraphicsSystem::SaveScreenshot(false)/(true) action via pending_screenshots for F9/Ctrl+F9, working above open dialogs and never ending the game — pinned end-to-end by running_f9_saves_presented_rgb_and_ctrl_f9_saves_full_landscape. Loading remains routed through the oracle-backed savegame menu (MenuAction::ActivateSavegame); screenshot fidelity gaps are tracked on the next two rows. |
| C++ F9 viewport screenshot | `C4Game.cpp:3373`; `C4GraphicsSystem::SaveScreenshot(false)` | **Partial** | Running F9 performs first-free Screenshot{:03}.png slot reuse, [General] ScreenshotFolder fallback, gamma-correct RGB output and exact active-language IDS_PRC_SCREENSHOT/IDS_PRC_SCREENSHOTERROR info-level log/message-board text with the ExePath-relative selected path on success or failure. Capture requires the live Pixels backing frame; ordinary OS-windowed play remains eligible because C++ `Application.isFullScreen` means graphical-client rather than `/console` mode. Bare F9 is registered `KEYSCOPE_Fullscreen | KEYSCOPE_Gui`, so it now also captures the presented frame on every startup view through the same numbered slot, gamma and localized result log; Ctrl+F9 is Fullscreen-only and stays inert there, matching the two registrations (`startup_f9_saves_the_presented_classic_gui_frame`). |
| C++ Ctrl+F9 whole-landscape screenshot | `C4Game.cpp:3374`; `C4GraphicsSystem::SaveScreenshot(true)` | **Complete** | Ctrl+F9 retargets to NO_OWNER, clears sky parallax to 10/10, and renders cursor marks plus the parallax foreground-object layer through the shared viewport pipeline before restoring state. The live backing-frame guard and localized path-aware success/failure log are shared with F9; Rust's single-pass render replaces C++'s tiled/page-flip implementation without changing the captured output. |
| Rust-only F6/F7 named save/load browser | no C++ equivalent | **Complete** | Bare F6/F7 bindings and the unreachable Rust-only named save/load presentation state were removed. Save/load remains available only through the classic ten-slot flow. |
| Rust synthetic menu sandbox | no C++ player-facing equivalent; `clonk-app/src/main.rs` | **Complete** | Empty discovery/catalog paths no longer inject the sandbox into player-facing menus. The explicit `--sandbox` developer/test route remains intentionally separate from menu discovery. |

## Required generic-fallback boundaries

Until the classic screens above are wired, central app boundaries should group
the remaining unsafe presentation routes rather than adding more status-only
call-site checks:

- startup status substitute: include the active startup view and producer/message;
- running viewport unavailable: distinguish only genuinely missing declared
  local slots; zero-object/no-local and focusless owned viewports already have
  real object-independent or retained camera paths;
- game overlay unavailable: identify remaining unmodeled Pause hold sources and
  debug/network paths. F1 Help, direct Pause/Unpause, every Upper Board mode,
  action cursors, Eliminated, timed Flash, viewport command buttons and
  Film+Replay suppression are implemented. `LC_APP_HUD_DEBUG` is a
  developer-only additive diagnostic and is never a fallback for a missing
  classic layer;
- HUD resources unavailable: report the complete missing classic resource set
  before any generic font, solid surface or omitted image can draw.

The boundary is reached only when that presentation would be visible. Merely
retaining an old renderer in the tree is acceptable when a tested guard makes
it unreachable.

## Reusable modal infrastructure required

Full recursive parity requires classic implementations of these C++ dialog
families before individual call sites can be considered complete:

The ordinary classic message-dialog foundation is now **Partial**: regular,
medium and small geometry; normal/extended icons; canonical OK/Retry/Cancel/
Yes/No ordering; title-close; active-only stacked focus; keyboard, Alt hotkeys,
mouse, touch and legacy low/high gamepad routing; GUI sounds; no-scrim rendering;
continued underlying timers; fail-fast asset validation; and the localized-label
capable “don't show again” checkbox are implemented. Caption drag, the 3 s
bounce auto-scroll and the title tooltip now ship for `message_dialog.rs`,
the F4 client list and its independently draggable info child,
Advanced Options, and the definition selector. Each close button shares
the same 500ms process-global tooltip clock. Startup/menu pointer routing now
also matches the exclusive C++ screen: movement and retained press–move–release
stay with the active message dialog, so OK and title-close activate without
leaking input below it. Standard button strings use active language resources.
Active-scenario `Graphics.c4g` and font overrides remain.

- message/error/info (**Partial**: the reusable base now backs many
  typed continuations beyond the original two — startup network
  join/redirect/scenario warnings, options scale-test/control-capture/
  alternate-server/reset/advanced warnings, and the runtime league
  vote/surrender and lobby ready-check flows. Active-scenario
  `Graphics.c4g` sheet and font overrides now rebind the shared dialog
  resources at the GraphicsResource refresh and restore at teardown);
- Yes/No and multi-button confirmation (**Partial**: beyond player deletion,
  Yes/No confirmations now gate startup player/crew/scenario deletion, league
  vote/surrender prompts, and the 15 s lobby ready-check and
  graphics-scale-revert timed prompts. The shared active-scenario resource
  override now applies through the rebound process-global sheet set);
- “don't show again” checkbox (**Partial**: exact component exists; the
  alternate-league-server warning (`HideMsgNoOfficialLeague`) is now a wired
  caller; remaining `HideMsg*` config-bound callers are M10);
- text/password input (**Partial**: `clonk-frontend/src/input_dialog.rs` provides
  the resource-validating classic controller/renderer; network-selector
  Password/Comment, the lobby host Password/Comment, the network-join password
  challenge and the crew death-message editor are app-wired; active-scenario
  sheet/font overrides now apply through the rebound process-global set);
- timed confirmation (**Complete**: the lobby ready-check prompt (15 s,
  `tick_lobby_ready_check_prompt`) and the graphics scale-test prompt (12 s,
  `tick_options_scale_test_prompt`) both match C++'s per-second text update and
  timeout-as-No close; these are the native `TimedDialog` callers);
- wait/progress with cancel (**Complete**: reusable `ProgressDialogState` is
  localized-Cancel-only and Enter-inert; `BlockingResourceWait` supplies live
  percent, cancellation and a 100-second unchanged-progress timeout that resets
  whenever progress advances, with a headless client join pinning 0→17→63%
  before Cancel tears down the wait and join);
- scrolling expandable `InfoDialog`/log text (**Complete**: reusable
  configurable-line-count scrolling state, TextWindow geometry,
  wheel/scrollbar, keyboard accessibility and scroll-preserving per-second
  refresh back live runtime/lobby client info; the static 10-line startup
  `Error Log` preserves consumed ringbuffer order and uses the regular
  `(no error)` empty-log fallback);
- context and nested context menus (**Partial reusable chassis**: exact classic
  geometry/assets, recursive submenus, deepest-first pointer routing,
  keyboard/hotkeys, touch and low/high gamepad input, flip placement,
  selection/tooltips/sounds, focus suppression, outside-down pass-through,
  action-release capture and fail-fast resources are implemented; startup
  player-row Properties/Delete, the startup Participants Add/Remove tree, the
  scenario-search Edit actions, crew rows (`PlrSelCrewContextMenu`), lobby
  right tabs (`open_classic_lobby_tab_context`), the lobby roster Client-row
  Kick menu, the lobby team-select combo and the lobby roster Player-row Take
  Over/Remove/New Color root entries are callers. Remaining consumers include
  combo boxes beyond the lobby team combo. Take Over children and association
  dispatch are covered on both lobby paths; submenu-open refresh timing
  remains);
- combo-box dropdowns (**Partial**: the classic lobby team-select
  `C4GUI::ComboBox` opens through the shared context-menu chassis
  (`open_classic_lobby_team_combo`) with keyboard/mouse/permission-recheck
  coverage, so activating a roster team dropdown opens the team menu instead of
  aborting. Other `ComboBox` consumers (options-screen fields) are unverified
  against this chassis);
- file/image/player/definition selection (**Partial**: Definitions, Player and
  the core Portrait body are wired, including the portrait location combo,
  progressive image grid, None tile and Picture/BigIcon options; the definition
  selector has shared Dialog title drag and auto-scroll; Portrait now has the
  native title/Close/option/location-entry tooltip targets but still lacks those
  title behaviors, active-font-derived geometry, runtime-localized selector
  strings, and long-caption ContextMenu expansion and viewport clamping).

Unsupported entry points must keep logging and returning an error until their
classic implementation lands. Reintroducing a generic pane is not completion.

## Editor-only GUI, tracked separately

The Rust startup editor/controller substitute is not this GUI and does not make
any row Partial. Until these branches exist, editor entry points must be
explicitly rejected instead of opening a synthetic controller or an external
editor as if parity had been reached.

| Screen or recursive branch | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Console shell, native menu bar, log, script entry and status bars | `C4Console.cpp:277-583,1358-1408` | **Partial** | `/console` opens a persistent scale-one winit developer window with the complete menu shell, bounded scrolling log capture, reachable history/completion entry, cursor text, change-cached Frame/Script/time+FPS labels and exact running/lobby enable rules. `/`, `#` and plain script input retain the native routing and strictness behavior. Remaining: native window-position persistence and `[Developer] AutoFileReload`/file-monitor behavior. |
| Tools/Property developer toolbox window chrome | `C4DevmodeDlg.cpp:63-68,90-121` | **Partial** | Showing or reopening the shared toolbox raises it and requests input focus through `winit::Window::focus_window`, while preserving its remembered position and page lifecycle. The X11-specific winit extension carries the classic utility hint when X11 is active. The portable API has no equivalent for the arbitrary `"toolbox"` role, transient-for-console relation or centre-on-parent placement; those three chrome details are an accepted platform limitation. The port keeps an ordinary resizable top-level window and does not substitute always-on-top or child-window behavior; Wayland compositor policy may also decline the focus request. |
| Play/Halt and Play/Edit/Draw mode toolbar | `C4Console::InitGUI`, `UpdateHaltCtrls`, mode callbacks | **Partial** | Play/Halt/Pause drive the native offline or network pause paths and reflect pending network status; mutually exclusive mode controls, Open reset, one-way no-input editing latch and GTK lobby gates are covered. Edit/Draw cursor and tool behavior remains in the dedicated rows below. |
| File / Open | `C4Console::FileOpen` | **Complete** | The native selector accepts `.c4s`, `.c4f` and exact `Scenario.txt`, performs Close/Default/PreInit/Init through the existing in-process loader, resets the edit mode, and leaves the persistent console alive after an Open failure. |
| File / Open with Players | `C4Console::FileOpenWPlrs` | **Complete** | The two-stage scenario/player selector retains ordered multi-selection and feeds the selected `.c4p` paths through the same in-process initialization command line. |
| File / Save Scenario and Save Scenario As | `C4Console::FileSave`, `FileSaveAs(false)` | **Complete** | The live C4 serializer follows native destination order through Scenario/Game and section cleanup, landscape/material, Strings/Objects, results/teams and Script/Title/Info, while preserving `C4String` and object enumeration identity and exact static/forced-landscape behavior. Save As preserves packed versus unpacked representation, overwrite/path/caption behavior and C4Group sorting; a late or landscape failure persists the already-written native prefix instead of rolling the group back. Folder packing keeps raw unwrapped images as files and wrapped images as children. |
| File / Save Game and Save Game As | `C4Console::FileSave`, `FileSaveAs(true)` | **Complete** | SaveCore writes Parameters before Scenario, then runtime components, delete-before-write `SavePlayerInfos.txt`, and embedded player groups one at a time in native runtime/player-info order; late errors retain earlier mutations and player children. The policy synchronizes physical local profiles, preserves user-only entries, and reopens offline saves including the old `Game.txt` `[PlayerFiles]` fallback. `Desc<language>.rtf` uses startup-cached raw legacy language text, filename bytes and charset, without a UTF-8 round trip. |
| File / Record | `C4Console::FileRecord` | **Complete** | Runtime-record eligibility queues the ordinary synchronized boundary and uses C++ index/basename/header policies. Fresh and loaded-game initial records use the live serializer; a loaded game first reconstructs its saved runtime source before the native `fInitial` splice. Recording keeps the unpacked working-group lifecycle, durable incremental `CtrlRec.c4b` writes with partial-write offsets, stream-before-local resource ordering, native embedded-player handling, and final roster/description cleanup, maker propagation and packing. |
| File / Close and Quit | `C4Console::FileClose`, `FileQuit` | **Complete** | Close performs the full in-process game/network teardown and restores the default caption without closing the developer window; Quit requests application shutdown. |
| Components / Objects | `C4Console::EditObjects`, `C4ObjectListDlg` | **Missing** | Open/update the recursive Object List screen described below. |
| Components / Script | `C4Console::EditScript` | **Missing** | Scenario script component editor, save/reload and syntax/error handling. |
| Components / Title | `C4Console::EditTitle` | **Missing** | Title component text editor and persistence. |
| Components / Info | `C4Console::EditInfo` | **Missing** | Info component text editor and persistence. |
| Player / Join | `C4Console::PlayerJoin` | **Complete** | The multi-player selector attempts every selected path and routes offline joins through `CtrlJoinLocalNoNetwork` semantics or network joins through resource publication and player-info control. |
| Player / dynamic Quit entries | `C4Console.cpp:1410-1461` | **Complete** | Rows rebuild in live player order with network client captions and host/editing gates; activation queues `CID_EliminatePlayer`, including local control-rate cadence rather than immediate mutation. |
| Viewport / New | `C4Console::ViewportNew` | **Partial** | Ownerless physical viewport creation and lifetime state are wired; detached native OS viewport windows, placement and independent input remain below. |
| Viewport / dynamic per-player entries | `C4Console.cpp:1203-1266` | **Partial** | Rows rebuild in player order and create player-targeted physical viewport state; detached window presentation remains below. |
| Network / dynamic client entries | `C4Console.cpp:1540-1608` | **Complete** | Host and client rows retain native labels/order and sensitivity; callbacks recheck control-host authority and submit the synchronized client-removal reason. |
| Help / About | `C4Console::HelpAbout` | **Missing** | Native developer About dialog. |
| Landscape mode selector | `C4ToolsDlg.cpp:262-407,865-896` | **Missing** | Dynamic/Static/Exact modes and enable/update rules. |
| Drawing tool selector | `C4ToolsDlg` | **Missing** | Brush/Line/Rectangle/Fill/Picker, temporary alternate tool and cursor integration. |
| Drawing material/texture/grade/IFT controls | `C4ToolsDlg.cpp:410-1015` | **Missing** | Material and texture combos, preview, grade slider, IFT toggle, defaults and landscape validation. |
| Object Properties display/script input | `C4PropertyDlg.cpp:62-68,103-166,394-399`; `C4EditCursor.cpp:463-467,724-744` | **Missing** | The reusable input backend snapshots caller-owned live selection order into a decided `EMMO_Script` packet with configured strictness; executor and transport are covered. The property window, selection-dependent text, definition reload, live edit-cursor ownership and reachable object-scoped entry widget remain absent. |
| Object List tree/selection | `C4ObjectListDlg.cpp:486-805` | **Missing** | Live add/remove/rename updates, definition icons, multi-selection and edit-cursor synchronization. |
| Shared developer notebook/window | `C4DevmodeDlg.cpp:44-130` | **Missing** | Add/remove/switch pages, title, hide/show, transient ownership and remembered position. |
| Edit-cursor context root | `C4EditCursor.cpp:79-104,582-627` | **Missing** | Permission-sensitive native popup and mode-dependent Properties/Tools label. |
| Edit cursor / Delete | `C4EditCursor::Delete` | **Missing** | Selected-object control dispatch and state refresh. |
| Edit cursor / Duplicate | `C4EditCursor::Duplicate` | **Missing** | Duplicate selection/control semantics. |
| Edit cursor / Grab Contents | `C4EditCursor::GrabContents` | **Missing** | Replace selection with contents, update properties and exit objects. |
| Edit cursor / Properties or Tools | `C4EditCursor::OpenPropTools` | **Missing** | Route by Edit versus Draw mode to the correct developer page. |
| Detached/new viewport windows and viewport input | `C4Viewport`, `C4Console::ViewportNew` | **Missing** | Native windows, player targeting, cursor context and teardown. |
| Platform open/save dialogs | console and component callbacks | **Partial** | Console Open/Open-with-players/Join/Save As use native platform choosers with overwrite/cancellation, purpose-aware filters and exact post-selection validation. Component-editor selectors remain unavailable with those editors. |
| Platform message/error/crash dialogs | console/startup/application error paths | **Partial** | Console save guards, warnings and errors now use localized modal dialogs in exact precedence, including sequential warning/error presentation. Expandable crash logs and restart/fatal platform variants remain unavailable. |

These may be scheduled after player-facing menus, but literal full C++ GUI
parity is not achieved while they remain absent.

## Verification gates

Every row promoted to **Complete** needs:

1. a C++-referenced behavior test for each transition and nested action;
2. input coverage for keyboard, mouse, wheel/drag, touch where C++ supports it;
3. state restoration/focus/selection/scroll boundary coverage;
4. render evidence using real classic resources at audited resolutions/scales;
5. no reachable generic fallback;
6. recursive scenario/script corpus coverage where the screen is data-driven;
7. table-driven failure coverage proving every startup status producer returns
   a typed error before cache replay, base composition or scale-native text can
   write startup pixels;
8. zero-object, no-local-player and ordinary local-player viewport tests proving
   that the app neither draws the navy empty-state fallback nor invents an
   observer selection target;
9. direct coverage for F1 Help, Pause, Flash, mouse control, eliminated text,
   all four upper-board modes and debug/network overlays, whether implemented
   exactly or guarded by the correct typed boundary;
10. explicit documentation and tests for intentional product divergences such
    as recursive scenario search; they are never silently labeled C++ parity.
