# Local patches

This directory contains the published `winit` 0.30.13 source from crates.io.

It is vendored so the Wayland key-repeat timer can be reused instead of
removed. See the follow-up commit on this branch for the behavioural patch.
