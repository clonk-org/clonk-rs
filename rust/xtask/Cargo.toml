[package]
name = "xtask"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
walkdir = "2"
zip = { version = "0.6", default-features = false, features = ["deflate"] }
lc-engine = { path = "../crates/lc-engine" }
lc-core = { path = "../crates/lc-core" }
tracing = "0.1"
