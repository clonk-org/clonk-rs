# C++ Menu Parity Inventory

This is the recursive completion checklist for the Rust menu port. The C++
classes in `../src/` are authoritative. A top-level screen is not complete
until every child sheet, context menu, confirmation/input dialog, callback,
scroll path, and transition reachable from it is complete.

Status meanings (the status applies only to the bounded row, not its parent
screen or an unwired library component):

- **Complete**: the reachable Rust path covers the C++ behavior and
  presentation with executable evidence and has no generic fallback.
- **Partial**: a classic-shaped implementation exists, but listed behavior is
  missing or indirect.
- **Fail-fast**: Rust logs and returns an error instead of drawing a generic or
  incomplete pane. This is preferable to false parity, but is not completion.
- **Dropped**: an explicit product decision excludes the bounded legacy feature
  from the Rust port. Any retained entry point must fail closed until its
  separate UI cleanup removes the affordance.
- **Missing**: no usable classic Rust implementation exists. A reachable
  generic, synthetic, status-only, or externally delegated substitute is still
  Missing, not Partial or Fail-fast.

The C++ source and shipped `content/`/`planet/` data are read-only oracle
material for this inventory. Implementations belong under `rust/`; never edit
the oracle to make a Rust result appear correct.

## Recursive census

- Startup graph: `C4Startup::SwitchDialog` plus every callback/subdialog from
  `C4StartupMainDlg`, `C4StartupScenSelDlg`, `C4StartupNetDlg`,
  `C4StartupPlrSelDlg`, `C4StartupOptionsDlg`, and `C4StartupAboutDlg`.
- In-game graph: `C4MainMenu`, `C4Menu`, `C4ObjectMenu`, player team selection,
  object context activation, and generic `C4GUI` dialogs.
- Overlay graph: `C4GraphicsSystem`, `C4UpperBoard`, `C4MessageBoard`, every
  `C4Viewport::DrawOverlay` child, `C4MouseControl::Draw`, network/debug status,
  screenshot feedback and eliminated/observer states. Non-dialog presentation
  is part of the literal recursive screen census.
- Rust-only graph: every app `StartupView`, retained generic renderer,
  status-state producer, environment/developer route and empty-state fallback
  must either be explicitly isolated or return a logged error before drawing.
- Script graph: 147 `CreateMenu` calls in 109 shipped `content/**/*.c` and
  `planet/**/*.c` files, plus 401 `AddMenuItem` calls. Styles comprise Normal,
  Context, Info, and Dialog.
  Full coverage comes from implementing the complete CreateMenu/AddMenuItem
  grammar rather than hardcoding individual scenario screens.
- Modal graph: at least 134 C++ message/confirmation/input/remove-dialog call
  sites across startup, networking, options, league, player, scenario, and
  update flows.
- Editor-only GUI is tracked separately at the end; it is never silently
  omitted from the literal full-GUI scope.

## Startup root

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Main six-button screen | `C4StartupMainDlg.cpp:38-122`; `rust/crates/lc-frontend/src/startup_main_menu.rs` | **Partial** | Dialog switch/fade/back stack (row below) is now Complete: 10-step cross-fade, input suppression and eLastDlgID-style back-stack quirks are implemented and tested. Common-screen cursor/focus/hotkey/tooltip behavior remains the blocker: classic cursor, centralized 500 ms tooltip delay, Program/other-sheet traversal, About gamepad traversal and Network Alt mnemonics. |
| Process-global C4GUI resources | `C4GUI::Resource::Load`; `C4GraphicsResource::InitFonts` | **Fail-fast** | Before startup, loading or running UI can mutate state, select a cache or draw pixels, one aggregate gate requires all five GUI fonts and the exact thirteen global sheets, including `GUIBigArrows.png` and `GUISpinBoxArrow.png`. Resolution preserves C++ base/`Extra.c4g` stem precedence, canonical rebinding and runtime-override detection; tooltip ownership uses its independent shadowless font. Missing, malformed or rebound resources produce a typed logged boundary instead of a default Rust pane. Vector requests absent from registered groups now try the requested filename and then an exact host-system face lookup; genuinely unavailable or malformed faces retain the typed boundary. |
| Eager startup graphics/font bootstrap | `C4Startup::EnsureLoaded`, `C4StartupGraphics::Init`/`InitFonts` | **Fail-fast** | After the process-global gate, one deterministic aggregate preflight requires the oracle-ordered 16-image startup bundle (including the recursively used `StartupPlrPropBG.png` and `StartupPlrCtrlType.png`) and Caption/Main/Title/MainSmall shadowless RX fonts before view/model/child/status checks, cache lookup, or logical/native pixels. All seven Rust `StartupView` roots are covered; `NetworkLobby` retains the prerequisite because C++ reaches its lobby before `C4Startup::Unload`, although it is not a startup `DialogID`. Decoded surfaces/fonts are checked for basic usability without inventing atlas dimensions C++ never validates. At integer application scale above one, Main also refuses a missing or mismatched scale-native font atlas. |
| Main screen fixed-default resource failure boundary | `C4StartupMainDlg`; startup graphics resources | **Fail-fast** | For the currently supported shipped-resource path, the app logs the complete missing set and returns an error before bitmap-font, optional-image, blue-pane or solid-button substitutes can become player-visible. It still hard-requires `LoaderGoldmine1.png`; C++ loader-image fallback remains unported. Font selection uses the shared catalog/file/system resolver above. |
| Dialog switch/fade/back stack | `C4Startup::SwitchDialog`, `C4StartupDlg::OnClosed` | **Complete** | Retain fade-timing (10-step, exact (100-iFade)*255/100 alpha), cross-fade compositing, input-suppression-until-frame-ten, and eLastDlgID-style back-stack regression tests, including that Back does not update the remembered dialog (mirrors C++'s pToDlg==pLastDlg skip) and the nested ScenSel-from-NetJoin Back target. |
| Common startup cursor/focus/hotkeys/tooltips | `C4GUI::Screen`, `Dialog`, `Control` | **Partial** | Bottom-button and Options-tab clicks preserve prior focus where IsFocusOnClick=false; gamepad horizontal routing no longer impersonates keyboard Back/Crew. Options Sound has exact Tab/Shift-Tab, Ctrl(+Shift)+Tab and gamepad-button ownership. About now dispatches Alt+letter mnemonics (L046); Network screens still don't. Centralize the classic cursor, 500 ms tooltip delay, remaining Program/other-sheet traversal and About gamepad traversal instead of per-screen approximations. |
| Participants Add/Remove context and player submenu | `C4StartupMainDlg::OnPlayerSelContext*` | **Partial** | Exact autosized markup label target and tooltip, recursive lazy Add/Remove tree (including empty 40x7 children), raw filesystem/config ordering, raw Remove indices, player icons/tooltips, activation/validation/deduplication and focus suppression are covered. Missing application paths and configuration-write failures retain diagnostic status but now hit the centralized typed boundary before startup pixels or cache output; their classic error dialogs remain unported. |
| Rust startup model/child/status boundary | no C++ screen; `GameApp::preflight_startup_presentation` | **Fail-fast** | After the eager bootstrap passes, both logical and scale-native presentation reject a missing required controller, then any active unported child, then generic diagnostic status before cache replay or pixel composition. Typed child payloads cover the four still-unported Options sheets; Program, Sound and Network Chat are supported panes. A stale matching cache cannot reveal a prior pane. This is a refusal boundary, not implementation of any listed descendant. |
| Startup base-frame cache and nested overlays | C++ redraw/invalidation ownership; `GameApp::render_for_presentation` | **Partial** | Context menus, Password/Comment input, definition selector, message dialogs and the participant tooltip are excluded by one shared cache-eligibility predicate for both lookup and write. Thus composited overlay pixels never replace the reusable base frame, including same-version open/render/close cycles. A stale game-over dialog is no longer treated as a startup overlay at all: the lifecycle boundary wins before cache lookup. Broader classic dirty-rectangle/redraw ownership remains unported. |
| First-run new-player properties modal | `C4StartupMainDlg::OnShown` | **Complete** | Retain the OnShown-equivalent trigger (search path, *.c4p extension check, hidden-file skip, name-only match without opening the group), its re-trigger on every Main show/Back-to-Main and on player deletion, and the Cancel/Submit/save-and-refresh-participants regression coverage. |
| Automatic/manual/incoming update flow | `C4StartupMainDlg::OnShown`, `C4UpdateDlg` | **Dropped** | Update discovery, incoming-package application and automatic checks are explicitly owned by the launcher/package manager in the Rust build; they are not repeated when the in-game main menu is shown. The About button provides the user-visible hand-off described below. |
| F6 editor launch from startup | `C4StartupMainDlg::SwitchToEditor` | **Missing** | Bind and validate the legacy editor launch. |
| Rust editor scenario/controller route | no C++ startup-list equivalent | **Fail-fast** | `ScenarioKind::Editor`, touch activation and `StartupMenuAction::EditEntry` return typed, logged boundaries; the external-editor substitute was removed. Keep the guard until the real developer UI exists. |
| Startup fatal/restart log presentation | `C4Startup::DoStartup`; `GameApp::finish_startup_network_failure` | **Partial** | Terminal host/join initialization failures now follow the same-process restart path: partial network state is cleared, the remembered NetworkHost selector or a fresh NetDlg is reconstructed, generic status stays empty, and a localized regular OK/Error `Error Log` modal presents the fatal string. Remaining: the ringbuffer-only fallback still needs the classic 10-line expandable `InfoDialog`, and other startup-fatal producers have not all been routed through this presentation. |

## Scenario selection subtree

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Local scenario book chrome/list/info/buttons | `C4StartupScenSelDlg`; `startup_scensel.rs::ScenselDialogFocus` | **Partial** | Exact focus chrome/order, basic keyboard/gamepad traversal, disabled-control skipping and pointer/touch hit bounds are covered. F5 rediscovers the current folder in place, F2 provides inline rename with group/title persistence, Delete confirms and removes ordinary or packed children, and Alt+M updates the shared/persisted mission-access modules at their exact override priority. Map-style folders remain outside this book projection. |
| Recursive `.c4s`/`.c4f`/directory discovery | `C4ScenarioListLoader` | **Partial** | Recursive discovery/order tests hold for the default sort, but compare_frontend_entries (main.rs:15370-15424) never consults Config.Startup.AlphabeticalSorting, so it always applies the folder-index and difficulty sort keys that C++ EntrySortFunc (cpp:823-853) skips once that option is set. Tracked as M09-P4-L064. |
| Recursive search results and deep-folder Back reconstruction | C++ current-folder filter: `C4StartupScenSelDlg::UpdateList`; requested Rust extension: `collect_frontend_search_matches`, `Folder::Start`, `FolderBack` | **Partial** | Rust intentionally implements the user-requested recursive search extension: it walks every descendant and reconstructs every intermediate folder layer so Back pops one level at a time. C++ filters only the immediate current folder, so this must remain documented as a deliberate product divergence rather than exact oracle parity. Keep deep-nesting and duplicate-path regressions. |
| Folder navigation and folder metadata | `Folder::Start`, `FolderBack` | **Partial** | Runtime reload/mutation parity. |
| Asynchronous scenario-loader progress state | `C4StartupScenSelDlg::UpdateList`, `C4ScenarioListLoader::GetProgressPercent` | **Complete** | Interactive discovery runs on a cancellable worker with recursive 0–100 progress. The selector forces the book loading view with the localized `Loading... (%d%%)` label, then atomically restores the folder, live search, and selection when the completed catalog arrives (`scensel_f5_rediscovers_current_folder_and_applies_live_search`). |
| Folder-map view (`FolderMap.txt`) | `C4StartupScenSelDlg.cpp:47-404,1016-1023`, `C4MapFolderData`; shipped `content/Western.c4f/FolderMap.txt` | **Partial** | Direct `.c4f` folders honor default-on `ShowFolderMaps`, case-insensitive packed/logical group traversal, localized map data, resolution/image fallback, aspect/fullscreen backgrounds, access overlays, scenario facets/titles, map-side info and classic select/second-click/single-click activation. Invalid maps and disabled maps fall back to the ordinary book. Developer `ImageDump` output and pixel-exact dynamic font selection remain intentionally unimplemented. |
| Search edit/filter | `OnSearchBarEnter`, `UpdateList`, `KeySearch`, `C4GUI::Edit` | **Partial** | Submit-only markup-stripped filtering, Ctrl+F select-all, caret/selection, word edits, clipboard shortcuts, mouse capture/double-click, horizontal scroll, blink, and render tests work. The exact state-dependent Cut/Copy/Paste/Clear/Select-all classic popup, right-down/Apps-key triggers, retained logical focus with suppressed focus drawing, clipboard mutation rules, and activation-release capture are covered. Remaining: non-Windows middle-click primary selection and the exact zoomed `¦` caret glyph. |
| Description `TextWindow` scrolling | `C4GUI::TextWindow`, `ScrollWindow` | **Complete** | Wheel, clipping, fixed pin, track jump-and-drag with capture, and held arrows match the C++ geometry/conversions. |
| Scenario list scrolling | `C4GUI::ListBox` | **Complete** | Selection-follow viewport, wheel, clipping, fixed pin, scrolled clicks, captured track drag, held arrows, end-stopping Up/Down, and fully-visible-row PageUp/PageDown/Home/End are covered. |
| Choose Definitions checkbox and definition selector | `StartScenario`, `C4DefinitionSelDlg`; `lc-app/src/main.rs::PendingDefinitionSelection` | **Complete** | Selection/reset rules, LocalOnly, recursive focus/input, raw-order `*.c4d`, fixed/optional checks, exact dialog/list/preview/buttons, scrolling/title drag, F5 rebuild, nested error, modal/capture cleanup, cancel retention, ordered output and rooted loading are covered. Local versus `NetworkHost` mode survives refresh, cancel, error and accept. |
| Explicit/folder-local definition order and packed parent resources | `C4Game::OpenScenario`, `FoldersWithLocalsDefs`, `InitDefs`, `InitMaterialTexture`; `C4GroupSet::RegisterParentFolders`; `C4GraphicsResource::RegisterMainGroups` | **Complete** | The startup handoff resolves the explicit global pack before folder-local packs, lets the later folder-local definition overload an ID collision, retains definition-root graphics, and traverses nested packed `.c4f` parents inner-first for graphics/materials and overload flags. The global-vs-folder-local collision and packed-parent resource regressions run in the default gate. |
| Scenario rename | `C4GUI::RenameEdit`, `ScenListItem::KeyRename`, `Entry::RenameTo`; `lc-frontend::rename_edit` | **Complete** | The reusable inline controller hides the replaced label, preloads/selects its text, applies the exact C++ modifier masks, owns Escape and the enabled primary gamepad's raw AnyHigh abort, submits on Enter or focus loss, reselects invalid input, and restores the saved focus for generic finish/abort. Raw AnyLow follows the dialog path (abort rename, then OK), while only gamepad Left/Right attempt focus transfer. F2 wires it to scenario/folder persistence: valid commits regenerate the type extension, move ordinary or nested packed entries, replace `Title.txt`, rediscover/resort and reselect; that callback uses C++'s `RR_Deleted` ownership and explicitly focuses the rebuilt list. Empty/same-title input is non-mutating; collisions and storage failures remain modal and nonfatal. |
| Scenario delete | `KeyDelete`, `DeleteConfirm` | **Complete** | Unmodified Delete outranks Search when a row is selected, shows the type/title Yes/No confirmation and original-group warning, removes ordinary or nested packed entries, selects the next visible row, and reports storage failure through the nonfatal Delete error modal. A focused inline rename edit retains its higher-priority text Delete. |
| Mission access/password | `KeyCheat`, `KeyCheat2` | **Partial** | Exact Alt+M priority opens the Options-icon input before the NetworkHost Comment mnemonic. Semicolon modules are added/removed case-insensitively in the process-shared store, persisted to `General.MissionAccess`, and immediately reflected by a list/selection rebuild. Missing access already blocks scenario starts and controls FolderMap access content; disabled book-row presentation remains tracked separately. |
| Start validation/warnings | `Scenario::CanOpen`, `DoOK` | **Partial** | Network replay rejection and the too-few-player OK/Abort warning are now implemented and tested at Open-click (network_scenario_open_decision, main.rs:55096-55174; M09-P2-L008). Remaining: the Network-mode list-row disabled color still reflects only MissionAccess — scenario_selector_open_error short-circuits before CanOpen's replay/max-player checks for NetworkHost (main.rs:54974-55000) — so replay and over-max-player rows render enabled instead of the 50%-alpha disabled color. |
| Local Fair Crew/Record option buttons | `C4GameOptionButtons`; `lc-frontend/src/game_option_buttons.rs` | **Partial** | Exact strip geometry/resources, tooltips, focus, keyboard/pointer/touch/gamepad input, normal config persistence and `ForcedNoCrew` constraints are app-wired. Persistence failures retain diagnostics and fail at the centralized startup-status boundary before presentation; the classic error path remains unported. |
| Network scenario book | `C4StartupScenSelDlg(true)`; `ScenarioSelectorMode::NetworkHost` | **Partial** | CanOpen validation (replay rejection, too-few/too-many player checks) is now wired and tested via network_scenario_open_decision at Open-click (M09-P2-L008). Remaining: Network-mode row disabled color still reflects only MissionAccess, not replay/max-player CanOpen failures — see Start validation/warnings row. |
| Network selector option buttons | `C4GameOptionButtons`; `lc-frontend/src/game_option_buttons.rs` | **Partial** | Internet, League, Password, Comment, Fair Crew and Record use classic layouts/states/tooltips and keyboard, pointer, touch and gamepad routing. Boundary pass-through, per-gesture capture and resize cancellation are tested; normal values load/persist through config. Alt+M opens the higher-priority selector Mission Access input instead of Comment, and an open context menu suppresses the underlying option strip. This row remains scoped to the selector, not the lobby. |
| Password/Comment `InputDialog` from network selector | `C4GameOptionButtons::OnBtnPassword`, `OnBtnComment`; `lc-frontend/src/input_dialog.rs` | **Partial** | Both selector callers use the resource-validating classic modal with edit/caret/selection/context actions, max length and all input modes. Strict underlying-screen exclusion, Apps-key context ownership, per-button gesture capture, close-surviving release consumption and resize cancellation are tested. Persistence failures now take the typed startup-status boundary before pixels; their classic error modal and other `C4GUI::InputDialog` consumers remain Partial below. |

## Startup network browser and IRC subtree

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Network browser chrome | `C4StartupNetDlg` | **Partial** | Exact classic layout/geometry, Games/Chat tabular switching, Back (Backspace/Left)/F5/Enter key bindings, and the Internet/Record toggle buttons are implemented and app-wired; live network behavior is detailed in the child rows below. OnSec1Timer's `IsActive(true)` freeze while a modal covers the dialog is not reproduced. |
| Retained NetDlg Internet refresh | `C4StartupNetDlg::OnShown`, `UpdateMasterserver`; `NetDlgController::sync_masterserver_signup_from_config` | **Partial** | Returning from the host selector resyncs the Internet icon/config and masterserver row via `sync_masterserver_signup_from_config`. Live query/status transitions and spawned game-reference rows are now implemented (see Live game list/query entries below); OnSec1Timer's `IsActive(true)` freeze while a modal covers the dialog, and clonk.de's immediate double-retry on masterserver failure, are not reproduced. |
| Live game list/query entries | `C4StartupNetListEntry`, `UpdateList` | **Partial** | Masterserver/LAN/direct queries, references, status text, MOTD/redirect, 42s reference and 10s error timeouts, and 1s refresh throttling are implemented and tested (`l040_masterserver_row_projects_counts_motd_and_query_error_states`, `l027_subsecond_refresh_only_plays_error_and_preserves_rows`, `references_expire_at_cpp_deadline_and_updates_refresh_it`). Querying, result counts and masterserver errors are owned exclusively by the native query row; successful search events leave the generic startup status empty, so discovered-game frames remain renderable (`l146_network_search_results_render_only_in_native_rows`). Remaining: masterserver failures wait the full 30s interval instead of C++'s immediate up-to-2 retry, and OnSec1Timer's `IsActive(true)` freeze while a modal covers the dialog is not reproduced. |
| Reload and direct-address two-stage query/join | `C4StartupNetDlg::DoRefresh`, `DoOK` | **Partial** | Reload restarts discovery/masterserver search with C++'s exact 1s throttle (`GUISound("Error")`, rows preserved: `l027_subsecond_refresh_only_plays_error_and_preserves_rows`). Enter on a nonempty, focus-retaining, untrimmed address adds a direct-reference query row and refocuses the list; a second Enter/Join on that still-pending row joins the raw address directly, matching `GetJoinAddress` (`network_direct_address_enter_adds_query_row_and_focuses_list_without_joining`). |
| Join validation/redirect/discovery modals | `DoOK`, `DoRefresh` | **Partial** | Empty selection, bad references, version mismatch, no-runtime-join confirm, masterserver redirect accept/decline and discovery-start failure all open the exact classic OK/Error/YesNo dialogs with C++'s ordering and text, each pinned by a named test (`network_version_mismatch_modal_precedes_runtime_policy_and_never_starts_join`, `l040_masterserver_redirect_decline_latches_and_accept_persists`, `l027_discovery_failure_opens_abort_modal_without_leaving_network_dialog`). |
| Create Game transition | `C4StartupNetDlg::CreateGame`; `lc-app/src/main.rs::process_network_dialog_actions` | **Complete** | It opens `C4StartupScenSelDlg(true)`/`NetworkHost` before any host socket or `NetworkManager` exists. |
| Selected host scenario/definitions staging | `C4StartupScenSelDlg::StartScenario`; `StagedNetworkHostScenario` | **Partial** | Only network replay is refused pre-bind (matching C++); savegame rosters, scenario-embedded parameters/league config and script/restore players are now applied via prepared_host_bootstrap.rs (apply_embedded_game_parameters, enforce_league_rules, PreparedLeagueHostConfig), not rejected. Unpacked System resource publication follows Unix `stat` like C++: a dangling entry is retained as zero-sized/CRC-zero and cannot abort pre-bind host preparation (`cpp_dangling_symlink_is_zero_sized_in_unpacked_system_crc`). `Network.LocalName` and `Nick` now follow C4Config/C4ClientCore name sanitization, including empty-Nick fallback, and the sanitized native-byte identity is retained through prepared-host setup. State/order regressions use a deterministic minimal host fixture, while the scoped Tutorial01 and Regicide integration tests remain shipped-content bootstrap sentinels. Broader start validation remains. |
| Post-connect lobby boundary | `C4Game::NetworkJoin`, `C4GameLobby`; `GameApp::poll_startup_network_connection` | **Partial** | A staged ordinary host enters the exact lobby over the scenario loader background while retaining its manager. Live roster, team, resource, add-player and kick paths are wired; remaining unsupported children stay typed fail-fast. Every terminal post-worker host/join failure now tears down partial state, reconstructs the C++-remembered NetworkHost selector or fresh NetDlg, and presents its diagnostic in the native `Error Log` modal without a generic startup-status boundary (`retained_netdlg_refreshes_internet_and_staged_host_keeps_options_noninteractive`, `l084_join_progress_names_target_and_dismisses_on_resolution`). |
| IRC login sheet | `C4ChatControl` | **Complete** | Nick/Password/Real name/Channel use the centered classic geometry, localized labels, masked transient password, native focus/traversal and ordered C++ validation. Invalid nick/password/channel and initial connection failures now open localized error modals while preserving and focusing the form. `IRC.Server2`, `IRC.Nick` (falling back to `Network.Nick`), `IRC.RealName` and `IRC.Channel` seed it; Connect persists only the three native editable settings before the `Startup.HideMsgIRCDangerous`-gated public-server warning (`chat_login_uses_cpp_centered_geometry_localized_strings_and_typed_validation`, `standalone_irc_validation_disconnect_and_window_close_use_classic_modal_ownership`). |
| IRC server/channel/query tabs | `C4ChatDlg`, `C4ChatControl` | **Complete** | Live server/channel/query sheets preserve topics, semantic transcript colors, control-byte sanitization, wrapping, wheel scrolling and new-line bottom-follow. Inactive captions turn classic yellow until selected; privileged nicks sort rank-desc/name-asc, draw classic rank icons and open queries on double-click. Server close confirms only while active then hard-disconnects, channel close sends PART and waits for backend removal, and query close is local. Mouse-down tab selection/close, phase-34 close facets and sheet cycling are covered by `chat_snapshot_sanitizes_colors_unread_routes_and_sorts_like_cpp`, `chat_tab_close_cycle_and_channel_part_match_native_sheet_types` and standalone renderer regressions. |
| IRC command language and quit confirmation | `C4ChatControl::ProcessInput` | **Complete** | Ordinary messages, `/me`, `/quit`, `/part`, `/join`/`/j`, `/notice`, `/msg`, `/raw`, `/ns`, `/cs`, `/ms`, `/query`/`/q` and `/nick` map to the process-global backend with classic local errors. `/quit` is immediate (the confirmation belongs only to closing an active Server sheet). Shared 20-entry newest-first deduplicated history and CR/LF multiline paste retain the final tail and abort remaining lines after `/quit` or implicit-current `/part` (`chat_multiline_paste_submits_lines_retains_tail_and_honors_abort`, `chat_history_is_shared_order_bounded_deduplicated_and_cycles_to_empty`). |

## Startup player subtree

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Player list/activation/info/portrait | `C4StartupPlrSelDlg` | **Partial** | List scroll/typeahead/Home-End/PageUp-Down, keyboard/mouse/gamepad routing, checkbox activation, delete/properties/crew dispatch and portrait/selection-info rendering are faithful and tested. Widget feedback sounds (L061) and the Config.General.Participants length overflow guard/IDS_ERR_PLAYERSTOOLONG dialog (L063) remain unimplemented. |
| Player row Properties/Delete context | `PlayerListItem::ContextMenu` | **Complete** | Both entries route through the shared context-menu system (touch/mouse/keyboard/gamepad, focus suppression, outside-down pass-through, exact popup shape) into real handlers: Delete opens the classic Yes/No warning, and Properties now opens the live C4StartupPlrPropertiesDlg port, not a stub action boundary. |
| New Player / Properties form | `C4StartupPlrPropertiesDlg` | **Partial** | Name, standard/RGB color cycling, 8 control sets, mouse toggle, Jump'n'Run/Classic movement (mirrors AutoContextMenu), tooltips, empty/duplicate-name validation, and the real nested portrait selector are implemented and tested; both name failures now open the classic blank-caption error modal and retain the form. Remaining: post-validation file-save failures still use inline/status presentation rather than C++'s separate ShowErrorMessage path. |
| Portrait selector | `C4PortraitSelDlg` | **Complete** | Picture opens a modal location combo seeded with user/program paths, a direct-child C4CFN_ImageFiles thumbnail grid with throttled progressive decoding, a working leading No Portrait tile, independent Player image/Lobby-Icon options, and OK/Cancel commit semantics. |
| Crew mode and crew detail list | `SetCrewMode`, crew list classes | **Complete** | Enter/leave crew mode, direct-child discovery with C++'s descending type/experience sort, live participation checkboxes, portraits, stats/physical bars and promotion text, and the exact no-crew message dialog are implemented and tested end-to-end against real player/crew groups. |
| Crew Rename/Delete/Death Message context | crew item callbacks | **Complete** | Delete (exact warning/removal) and Set-death-message (matches C++'s own modal C4GUI::InputDialog, same label/caption/icon strings) are faithful and tested, and Rename now uses the shared inline RenameEdit over the row label (pre-fill/select-all, Enter/focus-loss commit, Escape abort, invalid-name reselect) instead of a modal dialog. |
| Player/crew delete and validation dialogs | `OnDelBtn`, property close paths | **Complete** | Player and crew deletion share the exact warning/failure-dialog machinery and are tested against real packed and directory groups. Properties OK now distinguishes empty and duplicate names with the classic blank-caption error modal, preserves every entered field, and leaves the properties form open underneath after dismissal. |

## Startup options subtree

All six sheets (Program, Graphics, Sound, Keyboard, Gamepad, Network) now
render and route real behavior; no sheet returns an entry boundary anymore.
Shared Back/save failures now use the exact localized classic configuration
resources; remaining gaps are noted per row.

| Sheet or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Options chrome/tabs | `C4StartupOptionsDlg` | **Partial** | All six sheets (Program, Graphics, Sound, Keyboard, Gamepad, Network) are reachable by tab click, K_LEFT/K_BACK and Ctrl(+Shift)+Tab without a boundary (options_ctrl_tab_traverses_all_six_live_sheets_without_a_boundary). Every sheet now renders and routes real behavior instead of a typed boundary; see each sheet's own row. Remaining chrome gaps are common-dialog traversal/tooltip edge cases. |
| Program | program sheet controls | **Partial** | Language combo (real catalog, fallback recompose, dialog recreate), font face/size combos (rebuild+recreate, init-failure error dialog), white-chat ingame/lobby, timestamps, preloading, fair-crew slider and Reset/Advanced buttons are all wired and persisted (options_program_round_trips_bound_values_and_raw_fair_crew_strength, options_language_loads_real_de_and_selection_reloads_and_persists, options_font_size_rebuilds_all_startup_font_sets_and_recreates). Remaining gaps are tooltip completeness beyond the audited set. |
| Graphics | graphics sheet controls | **Complete** | Display-mode combo recreates real fullscreen/window state; scale spin/slider/Apply drive the 12s timed Yes/No confirm with revert-on-timeout (options_scale_test_times_out_reverts_and_yes_commits); all five option toggles plus fire particles and the smoke slider are bound and tooltip-covered. Shared Back/save failures use the exact classic configuration resources. |
| Sound | sound sheet controls | **Partial** | The classic Frontend/Game groups, four checkboxes and both volume sliders render and route exact pointer, touch, keyboard and gamepad behavior, including drag/release quirks, held-arrow frame ticks and ordered sounds/callbacks. Live `FEMusic`/`FESamples`/`RXMusic`/`RXSound`, active and pending music volume, sound test volume, bare-F3 synchronization, Ctrl+F3/modal stale-checkbox behavior and preserving six-key close-save are covered recursively. Missing audio fails typed before pixels. Remaining gaps are localized labels/tooltips. |
| Keyboard | `ControlConfigArea` | **Partial** | Control-set selector, all twelve key buttons, Reset (resets every set, matching Config.Controls.ResetKeys) and the KeySelDialog-equivalent capture modal are wired with tooltip coverage (draw_control_sheet; options_key_capture_matches_classic_modal_and_production_input_routing). Remaining: numeric 1-0 hotkeys to jump directly to a control set (C4GUI Dialog::KeyHotkey) are unimplemented (ticketed below). |
| Key capture modal | `KeySelDialog` | **Partial** | open_options_control_capture mirrors KeySelDialog's per-device message/caption/icon, rejects modified chords, captures bare Escape ahead of the dialog-cancel binding, and persists to Config (Kbd3Key6); gamepad capture filters by slot/cluster and rejects the wrong pad (options_key_capture_matches_classic_modal_and_production_input_routing). Remaining: render evidence at audited resolutions is unverified. |
| Gamepad | `ControlConfigArea` | **Complete** | Device/control-set selector (bounded to connected pads), a scoped selected-device open/live claim with close-old/open-new and sheet/dialog release, twelve bindings, GUI-control checkbox (visible only on set 0, matches pGUICtrl->SetVisibility) and Reset are wired and tooltip-covered (l097_options_gamepad_device_claim_switches_and_releases; gamepad_gui_checkbox_exists_only_on_first_pad; draw_control_sheet). |
| Network | network sheet controls | **Partial** | TCP/UDP/reference/discovery enable+spin controls, alternate-server checkbox/edit with the don't-show-again warning gate (persisted HideMsgNoOfficialLeague), automatic-update, UPnP, name/nick edits (with Nick==LocalName clearing) and TCP/Ref+UDP/Disc port-collision Back-validation are wired with the exact `IDS_ERR_CONFIG` caption and `IDS_NET_ERR_PORT_*` messages (draw_network_sheet; options_network_back_validates_both_port_pairs_and_alternate_notice_gate). Config-write failures surface the formatted `IDS_ERR_CONFSAVE` message before returning to Main. Remaining: tooltip completeness beyond the audited set. |
| Reset confirmation/quit | `OnResetConfigBtn` | **Partial** | OptionsDlgAction::ResetConfiguration opens the exact C++ Yes/No modal (caption/message/focused button); Yes clears reset-protected keys (VendorResetKey/FontSize/Scale) and requests app exit, No leaves the config file untouched (options_reset_confirmation_replaces_config_and_requests_clean_exit). Remaining: exit is a request flag consumed by the app shell rather than a direct Application.Quit(); render evidence at audited resolutions unverified. |
| Advanced warning/config editor | `C4StartupOptionsAdvancedConfigDialog` | **Partial** | show_options_advanced_warning reproduces the OK/Abort IDS_MSG_ADVANCED_SETTINGS_WARNING gate; AdvancedConfigController builds dynamic section tabs from a typed field/order/default/blocked-key table mirroring C4Config::CompileFunc and round-trips Save/Cancel preserving unknown keys/comments (l048_advanced_options_click_save_and_cancel_round_trip_typed_config). Remaining: not every C4Config field/section has been cross-checked against the typed table. |
| Timed scale confirmation | `ResChangeConfirmDlg` | **Partial** | The Apply-triggered scale test opens a 12-second Yes/No dialog whose message updates each second, reverts the display on timeout/No and persists on Yes (options_scale_test_times_out_reverts_and_yes_commits). Remaining: render evidence at audited resolutions unverified. |
| Validation/restart/font errors | options callbacks | **Complete** | Network Back validates TCP/Ref and UDP/Disc collisions with the exact localized `IDS_ERR_CONFIG` caption and `IDS_NET_ERR_PORT_*` message before saving. Config I/O failures show the formatted `IDS_ERR_CONFSAVE` feedback and still return to Main. Font-face/size failure shows 'Error initializing fonts' and leaves prior fonts/config intact, including across a restart and a blocked config write; Reset shows the restart-required warning text. |

## About/update subtree

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Credits page/chrome | `C4StartupAboutDlg` | **Complete** | Developer TextWindows have independent scroll, clipping, wheel routing, fixed-pin arrow repeat, captured drag and sounds. Tab/Shift+Tab traversal and tooltips are covered. Back/Update/Licenses now use the shared `ClassicGuiSkin`: `GUIButtonDown.png` replaces the full pressed plank, while active focus/hover draws the additive inset highlight before the caption (`l073_about_buttons_render_released_pressed_and_highlight_transitions`). |
| Licenses list/text page | `LicenseWindow` | **Partial** | The real second page uses the C++ one-fifth ListBox/rest TextWindow layout, unconditional `COPYING`/`TRADEMARK` data, selected/inactive rows, 1px list spacing, title/body wrapping, keyboard selection, separate list/text wheel offsets, fixed-pin arrow repeat and captured track/thumb dragging. Page entry no longer reaches a fallback boundary. Optional dependency licenses generated by an external `deps/licenses.cmake` remain. |
| Update controller / button hand-off | `C4UpdateDlg` | **Dropped** | The in-process lookup/download/apply controller is launcher/package-manager-owned. Activating Check Updates by mouse or focused keyboard now opens a classic update-icon informational dialog, dismisses back to the retained About screen and never raises a fatal parity boundary. |

## Network start/lobby subtree

The app deliberately rejects its old generic lobby after host/join connection.
`lc-frontend/src/game_lobby.rs` is a resource-validating implementation of the
classic lobby. `lc-app` owns it for a staged ordinary host — now including
live roster, chat (`CID_Message`), ready state, the ready-check modal and the
host countdown/start path — and the join flow reaches the password challenge;
remaining unsupported recursive children (player-row contexts, Options and
Scenario sheets) still fail at typed boundaries rather than falling
through to a generic pane.

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Initial network start wait | `C4Network2Dialogs.cpp:114-173,552-586`, `C4Network2StartWaitDlg` | **Complete** | Host-side C4Network2StartWaitDlg is app-wired: real client roster/ready-ack tracking, per-row Kick buttons with direct-remove/league-vote dispatch, Restart/Cancel/Close, sounds, scroll, and keyboard/gamepad focus (network_start_wait.rs; begin_network_start_wait/update_network_start_wait_ack; tests l104_network_start_wait_kick_click_reuses_direct_and_league_paths, network_start_wait_tracks_only_matching_accepted_status_acknowledgements, selected_network_scenario_installs_prepared_host_before_admission). A reached non-host client gets the classic small, unfocused, cancel-only Ico_NetWait MessageDialog, and both host/client waits close on the Go acknowledgement. |
| Password challenge and connection progress | `C4Network2Dialogs`, network join callbacks | **Partial** | Password retry/cancel on a WrongPassword reference use the shared classic InputDialog. Raw-connect joins now show the abortable Ico_NetWait progress MessageDialog and Cancel/Escape return to the retained NetDlg; later wait/error dialogs and immediate transport-level interruption remain. |
| Host/join app entry | `C4GameLobby::MainDlg`; `GameApp::poll_startup_network_connection` | **Partial** | The staged ordinary host constructs the persistent classic lobby, including live roster and Resources sheets; join and unstaged host remain typed fail-fast and tear down the new manager. Prepared-host TCP and reliable UDP now bind independently like `C4Network2IO`: either transport can survive the other's bind failure, disabled port 0 stays disabled, league/UPnP/local references advertise only live protocols in TCP-then-UDP order, and startup fails before publication only when neither transport is available (`l075_prepared_host_survives_occupied_tcp_with_configured_udp`, `l075_prepared_host_fails_only_when_both_transports_are_unavailable`). Unsupported host children still fail at typed boundaries instead of falling through to the generic lobby. |
| Legacy generic `NetworkLobbyState` pane | no C++ visual authority | **Fail-fast** | State may still be constructed internally, but the typed, logged `StartupScreen` preflight rejects `StartupView::NetworkLobby` before cache lookup even with empty status and a matching stale cache. Remove the dormant model when the classic lobby owns the route. |
| Exact lobby frontend resource boundary | `C4GameLobby.cpp:141-314`; `lc-frontend/src/game_lobby.rs::LobbyResources` | **Partial** | The staged host preflights every required classic image sheet/font, including `GUIContext.png` and shadowless `TooltipFont`/BookFont, before binding a socket; missing or invalid resources are typed refusals. Players/Teams and Resources are app-wired; Options/Scenario contents remain. |
| Scenario/team/parameter metadata projection | `C4Scenario`, `C4TeamList`, `C4GameParameters`, `StdCompilerINIRead`; `lc-engine/src/scenario.rs` | **Partial** | Exact recursive INI hierarchy, case/duplicate/default/string semantics, compound-array recovery, teams, clients, savegame/replay rules and definition-resolution boundaries are projected and tested, including shipped content. Runtime-selected loader resources, current roster/script-player naming, config/network/league adjustments and old-save effective definition resources still require app ownership. |
| `PID_Status` / `PID_StatusAck` barrier foundation | `C4Network2Status`, `C4Network2::ChangeGameStatus`; `StatusBarrier`, `HostHandle`, `ClientHandle`, `NetworkManager` | **Complete** | Exact signed payloads, host barrier transitions, stale/duplicate rejection, client acknowledgement and real-TCP tests exist. The classic host lobby now initiates the Lobby->Go transition itself: `start_network_game_now` (reached from Start / countdown-zero) calls `NetworkManager::change_status`, gated to the host with no generic fallback. |
| `PID_LobbyCountdown` transport foundation | `C4PacketCountdown`, `Countdown`; `LobbyCountdown`, host/client session APIs | **Complete** | Exact codec, abort value, cadence predicate, host-only broadcast and real-TCP round trips are tested. The classic host lobby now owns the timer: `tick_network_lobby_countdown`/`HostLobbyCountdown::advance` mirror `Countdown::OnSec1Timer`'s sparse-broadcast cadence exactly, and `apply_countdown_packet` applies every transition to chat log, sounds (Elevator/Pshshsh/Fuse/Blast3/Command) and Start/Ready UI state. |
| `PID_ReadyCheck` / lobby-ready foundation | `C4PacketReadyCheck`; `ReadyCheck`, `HostHandle::set_lobby_ready`, `ClientHandle::set_lobby_ready`, `NetworkManager::set_local_ready` | **Complete** | Request/reply codec, role-correct host/client APIs, origin validation and real-TCP tests exist. The app lobby now binds them: the checkbox submits `set_local_ready` via `apply_classic_lobby_ready_change`, the roster reflects Ready/Client status, and `request_lobby_ready_check_at`/`handle_lobby_ready_check_request` implement the host cooldown, forced non-host reset and the client's Yes/No ready-check modal. |
| Main transparent chrome and responsive layout | `C4GameLobby::MainDlg::MainDlg`; `game_lobby.rs` | **Partial** | The bounded scale-1 host route owns lifecycle/teardown, loader-background composition, layout, fonts, focus and selection/scroll/option traversal. Right-sheet state, shared context menus, the player selector, chat (CID_Message), ready-check and Start/Cancel countdown now layer over it; Options/Scenario sheet contents and the player-row context menu remain typed refusals. |
| Chat log, scrolling and input edit | `C4GameLobby.cpp:272-280,475-766,1018-1056` | **Partial** | Paste (CR/LF line-split submit), Up/Down history, and lobby-local /joinplr, /plrclr, /start, /abort, /readycheck, /help, /kick, /observer, /mute, /unmute, /clear are now app-wired (`process_classic_lobby_chat_request`, `process_classic_lobby_command`), sending ordinary/team/me/sound/alert chat via CID_Message. `/set comment`, `/set password`, `/set faircrew`, and `/set maxplayer` apply the native host/league gates and synchronized option side effects. |
| Lobby chat `C4ControlMessage` / `CID_Message` | `C4ControlMessage`, `CID_Message` (`C4Control.cpp:1071-1260`); `lc-engine/src/control.rs::MessageControlData`; `lc-app/src/main.rs::{parse_lobby_message_control, process_classic_lobby_chat_request}` | **Partial** | The exact wire codec is ported (`MessageControlData`, byte-for-byte matching C4Control.cpp) and lobby Submit sends NORMAL/ME/TEAM/SOUND/ALERT via `network.submit_message`; incoming messages update the log/roster with team-visibility and sender-vs-by_client checks. Private messages correctly stay running-game-only, matching C++'s `Game.IsRunning` gate, and the lobby `/set` command surface is app-wired. |
| Chat input context popup | `C4GUI::Edit::OnContext`; `LobbyChatRequest::OpenContextMenu` | **Complete** | Component emits typed `OpenContextMenu`/`ContextCommand` requests; the app opens the shared classic context-menu chassis and dispatches Cut/Copy/Paste/Clear/Select-all against the live edit view (`process_classic_lobby_chat_request`, `lobby_chat_context_entries`), matching `C4GUI::Edit::OnContext`'s predicate set and order. |
| Direct lobby Exit | `MainDlg::OnExitBtn`, `OnClosed` | **Complete** | Exit/Escape closes directly with no invented confirmation, drops countdown/lobby/loader/network state and queued synchronization resources, returns to startup Main and restores menu music. |
| Ready checkbox/loading lock | `MainDlg::OnReadyCheck`, `UpdatePreloadingGUIState`, `RequestReadyCheck` | **Complete** | `apply_classic_lobby_ready_change` submits `set_local_ready`, updates authoritative client/roster state and triggers host auto-start checks; `request_lobby_ready_check_at`/`handle_lobby_ready_check_request` implement the forced non-host reset, the host's 2s+cooldown-config rate limit and the client Yes/No modal; `sync_classic_lobby_resource_ready` gates the checkbox on live resource completion, matching `UpdatePreloadingGUIState`. |
| Host Start/Cancel and synchronized countdown | `MainDlg::OnRunBtn`, `Start`, `OnCountdownPacket`; `Countdown` | **Complete** | `start_network_lobby_countdown_with`/`tick_network_lobby_countdown` broadcast the exact sparse cadence (mirrors `Countdown::OnSec1Timer`), apply `LobbyCountdownPacket` to UI/sounds, validate the league minimum-5s and unassociated-savegame-player warning (`Start`), and reach `NetworkManager::change_status` at zero; `/start`/`/abort` chat commands and the Cancel button share the same path. |
| Bottom lobby game-option strip | `C4GameOptionButtons`; `game_option_buttons.rs` | **Complete** | Exact Host/Client contexts, focus, sounds and countdown/forced locks are app-wired. Internet queues live masterserver Start/End without blocking the lobby; Start owns the native-style Abort wait, cancellation/failure rollback, full League/RandomSeed/MaxPlayers/StreamAddress application, and per-family puncher reinitialization. Password set/clear updates host admission and the advertised reference transactionally; comment persists, invalidates, logs and plays Connect; Fair Crew emits `CID_Set(C4CVT_FairCrew)` and updates on its synchronized echo; Record remains local/config-backed. League is intentionally display-only/disabled in the C++ lobby. |
| Password and Comment lobby input dialogs | `C4Network2Dialogs.cpp:718-776` | **Complete** | The host opens the exact shared InputDialog; an existing password clears in one click, otherwise LastPassword is preset. Validated password/comment submissions commit only after their live admission/reference side effects succeed, retain LastPassword, invalidate advertisement, log comment changes and preserve Connect-sound ordering; rejected text leaves the prior lobby state intact. |
| Right tab icon buttons | `C4GameLobby.cpp:255-264,922-998` | **Partial** | Players/Teams share the live roster, Resources has its exact list and Options owns a reachable scrollable core-options list, all with retained sheet state, caption and highlight. The phase-15 extended IRC icon is now included exactly when the process-global client was active at lobby construction and opens/raises the standalone dialog for host and client lobbies (`generic_client_lobby_external_irc_button_is_retained_and_emits_typed_action`). Scenario alone remains the typed child boundary. |
| Right-caption tab context popup | `MainDlg::OnRightTabContext` (`C4GameLobby.cpp:844-866`) | **Complete** | The flat Players, optional Teams, Resources and Options popup uses the shared classic context-menu chassis; Scenario remains correctly available only as a direct tab button. |
| Players/clients roster layout and scrolling | `C4PlayerInfoListBox.cpp:39-488,1237-1607`; `game_lobby.rs` | **Partial** | Authoritative client and PlayerInfo controls now rebuild host/client roster projections while preserving selection, focus and scrolling. Several status/ping/sound actions remain. |
| Player row context root | `C4PlayerInfoListBox.cpp:490-533` | **Complete** | The lobby always opens the root, including empty unauthorized roots, with exact conditional Take Over, Remove and New Color ordering, permissions, localized tooltips, icons and accelerators; the Take Over child behavior is documented below. |
| Nested Take Over submenu | `C4PlayerInfoListBox.cpp:535-572` | **Partial** | The host classic lobby enumerates the local client's PlayerInfo packet in order, excludes joined/join-issued or already-associated entries, presents exact `Using %s` children, and submits a fresh full-packet association without optimistic mutation. Remaining: children snapshot at root-open rather than C++'s submenu-open callback, and real client sessions still use the generic lobby ancestor without this recursive context. |
| Player Remove control | `C4PlayerInfoListBox.cpp:574-600` | **Complete** | Revalidates host/local ownership, aborts an active host countdown first, swap-removes the player from the cloned full owner packet, submits it without confirmation and clears remembered league authentication. |
| Player New Color action | `C4PlayerInfoListBox.cpp:602-616` | **Complete** | Revalidates host/local ownership and submits the cloned full owner packet with only the selected player's current color reset to its original color. |
| Player team combo/dropdown | `C4PlayerInfoListBox.cpp:618-646` | **Complete** | The classic popup applies permission/capacity filters, submits a full PlayerInfo update with only Team changed, waits for the authoritative echo, then converges every roster projection. |
| Restart player-team restoration | `C4PlayerInfoListBox.cpp:170-181`; `C4Game.cpp:2390-2397` | **Complete** | A same-scenario Restart preserves the full-game PlayerInfo name/team snapshot and raw `SetRestoreInfos` mask into the next host lobby. Newly constructed, exact-name-matched User rows submit a cloned full owner packet when their recorded team differs, while the mask/type/equal-team gates remain inert; actual Next Mission clears the snapshot like C++. |
| Client rows/status/ping/sound/add button | `C4PlayerInfoListBox.cpp:737-916` | **Partial** | Live synchronized rows, Add Player, the transient muted/unmuted sound icon, message-route-first/data-route-fallback ping, and exact chunk-weighted per-client resource progress are refreshed on the lobby's one-second callback. Local/disconnected rows suppress telemetry; connected remote rows retain native `(100%) name` completion text. Remaining: Rust exposes the selected route's cached RTT rather than C++ `getLag()`'s growing elapsed value while a ping is unanswered. |
| Client context Mute/Kick/Activate/Info | `C4PlayerInfoListBox.cpp:918-979` | **Complete** | Real host and joined-client rosters expose the exact ordered, iconless actions: remote Mute/Unmute, host-only remote Kick and Activate/Deactivate, then Info. Mute is local-only, activation submits `CLIENT_UPDATE_ACTIVATE` and waits for the authoritative echo, Kick retains its established path, and Info opens the modal per-client detail panel. |
| Add Player selector | `C4PlayerInfoListBox.cpp:981-985`, `MainDlg::OnClientAddPlayer` | **Complete** | The single-selection C4PlayerSelDlg specialization refreshes player files, preserves physical versus executable-relative names, aborts a host countdown, publishes the resource and waits for authoritative PlayerInfo before adding the roster row. |
| Team header rows and move-local-players action | `C4PlayerInfoListBox.cpp:989-1110` | **Complete** | Normal and random-team headers use semantic row identities, the team icon, localized name/tooltip and classic spacing. A physical header double-click rechecks countdown, distribution, host and capacity gates, then submits one cloned full local packet with every differing User player moved while Script players remain unchanged; presentation waits for the authoritative echo. |
| Free-savegame player group | `C4PlayerInfoListBox.cpp:1112-1143` | **Partial** | The host path has the localized group presentation, restore-player projection, Take Over root, filtered nested chooser and full-packet association request; same-scenario restoration is covered by the completed row above. Remaining: the real client-lobby ancestor does not expose this classic group/context, and submenu refresh timing is tracked in the nested row. |
| Script-player group and Add action | `C4PlayerInfoListBox.cpp:1146-1210` | **Complete** | The live projection gates the Add row by `MaxScriptPlayers`, selects the first unused configured script name, falls back through `SafeRandom` to that same list when every name is active, and submits the host PlayerInfo creation request with three fresh `SafeRandom(302)` color-channel draws. |
| Replay-player group | `C4PlayerInfoListBox.cpp:1212-1235` | **Partial** | Component presentation exists; replay-specific state/update ownership is absent. |
| Teams-filtered sidebar mode | `MainDlg::OnTabTeams`, `C4PlayerInfoListBox::PILBM_LobbyTeamSort` | **Complete** | The shared roster switches to team-registry/member order, filters invisible or deactivated-client players, retains configured empty-team headers and skips unused generated teams. Random-invisible assignment uses one lazy localized Random team header in active-client/packet order; switching preserves roster focus and any surviving semantic selection. |
| Resources list/progress | `C4Network2ResDlg.cpp:32-87,158-216` | **Complete** | Resource IDs reconcile in order with basename/icon rows, integer chunk progress and hidden-at-100 labels. Per-chunk backend telemetry updates only the active sheet's visible snapshot; reopening forces the retained latest state. |
| Resource Save/overwrite/success/error branch | `C4Network2ResDlg.cpp:88-157` | **Complete** | Resource rows expose the exact conditional phase-13 Save button from explicit local/remote completion provenance and the native Scenario/Definitions/AllowPlayerSave type gate. Client and host actions re-resolve the resource ID, preserve the raw WorkPath guard and `.c4p` PlayerPath routing, then use the shared MessageDialog stack for overwrite decline/accept and localized success/error outcomes. |
| Blocking network resource retrieval | `C4Network2::RetrieveRes` (`C4Network2.cpp:1852-1937`) | **Complete** | A reusable classic `ProgressDialog` presents `GUIProgress` percentage, NetWait icon and localized Abort while client startup resolves scenario, dynamic and ordered game resources or a runtime `JoinPlayer` waits for its player file. One active-resource clock resets on every changed percentage, emits the native fatal timeout after 100 seconds of unchanged progress and releases the appropriate startup/player caller; completion and transfer failure dismiss the wait (`progress_dialog.rs`; `BlockingResourceWait`; `unknown_loadable_resource_join_stalls_until_resource_completion`). |
| Preload button, automatic preload and failure log | `C4GameLobby.cpp:231-245,766-842,1001-1016` | **Complete** | Staged classic hosts, catalog/CLI hosts, and synchronized clients share the non-player resource-completion/one-shot eligibility gate. Manual mode reserves the native 32px Resources-sheet strip, reveals the button only while eligible, retains it after a setup/thread-launch rejection, and restores the list height after a successful launch; automatic mode fires once on the same edge. The owned, named background worker caches raw-load-key/effective-definition-matched game graphics plus material textures/render metadata, and catalog hosts or clients also hand their parsed scenario to startup; host Start or client GO waits for the worker. Client preload composes `Combined<client>.c4s` through an owned staging file, commits it only for matching JoinData, retires the dynamic resource asynchronously, and removes unconsumed owned files on lobby teardown. Setup/thread-launch rejection appends localized `IDS_ERR_PRELOADING` in red without an error sound; unmatched activation uses the normal load path. |
| Options list container | `C4GameOptions.cpp:28-310` | **Complete** | The Options tab opens a scrollable stacked-dropdown list, forces an authoritative refresh on activation and refreshes once per second only while active. The native three-pixel ListBox inset, list-level focus/selection, selected-child traversal, scroll-into-view, blank-click clearing and ComboBox/context-menu input are covered by frontend and app tests. |
| Options / Control mode | `C4GameOptionsList::OptionControlMode` | **Complete** | The first core row is present for host and client, always read-only in the non-runtime lobby and refreshed from the current network mode. Row order, visibility and editability are tested. |
| Options / Control rate | `OptionControlRate` | **Complete** | The row exposes exactly values 1-9, editable only by the host. A changed selection submits the relative `CID_Set` Type 0 adjustment, waits for its authoritative echo and refreshes both control clocks and the displayed value; role gates and no-op/value-change cases are tested. |
| Options / Runtime join | `OptionRuntimeJoin` | **Complete** | The host-only editable row exposes prohibited/allowed, persists the inverse `Network.NoRuntimeJoin` value and updates the prepared admission policy. Lobby exit applies the Go status and admission gate in one acknowledged host-loop command, preventing a late join from interleaving between them; visibility, persistence, atomic transition and worker-error propagation are tested. |
| Options / Team distribution | `OptionTeamDist` | **Missing** | Scenario-provided choices and synchronized selection. |
| Options / Team colors | `OptionTeamColors` | **Missing** | Enabled/disabled choice and synchronized update. |
| Options / Random team count | `OptionRandomTeamCount` | **Missing** | Conditional values, team recreation and dependent option refresh. |
| Scenario-description sheet | `C4GameLobby.cpp:64-137`, `ScenDesc` | **Complete** | Host/client Scenario tabs use the undecorated scrolling TextWindow geometry, refresh localized loading percentage only while active, load exact `Desc{}.rtf` through the language sequence as plain text (or title/load-error fallback), and stop after the terminal result. Covered by `l108_*` frontend/app tests. |
| Optional IRC/chat window button | `MainDlg::OnBtnChat`, `C4ChatDlg` | **Complete** | The conditional lobby icon, active-IRC viewport/HUD Chat button and exact Alt+C chord open or raise one standalone `C4ChatDlg`-style window without conflating it with synchronized game/lobby chat. It owns a distinct UI controller, uses the native centered 80% bounds, title drag, phase-34 close facet and Login/Chats default focus, while sharing the process-global IRC transport and 20-entry message history. Escape, the outer close icon or Alt+C destroys only UI-local state; the connection survives (`standalone_irc_entry_points_share_the_singleton_dialog_and_alt_c_toggles_it`, `active_irc_runtime_hud_chat_button_opens_ui_without_disconnecting_transport`). |
| Per-client information dialog | `C4Network2Dialogs.cpp:42-110`, `C4Network2ClientDlg` | **Partial** | The classic-shaped per-client info panel (activation/host/local role, id/label, addresses, connection protocol/peer/ping) is shared by the runtime F4 client list and the now-wired lobby client-row Info action. The lobby variant is modal over both host and joined-client rosters and closes without leaking input to the lobby. Unknown-client-ID text and host `(!ack)` are not reproduced. |

## In-game main-menu subtree

| Screen or recursive child | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Player/observer main page | `C4MainMenu::ActivateMain` | **Partial** | The Select-team entry now reads the live scenario `AllowTeamSwitch` flag and opens the synchronized mid-round switch page; Abort now follows the live fullscreen/window mode. Other condition inputs remain coarse/hardcoded (including remote-client presence). Every remaining unsupported child returns a distinct typed parity error through production input instead of reopening/closing the root, setting status text, or using `NoOp`. |
| Goals list | `ActivateGoals` | **Partial** | Rows use the definition description, fulfilled goals carry the scaled Captain overlay, and selection queues CID_ActivateGameGoalRule locally or networked while failure to resolve a live object reaches the distinct `ClassicIngameMenuChild::GoalInfo` typed boundary. Remaining: declared-image decode failures are silently lost by cache_definition_icons. Recursively, 101 of 110 shipped Goal/Rule definitions define `Activate`, reaching Info/Dialog/Normal menus, chooser trees and side effects. |
| Rules list | `ActivateRules` | **Partial** | Rows use the definition description, selection queues the shared CID_ActivateGameGoalRule dispatch, and failure to resolve a live object reaches the distinct `ClassicIngameMenuChild::RuleInfo` typed boundary. The recursive `Activate(player)` subtree remains; legitimately blank definition pictures reserve an empty symbol in C++, while malformed declared graphics must fail closed. |
| Hostility | `ActivateHostility` | **Complete** | Native-order visible-player rows, directional captions/tooltips, cached BigIcon or owner-colored Crew symbols, and the hostile overlay refresh on Tick35 in the permanent normal-grid page; toggles submit authenticated `CID_ToggleHostility` controls and honor `IsHostilityChangeAllowed()`. |
| Initial team selection/team switch | `C4Player::ActivateMenuTeamSelection` | **Partial** | Initial round-start selection (fFromMain=false) uses real team rows, sole-team auto-bypass and synchronized DoTeamSelection dispatch. The main-menu path (fFromMain=true) reuses those ordered rows, returns to Main on close, rechecks live `AllowTeamSwitch` on selection and dispatches authenticated `CID_SetPlayerTeam` locally or over the network. Remaining: TeamInfo.IconSpec never renders, team pages still use the context layout instead of the native normal grid, and long-open pages do not Tick35-refill membership captions/new-team availability. |
| Observer target/free view | `ActivateObserver` | **Complete** | The NO_OWNER page lists free view followed by every non-invisible runtime player in player-list order, preselects the current target, and uses one retained-viewport transition for keyboard/hover live preview and Enter. The viewport's durable ownerless identity survives temporary player targets. Covered by `observer_menu_lists_players_and_live_previews_selection`. |
| Runtime player join | `ActivateNewPlayer` | **Complete** | DirectoryIterator-order discovery skips in-use/directory groups and closes when full (`activate_new_player_lists_cpp_eligible_files_in_source_order_and_closes_when_full`). Network Join dispatch works for hosts and clients (`active_network_host/client_runtime_join_*`). Offline `CtrlJoinLocalNoNetwork` now applies resource-free AddPlayers/PlayerInfo and performs a locally controlled filename join; unreadable files fail through a typed menu boundary without status-text mutation (`offline_runtime_join_player_local_no_network`). |
| Ten save slots | `ActivateSavegame` | **Partial** | The configured `General.SaveGameFolder`, classic old-style numeric-parent/new-style trailing-digit `ScenName.c4f/ScenNameN.c4s` layout, and language-resolved title in Rust slot metadata are implemented and tested. Rust slot payloads remain JSON rather than native C4Group saves (including native folder `Title.txt` components); occupied probing is filesystem-based and the generic save browser still discovers only flat `.lcsave` files. |
| Options | `ActivateOptions` | **Partial** | Music now follows `ToggleOnOff(true)` (RXMusic plus current playback and localized timed flash), while Sound changes RXSound without incorrectly halting existing instances; both persist their classic `[Sound] Music` and `[Sound] Sound` keys and survive restart, and the menu reopens at its prior selection. Mouse control is scoped to the requesting player: a raw-true player sees the on phase, a raw-false player omits the row while any local raw flag is true, and otherwise every eligible player sees the off phase. The remaining localization/lifecycle details are incomplete. |
| Display | `ActivateDisplay` | **Partial** | Player/Clonk-name toggles now compose and draw the classic crew label, Clock/FPS feed the upper-board right-side rows, and fullscreen-only entries follow the live window mode. Exact Hide/Small/Mini upper-board geometry and durable toggle persistence remain incomplete. |
| Host disconnect/client kick | `ActivateHost` | **Complete** | The permanent client-ID-ordered page lists every client with the native Host/Client/ObserverClient icon and selectable host no-op; league clients with live players submit `VT_Kick`, while other remotes submit the `IDS_MSG_KICKBYMENU` removal and close the page. |
| Client disconnect | `ActivateClient` | **Complete** | The Yes/No page exists. Part is fully wired: ordinary Part records the first localized `NR_NetError` result before graceful teardown and the ChangeToLocal transition, including league observers without a local player; league Part with a local player submits an authenticated self-kick vote (`non_league_network_part_continues_the_running_round_locally`, `league_observer_part_uses_ordinary_network_clear_path`, `league_network_part_submits_authenticated_self_kick_vote`). |
| Surrender | `ActivateSurrender` | **Partial** | Offline and network Surrender both work: CID_SurrenderPlayer queues through the next control tick in both modes, no typed boundary (surrender_from_menu_ends_local_round, network_surrender_menu_queues_the_next_authenticated_control_tick). C++ never uses vote semantics for Surrender (only Part does); that claim was stale. Remaining: league-specific round-result/eval treatment of a surrendered player is unverified. |
| Abort/restart/no | `C4Game.cpp:3428`; `C4GameDialogs.cpp:33-128`, `C4AbortGameDialog` | **Fail-fast** | Bare unmodified runtime Escape and the main-menu Abort action reach the same logged typed boundary instead of opening the wrong C4MainMenu-shaped pane. The menu-shaped approximation is unreachable. Exact dialog halt state, restart policy, league vote subtree and cancel semantics remain unported. Default macOS SDL window-close still exits directly like the platform oracle. |
| Mouse hit testing/scrollbars/tooltips | `C4Menu` | **Partial** | Keyboard navigation works; pointer/scroll behavior remains incomplete. With player mouse control disabled, Rust can still route a left gesture into world mouse-down instead of keeping mouse control inactive. |
| In-game/script menu font and sheet boundary | `C4GUI::Resource`, `C4Menu::Draw`; `lc-app/src/ingame_menu.rs`, `lc-app/src/main.rs` | **Partial** | Render and script-menu pointer paths preflight the current font/sheet set, including the required Captain graphic; pointer failures are typed and cannot fall through into world input. Normal/Context item images can remain unresolved. |
| HUD resource boundary | `C4GraphicsResource`, player HUD; `lc-frontend/src/hud.rs` | **Missing** | The Rust HUD can synthesize fallback surfaces. Replace that reachability with a logged error until classic resources load. |

## Object-menu subtree

Construction, Activate, and Get requests now open real engine-owned
`C4ObjectMenu` instances rendered through the shared classic presentation, not
rejected at creation. `ClassicObjectMenuBoundary` retains only Activate/Get
variants and both are now unreachable dead code in `handle_menu_requests`; the
formerly described Inventory/Container/Build boundary modes no longer exist in
the enum. Buy/Sell/Contents/Info/Context were already engine-owned.

| Menu identification/style | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Construction (`C4MN_Construction`) | `C4Command::Construct`; `C4Object::ActivateMenu` | **Partial** | Now opens a real classic menu: player Knowledge-driven Structure rows (honoring script GetCustomComponents), per-row component costs, CXCN/WKS1 symbol, and mouse drag-out-of-menu site placement all work and are tested. CanConstruct still gates the request; no app-owned boundary or generic pane remains. Remaining gap is the shared C4Menu persistent scroll-offset, mouse-wheel, and title-bar-drag affordance. |
| Activate (`C4MN_Activate`) | `C4Command::Activate`; `C4Object::ActivateMenu`; `C4ObjectMenu` | **Partial** | Implicit Take and explicit Target2 both fully open the engine-owned C4MN_Activate menu: RejectContents gating, grouped/concatenated picture rows, primary and secondary get-all commands, ID-preserving selection across refill, and periodic/immediate refill are implemented and tested. No app-side fail-fast boundary is reachable. Remaining gap is the shared C4Menu scroll-offset/wheel/drag affordance. |
| Get (`C4MN_Get`) | `C4ObjectMenu` | **Partial** | C4MN_Get (and legacy Get Data=1) fully opens the engine-owned menu: RejectContents gating, the OCF_Carryable get-vs-activate branch, entrance-forced Get, grouped rows, primary/secondary commands, and periodic/immediate refill are implemented and tested. No app-side fail-fast boundary is reachable. Remaining gap is the shared C4Menu scroll-offset/wheel/drag affordance. |
| Buy (`C4MN_Buy`) | `C4ObjectMenu` | **Partial** | HomeBaseMaterial order, per-row GetValue-based pricing, and BASEFUNC_Buy gating are implemented and tested; the earlier alphabetical-order and flat-value bugs are fixed. No ordering/value/availability defect remains. Remaining gap is the shared C4Menu scroll-offset/wheel/drag affordance. |
| Sell (`C4MN_Sell`) | `C4ObjectMenu` | **Partial** | NoSell skip, GetValue-based pricing, Hostile(Base) eligibility, picture-concat grouping, and periodic/immediate refill are implemented and tested; the earlier flat-value/NoSell bugs are fixed. Remaining gap is the shared C4Menu scroll-offset/wheel/drag affordance. |
| Context (`C4MN_Context`) | `C4ObjectMenu` | **Partial** | AddContextFunctions is fully ported in C++ order (ActionContext, Fx*Context, AttachContext, Activate/ControlDigDouble, duplicate suppression, target Context*), plus Put/Contents grab-put/grab-get and friendly-remote rows, Buy/Sell, BuildInfo, Info, Exit, and the clonk submenu threshold — all tested. Free click-location alignment (menu always opens at the default corner, not the right-click point) and the shared scroll/wheel/drag affordance remain. |
| Info (`C4MN_Info`) | `C4ObjectMenu` | **Partial** | Classic target picture/title, one-shot target-relative placement, rich wrapped `InfoCaption`, and ordered static + `Fx*Info` text render in production. Shipped paths are covered; remaining mod-facing gaps are effect-list mutation during `Fx*Info`, italic/image markup transforms, and arbitrary text-image grammar. |
| Contents (`C4MN_Contents`) | `C4ObjectMenu` | **Partial** | CollectionLimit and RejectCollection downgrade-to-Activate, entrance-forced Get, grouped rows, and periodic/immediate refill are implemented and tested; the earlier missing-gate bug is fixed. No refill defect remains. Remaining gap is the shared C4Menu scroll-offset/wheel/drag affordance. |
| Selection-follow scrolling/pointer drag | `C4Menu`, `C4ObjectMenu` | **Partial** | Construct drag-out-of-menu now works end-to-end: 5px drag gate, live ConstructionCheck site preview, dimmed menu while dragging, and a synchronized Construct command on a valid drop, all tested. Persistent per-menu scroll offset, mouse-wheel scrolling, and title-bar drag repositioning remain unimplemented; the visible window is still re-derived from selection every frame. |

## Script-created menu grammar

| Feature | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Normal style | `CreateMenu`, `AddMenuItem` | **Partial** | Classic grid geometry, selection, shipped image recipes, decoration, progressive text and title/tooltip TextSpec images render in production. Render preflight fails fast with a logged error for every unresolved requested `AddMenuItem` picture, including non-TextSpec recipes. Remaining: generic pointer/scrollbars, callback gaps, and the portrait-state forms listed below. |
| Context style | same | **Partial** | Classic layout, command strip, pointer targeting, shipped image recipes and caption/title TextSpec images render in production. Render preflight fails fast with a logged error for every unresolved requested `AddMenuItem` picture, including non-TextSpec recipes. Remaining: exact scrolling, nested callback cases, and late-layout ObjectRank height for mod-created menus. |
| Info style | `C4MN_Style_Info` | **Partial** | Classic width/row geometry, pictures, markup-aware wrapping, complete TextSpec image grammar, pointer targeting, no highlight/tooltip, and close-only footer render in production. Both unresolved TextSpec images and unresolved requested non-TextSpec `AddMenuItem` pictures fail with a logged error during render preflight. All shipped paths create exactly one row; 137 `MessageWindow` callers across 103 files reach this style. Remaining: exact italic markup transforms, active-scenario resource overrides, and generic multi-row scrollbars. |
| Dialog style | `C4MN_Style_Dialog` | **Partial** | Four direct shipped creation sites fan out through Hazard, LastWill, and Western dialogue helpers. Production uses the classic variable-height layout, portrait column, empty-title rule, pointer targets, selection/progress behavior, decoration path, complete inline TextSpec grammar and shipped image recipes; unresolved requested images or drawable decoration sheets fail fast. A natural Goldrush Talker sequence and real LastWill resources render without fallback. Remaining: natural LastWill/Hazard scenario fixtures and current-vs-permanent portrait-state gaps; all shipped Dialog sites use `extra=0`, so the optional footer is mod-facing. |
| EqualItemHeight flag | `C4MN_Style_EqualItemHeight` | **Complete** | The raw style bit is preserved independently of base style and Dialog symbol rows use the C++ equalization/restacking rule. |
| Rank/Indexed/ObjRank/Object/TextSpec/Color symbols | `AddMenuItem` image grammar | **Partial** | Shipped calls use exact fallback-definition resolution, clipped indexed phases/colors, inherited rank strips and extended/captain ranks, and add-time Object/ObjectRank snapshots. Snapshots include temporary objects, foreign graphics/overlays/transforms, `ChangeDef`, color modulation, and Dialog's 64-pixel symbol size. All 75 shipped definition portraits are recursively inventoried/loaded in group order, and natural Western/LastWill paths are covered. TextSpec now shares one exact parser for uppercase/underscore IDs, scanf-style indexed phases, named portraits with real bitmap validation, and all seven prefix-matched `Ico:*` forms; inline images participate in title/body measurement, rendering, tooltips and pointer layout for every style, including Goldrush's shipped `SBTR` Context rows. Remaining shipped gap: persisted/current-vs-permanent crew portraits used by Knights/Hazard. Remaining mod-facing gaps include late-layout Context ObjectRank height, active-scenario graphics/font overrides, and owned-pixel snapshots if asset hot reload is introduced. |
| TextSpec grammar/corpus | `C4Game::DrawTextSpecImage`, `C4DefList::GetFontImage` | **Complete** | Parser tables cover C scanf prefixes, raw portrait IDs/colors, case rules and icon prefixes; recursive shipped team census covers 39 `IconSpec` values in 19 files. Keep the Goldrush Context render/hit-test regression current. |
| Footer extras (`C4MN_Extra_*`) | `C4Menu::DrawElement` | **Partial** | Value, magic, components and close/command strips are modeled; retain explicit coverage for every extra kind and dynamic refill source. |
| Explicit size/permanence/alignment | `SetMenuSize`, `SetMenuPermanent`, `CreateMenu` alignment | **Partial** | Core fields survive; finish free-position Context requests, movement/clamping and close semantics. |
| Primary/secondary commands | `C4MenuItem::Command`, `Command2` | **Partial** | Engine preserves both command strings; complete right-click dispatch through every menu style and nested callback path. |
| Menu decoration | `SetMenuDecoration` | **Partial** | `SetByDef` snapshots callback-derived color/borders and all eight `FrameDeco*` facets immediately; every classic style applies packed alpha, tiled/truncated edges, protruding corners, clipped out-of-bounds source facets, and captured margins. Background-only decoration is valid; unsafe geometry and unresolved drawable facets fail fast. Real LastWill assets and the natural Western Goldrush dialogue path are covered. Remaining: definition hot-reload refresh/clear behavior when Rust gains that API. |
| Progressive text | `SetMenuTextProgress` | **Complete** | Per-row byte offsets, portrait exclusion, markup-skipping shared budgets, selectable-row reveal, late-added rows, one-byte menu ticks, explicit show-text, local-only first-input conversion, and byte-prefix Dialog rendering are modeled and tested. |
| Selection/close/command callbacks | `C4Menu`, script callbacks | **Partial** | Object-owned `MenuQueryCancel` and `OnMenuSelection` work on the covered host paths. Finish engine-side close/selection/command dispatch and the remaining no-command-object `CB_Scenario` paths; do not describe all `MenuQueryCancel` handling as absent. |
| Script-menu resource/font fallback boundary | `C4Menu::Draw`, `C4GUI::Resource`; `ingame_menu.rs` | **Partial** | Render preflight uniformly fails fast with a logged error for every unresolved requested `AddMenuItem` picture across Normal, Context, Info and Dialog, including non-TextSpec recipes. Pointer targeting shares the exact global-resource preflight and returns a typed, logged boundary for unresolved inline images before hover, left, or right input can reach the world. |

## Other game-visible dialogs and overlays

| Screen | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Game over/evaluation | `C4GameOverDlg` | **Partial** | The game-over Evaluation dialog is running-mode only (the result computation itself can still occur during game clear): a forged stale `game_over_dialog` in Menu now returns a typed boundary carrying the exact startup root before startup bootstrap/view/model/child/status, matching-cache replay, or logical/native pixels. All seven `StartupView` roots—including the retained Network Chat, Options Graphics and About Licenses children—are exhaustive regressions; invalid lifecycle also wins before game-over resource validation and the `GameOverResources` boundary, matching C++'s exclusive startup/running lifecycles. The eight unconditional running-mode core/dialog/icon resources (`CStdFont/Endeavour.ttf`, `GUICaption.png`, `GUIButton.png`, `GUIButtonDown.png`, processed `GUIButtonHighlight.png`, `GUIIcons.png`, `Player.png` and the HUD `Score.png` source) retain their ordered fail-fast preflight before viewport collection or pixel output. Later unported branches still need a recursive resource inventory for `GUIIcons2.png`, `Crew.png`, `GUIScroll.png` and dynamic definition/player images. Team/league rows, live network result/streaming updates and exact host/film button policy also remain. |
| Game-over goal-display child | `C4GoalDisplay`, `GoalPicture` (`C4GameOverDlg.cpp:25-78`) | **Partial** | Rust freezes and renders the recursive goal grid with definition pictures, fulfilled-star overlays, unfulfilled grayscale and responsive rows. Exact goal tooltips and remaining dynamic-definition resource ownership are missing. |
| Game-over chat input | `C4GameOverDlg.cpp:317-321`; `C4MessageInput` | **Complete** | Bare Return/F2 opens All chat and Shift+Return opens Allies through the ordinary running-chat controller, including its edit, history, completion, paste/clipboard, submit/cancel and network lifecycle. Exact Alt/Alt+Shift first dispatches the localized visible-button mnemonic at control priority; an unmatched Alt+Return then falls through to Say chat. With signed `[Controls] GamepadGuiControl` enabled, raw gamepad-0 `AnyLow` opens All chat whenever no actionable close/button owns focus, so East remains chat despite its later Cancel alias; disabled GUI control and every nonzero physical pad ID remain inert for the exclusive evaluation GUI stack. Logo remains outside the C++ modifier mask and keypad Enter is not silently aliased on the macOS SDL route. Shared command-language descendants are inventoried below. |
| Game-over focus and keyboard/gamepad lifecycle | `C4GUI::Dialog`, `C4GameOverDlg` | **Partial** | Evaluation opens with true no-focus state. Tab/Shift+Tab and enabled gamepad-0 Right/Left traverse and wrap the exact currently rendered C4GUI order: caption Close, the one or two selection-disabled PlayerInfo lists, then each visible End/Continue/Restart/Next-Mission button. Focus remains stored but inactive while chat or another higher dialog is open and resumes when exposed; its dialog/control callbacks cannot receive keyboard or gamepad input through an active chat or newer F4 client-list dialog. Each actionable control owns the C++ `fDown` latch shared by Return, Space, gamepad Low and pointer input, including mixed-key releases, idempotent repeated downs, focus changes, drag leave/re-entry, ArrowHit/Click dispatch and Clear cancellation. A non-action focus leaves Return/Low to All chat. Localized Alt mnemonics for End/Continue/Restart invoke those visible buttons directly without synthesizing press sounds. Pointer hover/drag remains independent of keyboard focus, while Escape/High invokes End. Existing message/context/input/evaluation precedence, physical gamepad eligibility and per-cluster alias ownership remain intact. Remaining: default (omitted-text) `SetNextMission` labels/descriptions are still frozen to US strings instead of the active `IDS_BTN_NEXTSCENARIO`/`IDS_DESC_NEXTSCENARIO`; F4 client-list keyboard/gamepad traversal itself remains outside this row, and live network-result host button visibility/focus repair is tracked by L117 (`game-over-net-result-host-button-gate`). |
| In-game chat/script query input | `C4MessageInput` | **Partial** | C4ChatInputDialog now ports ordinary running chat: All/Allies/Say modes, 20-entry history, Tab nick completion, multiline paste, and Ctrl/gamepad forwarding are implemented and tested; team/private/me/sound/alert/say classify correctly. Missing: the fObjInput script-query dialog (CallMessageBoard prompt/uppercase) never opens despite full engine-side query/answer plumbing, and ProcessCommand beyond mute/unmute -- msgboard/clear/observer/fast/slow/set/chart/kick/script/help/custom AddMsgBoardCmd commands stay unreachable from chat. |
| Scoreboard dialog | `C4Scoreboard`, `C4ScoreboardDlg` | **Partial** | The saved matrix/refcount and ordered `DoScoreboardShow` lifecycle now drive a real classic dialog: top-right preferred-viewport placement, wooden optional title/Player icon/close button, the constructor's allocated-empty-title stale margin, live row/column reflow, 4px/3px cell geometry, first-column/center alignment, markup-aware title, unclipped spreadsheet overflow, alpha/color markup, C++ `-0.3` italic shear and resolved `{{TextSpec}}` definition images. Bare or Logo+Tab and the close button honor dialog/context/player-control priority plus CMouse's left-button drag-move/release hit testing; game over, restoration and same-tick open/close semantics retain the C++ ownership rules. Missing/malformed sheets, fonts or inline images log a typed error before viewport/output pixels instead of exposing a generic pane. Remaining: custom `C4KeyConfig` ScoreboardToggle remapping; close-button hover/down highlights and ArrowHit drag sounds; complete shared-screen stacked-dialog activation/z-order and right/middle/touch/wheel fall-through; draggable title, tooltip and 3-second title autoscroll; collapse the one-update allocated-empty title margin on the next data invalidation; cache placement until show/data Update instead of following a preferred-rect-only change every frame; preserve fractional custom-image draw widths/pen advances and post-filter color/alpha modulation; console-mode native-child handling; and pathological atlas-neighbor sampling at italic image edges. |
| Upper board modes and right-side indicators | `C4UpperBoard::Init`, `Draw`, `Height`; `lc-frontend/src/hud.rs` | **Partial** | Rust renders a substantial Full-mode board, scenario title, game time, optional local wall clock and sampled FPS in the classic right-slot order. Implement Hide/Small/Mini geometry and texture/logo scaling plus the Mini message-board split. |
| Player and cursor HUD subtree | `C4Viewport::DrawCursorInfo`, `DrawPlayerInfo`, `DrawPlayerControls`, `DrawPlayerStartup`; `lc-frontend/src/hud.rs` | **Partial** | ShowPortraits and DefCore HideHUDElements/HideHUDBars now gate portrait/captain/rank/name/inventory/bars, and HUD command-bar/inventory left-clicks send region Coms (COM_Contents, AutoStopControl) instead of leaking MoveTo. Missing: ShowPlayerHUDAlways/ViewWealth/ViewValue conditional gating of the wealth/score/crew HUD (Rust always draws them), player-menu button HUD regions, remaining dynamic visibility rules and exact resource ownership. Separate from the in-game menu section's Missing fallback. |
| Eliminated/surrendered viewport message | `C4Viewport::DrawMenu:970-975` | **Missing** | Rust retains eliminated viewport records but does not draw the centered red localized eliminated/surrendered text or suppress the ordinary menu path as C++ does. |
| Object-independent `NO_OWNER` observer viewport | `C4FullScreen::ViewportCheck`; `C4Viewport` | **Partial** | Zero-object/no-locals now render a real object-independent NO_OWNER camera (with mouse edge-scroll and audio-mix awareness); only a declared local with a missing player or viewport reaches the typed LocalViewportUnavailable boundary before pixels. Missing: exact C++ CenterPosition/UpdateViewPosition math (ViewX/Y init, fullscreen border-clip when GBackWdt<ViewWdt, iAutoScrollBorder mouse-scroll percentage, ViewOffsX/Y reset) — Rust uses a static landscape-center anchor instead. |
| F1 two-column game-help overlay | `C4Game.cpp:3378`; `C4GraphicsSystem::DrawHelp` | **Partial** | Bare or Logo+F1 now toggles the real unframed FontRegular overlay on every Down (including repeats); Up has no help callback, modified variants retain downstream priority without leaking into modifier-blind player bindings, and persisted SDL/X11/Win32 physical F1 player bindings win at `PRIO_PlrControl` while Control scope is active. That priority is recursively covered across all ten `IngameMenuState` page tags (including both `AbortConfirm` model variants), engine-owned cursor/script menu styles 0–3 and both progressive-text states, the save browser, nonexclusive messages, contexts and the scoreboard; exclusive game over suppresses Control but retains Generic help. App-owned Inventory/Container/Context/Build state is production-unreachable and rendering it already fails typed rather than showing a guessed pane, so synthetic state-only coverage is not claimed as visual parity. New-game/default reset, process-start language caching, per-game KeyConfig ownership, case-insensitive directory/packed language lookup, C4ResStrTable malformed/duplicate/empty-file rules, CP1252/table-owned UTF-8 decoding, eager Unicode face-charmap rasterization with `.notdef` fallback, exact Full-mode `ViewportArea` anchors, active labels, 11-left/6-right action rows, yellow key markup, the oracle's blank mismatched `GameSpeedDown` key, scenario gamma and draw order after menus/before C4GUI are covered. Missing/ambiguous language or UpperBoard/font ownership, unsupported FontRegular italics/images, a hidden-to-visible toggle under non-Full board geometry, tiny-surface fallback, or an `Extra.c4g/KeyConfig.txt` override fails typed before help pixels; an already-visible overlay can still be hidden after geometry changes. Remaining: load/apply arbitrary global KeyConfig remaps and reflect every remapped displayed key live; port Hide/Small/Mini upper-board geometry instead of refusing help there; support non-default backend names; and share the process-global localized table with the rest of the UI instead of a help-specific cache. |
| Pause/hold overlay | `C4Game::TogglePause`, `Pause`, `Unpause`; `C4GraphicsSystem::DrawHoldMessages` | **Fail-fast** | Bare or Logo+Pause is now handled as a runtime-global key before every modal, context, object, player or evaluation dialog. It is the exact no-op while game over is shown. Otherwise it logs and returns a typed route: offline cannot mutate the unavailable `Game.HaltCount`; a consistently identified host/client cannot choose safely because runtime league/evaluated state is unavailable; inconsistent network manager/mode/client-ID combinations use a distinct unknown-role boundary. This deliberately never claims non-league mode: league Pause/Unpause recursively starts a `VT_Pause` vote, while non-league host changes network status, client returns without mutation and offline toggles the halt count. Releases are consumed and repeated presses can reach the boundary again. The centered Pause overlay, halt/status ownership, console toolbar synchronization and vote subtree remain unported. |
| Timed flash-message overlay | `C4GraphicsSystem::FlashMessage`, `DrawFlashMessage` | **Partial** | The one-message replace/clear buffer, classic-encoded byte-length lifetime, 512-byte/NUL behavior, draw-only countdown, final visible pass, frozen upper-board/split-viewport Y, resize recentering, markup/gamma/Unicode FontRegular rendering and exact layer between F1 and C4GUI are implemented; a 512-byte cut through a UTF-8 scalar deliberately fails typed instead of drawing invalid text. Running F3 derives On/Off from actual or pending playback without changing RXMusic; in-game Options changes RXMusic too; script Music state, ended-track restart, save restoration and owned-viewport clear are synchronized. Recursive executable coverage includes all ten player-menu tags with both Abort variants, styles 0-3 with both progress states, message/context/scoreboard/game-over layers, and typed pixel-free Save/Load/object/observer refusals; rebound player priority and Ctrl+F3 are included. Debug/speed chords and custom KeyConfig ownership fail typed. `CID_Set` is decoded and its inner author is authenticated before queued tick ingestion: host control-rate and fair-crew producers fail at their named boundary, other unimplemented values at a distinct Set boundary, all before batch mutation. Non-host Types 0/2/3/4/5 preserve their C++ host-gated no-op; Type 1 `DisableDebug` is not host-gated and fails typed for every author until implemented. `SetPreSend` keeps negative/offline return semantics and network calls escape ordinary tolerated script errors into the logged `ScriptTargetFps` boundary. Observer, runtime-join and lobby producers remain behind typed ancestor screens where unimplemented. Adaptive PreSend/TargetFPS remains missing because exact reachability requires per-peer RTT/tunnel state, rolling send time, applied control mode, current PreSend and TargetFPS; packet timestamps cannot substitute. Generic status text is never counted as flash parity. |
| In-game mouse-control/cursor overlay | `C4MouseControl::Draw`; `C4Viewport::DrawMouseButtons` | **Partial** | Cursor icons (Help/Add/Construct/8-direction edge-scroll via draw_mouse_cursor), selection marks/frame, construction-drag preview, FoW-gated help targeting, Time-on-Target caption delay, and Help/Player-menu/Chat command regions are implemented (L053/L054). `MouseWorldCursor`/`IngameMouseCursorKind` model Grab/Chop/Dig/Build/Put/Throw*/Jump*/Vehicle*/Crosshair/etc. for caption text only — draw_mouse_cursor never draws their matching C4MC_Cursor_* icon, so those action pointers stay the generic cursor. |
| Debug and network-status viewport overlays | `C4Viewport::Draw`; `C4GraphicsSystem::ToggleShow*`; `C4Network2::DrawStatus` | **Fail-fast** | Ctrl+F5/F6/F7/F8 (DbgModeToggle/Vtx/Action/SolidMask) now reach the typed RuntimeFlashProducerBoundary before any mutation — shared machinery with the flash-message row, tested at KEYSCOPE_Generic priority over game-over exclusivity with no player-control leak. NetStatsToggle has no default binding, same as C++. All debug draw content itself (entrances/vertices/actions/commands/pathfinder/solid-masks, detailed network state) remains unported; the boundary only guards the toggle keys. |
| Rust-only FRAME/POS/VEL debug HUD | no C++ visual authority; `LC_APP_HUD_DEBUG`, `GraphicsSystem::draw_hud` | **Missing** | This explicit environment route exposes Rust status strings from object-menu actions, unsupported in-game main-menu actions, save/load, network/desync and right-click failures, game-over/load transitions and focus diagnostics. Keep it developer-only and documented, or reject it in parity runs; it is not parity evidence and never substitutes for help, flash, network status or other classic overlays. |
| Loader/progress/log screen | `C4LoaderScreen.cpp:126-177`; `lc-frontend/src/loader_screen.rs`; `lc-app/src/main.rs` | **Partial** | The resource-validating frontend and app wire classic group priority/order, weighted loader selection, extension-directed decode, title and GUI-resource precedence, user gamma, resource refresh, logical chrome and scale-native text. At scaled output the ordered compositor now preserves C++'s loader-text-before-GUI order for the host start-wait and client message-dialog layers, including fractional scales (`l149_scale_three_host_start_wait_renders_after_native_loader_text`, `l149_fractional_client_wait_and_upper_dialog_keep_native_layer_order`). Unsupported Extra/language-pack ambiguity fails closed. The Rust workers still do not own C++'s live boot/scenario progress, process text or log-buffer milestones; nonpositive `Config.Graphics.Scale` remains a typed boundary rather than C++ config sanitation. |
| Loader generic-fallback boundary | `C4LoaderScreen`; `lc-app/src/main.rs::render_loading` | **Fail-fast** | Every missing selection, resource, font, invalid/nonpositive scale and render failure is logged and returned as a typed error before the old generic pane can draw. This guard remains required for the unresolved Partial states above; it is not evidence that live progress/log behavior is complete. |
| Object/global game messages (`C4GameMessage`) | `C4GameMessage.cpp:38-231` | **Partial** | Global/player-global messages use C++ viewport-relative geometry, wrapping, portrait resolution, snapshotted FrameDecoration, clipping and insertion order; high-DPI native rendering and Tutorial01's exact pixels are pinned. Inline {{image}} text now renders for Global/GlobalPlayer messages. Target-attached/FoW messages remain typed fail-fast boundaries; remote-only, missing-target and logically offscreen messages do not overblock. |
| Message board modes/history | `C4MessageBoard` | **Partial** | C4MessageBoard::ControlScrollUp/Down (Shift+Up/Down), the 1000-line/30000-byte LogBuffer history, and player-color-by-owner text are now implemented and tested. Missing: iMode 1 (Config.Graphics.MsgBoard >=2 lines) continuous multi-line board, ChangeMode/the /msgboard command and mode persistence, and Fader/ScreenFader fade-in/out timing, which stays hardcoded to zero. |
| Runtime per-client information dialog | `C4Network2ClientDlg` | **Partial** | Clicking a client row in the F4 client list or choosing Info on a lobby client row opens the shared classic-shaped panel (activated/local/host, label, addresses, connection lines matching `IDS_NET_CLIENT_INFO_*` wording). The lobby route is wired through the real host and joined-client rosters; the evaluation-dialog entry point remains. |
| Runtime client list | `C4Network2ClientListDlg` | **Partial** | Host mute/activate/kick/disconnect (league-gated), connection rows, live Tick/Behind/Rate/PreSend/ACT footer, pointer hover/click row and info-panel interaction, wheel scroll, and Escape/toggle lifecycle are implemented and tested (`runtime_client_list_league_actions_gate_activate_and_vote_to_kick`). Remaining: the real `C4GameOptionsList` row (a two-line text stub), keyboard Tab/arrow focus traversal, gamepad Left/Right/Low/High focus and activation routing, and name/nick/player tooltips. |
| Runtime ready-check timed dialog/toast | `C4Network2::ReadyCheckDialog`, `C4Network2.cpp:129-198,1625-1690` | **Partial** | `MessageDialogContinuation::LobbyReadyCheck` reproduces `C4Network2::ReadyCheckDialog`: exact 'Are you ready?' caption, `IDS_DLG_READYCHECKTEXT` per-second countdown, 15s timeout closing as not-ready, `CanBeReady`/resource-loaded gating, and Yes/No wired to `PID_ReadyCheck` (`complete_client_ready_check_opens_one_exact_fifteen_second_prompt`, `ready_check_prompt_counts_down_and_times_out_not_ready_at_fifteen_seconds`). Only the desktop toast action remains, tracked separately at M10-P4-L030. |
| Network vote dialog | `C4VoteDialog`, `C4Network2::Vote`, `AddVote`, `EndVote`, `OpenVoteDialog` | **Partial** | `MessageDialogContinuation::LeagueVote`/`LeagueSurrender` implement 120s rate limiting, duplicate suppression, 10s host timeout->reject, ordered pending votes, host pause-for-vote, `IDS_VOTE_*`-matching text, Yes/No dispatch and rejected-own-vote surrender branching. Missing: `C4Network2::Vote`'s immediate surrender-dialog fallback when a rate-limited own cancel/kick resubmits — Rust silently drops that resubmission instead of opening the surrender confirmation. |
| League surrender confirmation | `C4Network2::OpenSurrenderDialog`, `LeagueSurrender` | **Partial** | The Yes/No text matches `IDS_VOTE_SURRENDERWARNING` verbatim and is reachable after a rejected own kick/cancel vote (`accepting_league_surrender_clears_network_and_aborts_round`). Accepting only clears the network and returns to the menu; unlike C++'s `LeagueSurrender`/`OpenSurrenderDialog` it never records `RoundResults.EvaluateNetwork(NR_NetError, ...)` nor reports the disconnect to the league server first. |
| Network statistics chart | `C4ChartDialog`; `C4Network2Stats` backing model | **Missing** | OC/FPS/IO/ping/control/APM tabs. |
| League signup/register/auth form | `C4LeagueSignupDialog`, `C4League.cpp:548-749` | **Missing** | Account/password/confirm/check-password fields, validation, warning and modal return. |
| League waits/retries/results | `C4League`, network dialogs | **Missing** | Request progress, retries, authentication failures, score/rank/result screens and disconnect reporting. |
| Reusable URL/download progress dialog | `C4DownloadDlg` | **Missing** | Port transfer progress, cancel, completion/error ownership and the updater callers. Lobby resource status/save remains the separate `C4Network2ResDlg` subtree above. |
| File/player/definition/portrait selectors | `C4FileSelDlg` family | **Partial** | Definitions (multi-select) and Player (single-select `C4PlayerSelDlg`, wired from the classic lobby Add-Player flow with resource-validated fail-fast and wire-safe relative filenames) specializations are both complete (`classic_lobby_add_player_picker_publishes_relative_file_and_projects_echo`). `C4PortraitSelDlg` (picture/BigIcon options) and the location combo-box jump remain the only unported specializations. |
| Rust-only F5 quick save | no C++ menu equivalent; C++ uses `C4MainMenu::ActivateSavegame` | **Fail-fast** | The shortcut logs and returns a typed boundary instead of bypassing the classic ten-slot flow. |
| Rust-only F9 quick load | conflicts with C++ `C4Game.cpp:3373-3374` | **Complete** | Resolved: F9 no longer quick-loads or fails typed. handle_runtime_global_key now queues the real C4GraphicsSystem::SaveScreenshot(false)/(true) action via pending_screenshots for F9/Ctrl+F9, working above open dialogs and never ending the game — pinned end-to-end by running_f9_saves_presented_rgb_and_ctrl_f9_saves_full_landscape. Loading remains routed through the oracle-backed savegame menu (MenuAction::ActivateSavegame); screenshot fidelity gaps are tracked on the next two rows. |
| C++ F9 viewport screenshot | `C4Game.cpp:3373`; `C4GraphicsSystem::SaveScreenshot(false)` | **Partial** | F9 now queues and executes a real PNG screenshot capture end-to-end: first-free Screenshot{:03}.png slot reuse, [General] ScreenshotFolder config with install-root fallback, and gamma-correct RGB output, verified by a decode-and-compare test. Missing: localized IDS_PRC_SCREENSHOT/IDS_PRC_SCREENSHOTERROR log text (Rust logs a generic tracing message) and the C++ Application.isFullScreen/lpBack preconditions. |
| C++ Ctrl+F9 whole-landscape screenshot | `C4Game.cpp:3374`; `C4GraphicsSystem::SaveScreenshot(true)` | **Partial** | Ctrl+F9 now temporarily retargets the viewport to NO_OWNER, clears sky parallax to 10/10, renders, and restores state — matching DoSaveScreenshot(true) — verified end-to-end alongside the F9 test. Rust renders the world in one pass rather than C++'s tiled/page-flip loop. Still missing: cursor marks and the parallax foreground-object layer that C++'s Viewport::Draw still emits even with fDrawOverlay=false; same localized-feedback gap as F9. |
| Rust-only F6/F7 named save/load browser | no C++ equivalent; `lc-frontend/src/save_browser.rs` | **Fail-fast** | The renderer remains in-tree, but the app guard rejects both shortcuts and any defensively retained Save or Load state now returns a mode-specific typed boundary before output pixels or timed-flash countdown mutation. Remove it or replace it with the classic ten-slot flow; never relax the guard. |
| Rust synthetic menu sandbox | no C++ player-facing equivalent; `lc-app/src/main.rs` | **Complete** | Empty discovery/catalog paths no longer inject the sandbox into player-facing menus. The explicit `--sandbox` developer/test route remains intentionally separate from menu discovery. |

## Required generic-fallback boundaries

Until the classic screens above are wired, central app boundaries should group
the remaining unsafe presentation routes rather than adding more status-only
call-site checks:

- startup status substitute: include the active startup view and producer/message;
- running viewport unavailable: distinguish zero objects, each missing/empty
  declared local viewport, each owner/slot with no live focus and the
  unavailable object-independent observer camera without inventing a focus
  object;
- game overlay unavailable: identify Help, Pause, Flash, Mouse Control,
  Eliminated, Upper Board mode and debug/network status;
- HUD resources unavailable: report the complete missing classic resource set
  before any generic font, solid surface or omitted image can draw.

The boundary is reached only when that presentation would be visible. Merely
retaining an old renderer in the tree is acceptable when a tested guard makes
it unreachable.

## Reusable modal infrastructure required

Full recursive parity requires classic implementations of these C++ dialog
families before individual call sites can be considered complete:

The ordinary classic message-dialog foundation is now **Partial**: regular,
medium and small geometry; normal/extended icons; canonical OK/Retry/Cancel/
Yes/No ordering; title-close; active-only stacked focus; keyboard, Alt hotkeys,
mouse, touch and legacy low/high gamepad routing; GUI sounds; no-scrim rendering;
continued underlying timers; fail-fast asset validation; and the localized-label
capable “don't show again” checkbox are implemented. Caption drag, the 3 s
bounce auto-scroll and the title tooltip now ship for `message_dialog.rs`
(L055). Active scenario `Graphics.c4g`/font overrides, localized standard
button strings and further call sites remain; sibling Dialog-titled screens
(F4 client list, its info sub-dialog, Advanced Options) still lack the whole
drag/autoscroll/tooltip trio, and `definition_sel.rs` has drag only.

- message/error/info (**Partial**: the reusable base now backs at least a dozen
  typed continuations beyond the original two — startup network
  join/redirect/scenario warnings, options scale-test/control-capture/
  alternate-server/reset/advanced warnings, and the runtime league
  vote/surrender and lobby ready-check flows (17 `MessageDialogContinuation`
  variants). Active-scenario `Graphics.c4g`/font overrides and localized
  standard button strings remain);
- Yes/No and multi-button confirmation (**Partial**: beyond player deletion,
  Yes/No confirmations now gate startup player/crew/scenario deletion, league
  vote/surrender prompts, and the 15 s lobby ready-check and
  graphics-scale-revert timed prompts. Other C++ Yes/No callers outside these
  families still need coverage);
- “don't show again” checkbox (**Partial**: exact component exists; the
  alternate-league-server warning (`HideMsgNoOfficialLeague`) is now a wired
  caller; the other shipped config-bound callers remain);
- text/password input (**Partial**: `lc-frontend/src/input_dialog.rs` provides
  the resource-validating classic controller/renderer; network-selector
  Password/Comment, the lobby host Password/Comment, the network-join password
  challenge and the crew death-message editor are app-wired; the other C++
  callers and active-scenario resource overrides remain);
- timed confirmation (**Partial**: the lobby ready-check prompt (15 s,
  `tick_lobby_ready_check_prompt`) and the graphics scale-test prompt (12 s,
  `tick_options_scale_test_prompt`) both match C++'s per-second text update and
  timeout-as-No close, but duplicate near-identical tick/expire logic instead
  of sharing one `TimedDialog`-equivalent chassis; no other C++ `TimedDialog`
  caller is ported);
- wait/progress with cancel (**Missing**);
- scrolling text/info (**Missing**);
- context and nested context menus (**Partial reusable chassis**: exact classic
  geometry/assets, recursive submenus, deepest-first pointer routing,
  keyboard/hotkeys, touch and low/high gamepad input, flip placement,
  selection/tooltips/sounds, focus suppression, outside-down pass-through,
  action-release capture and fail-fast resources are implemented; startup
  player-row Properties/Delete, the startup Participants Add/Remove tree, the
  scenario-search Edit actions, crew rows (`PlrSelCrewContextMenu`), lobby
  right tabs (`open_classic_lobby_tab_context`), the lobby roster Client-row
  Kick menu, the lobby team-select combo and the lobby roster Player-row Take
  Over/Remove/New Color root entries are callers. Remaining consumers include
  combo boxes beyond the lobby team combo and nested lobby Take Over
  enumeration);
- combo-box dropdowns (**Partial**: the classic lobby team-select
  `C4GUI::ComboBox` opens through the shared context-menu chassis
  (`open_classic_lobby_team_combo`) with keyboard/mouse/permission-recheck
  coverage, matching M09-P2-L002. Other `ComboBox` consumers (options-screen
  fields) are unverified against this chassis);
- file/image/player/definition selection (**Partial**: Definitions
  (multi-select) and Player (single-select `C4PlayerSelDlg`, wired from the
  classic lobby Add-Player flow) specializations are both complete.
  `C4PortraitSelDlg` (picture/BigIcon options) and the location combo-box jump
  remain the only unported `C4FileSelDlg` specializations).

Unsupported entry points must keep logging and returning an error until their
classic implementation lands. Reintroducing a generic pane is not completion.

## Editor-only GUI, tracked separately

The Rust startup editor/controller substitute is not this GUI and does not make
any row Partial. Until these branches exist, editor entry points must be
explicitly rejected instead of opening a synthetic controller or an external
editor as if parity had been reached.

| Screen or recursive branch | C++ authority | Rust status | Remaining work |
|---|---|---|---|
| Console shell, native menu bar, log, script entry and status bars | `C4Console.cpp:277-578` | **Missing** | Window lifetime/position, log scrolling, command/script dispatch, cursor label, frame/script/time/FPS status and enable rules. |
| Play/Halt and Play/Edit/Draw mode toolbar | `C4Console::InitGUI`, `UpdateHaltCtrls`, mode callbacks | **Missing** | Pause ownership, mutually exclusive tool modes, icons and editing/network gating. |
| File / Open | `C4Console::FileOpen` | **Missing** | Native scenario selector, close/current-game confirmation, load errors and recent state. |
| File / Open with Players | `C4Console::FileOpenWPlrs` | **Missing** | Scenario selection plus recursive player-file selection and start transition. |
| File / Save Scenario and Save Scenario As | `C4Console::FileSave`, `FileSaveAs(false)` | **Missing** | Component packing, path selector, overwrite/error dialogs and current path update. |
| File / Save Game and Save Game As | `C4Console::FileSave`, `FileSaveAs(true)` | **Missing** | Runtime/player eligibility, savegame path/overwrite and error branches. |
| File / Record | `C4Console::FileRecord` | **Missing** | Runtime-record eligibility, filename flow and record-state transition. |
| File / Close and Quit | `C4Console::FileClose`, `FileQuit` | **Missing** | Unsaved/running confirmations, teardown and application quit. |
| Components / Objects | `C4Console::EditObjects`, `C4ObjectListDlg` | **Missing** | Open/update the recursive Object List screen described below. |
| Components / Script | `C4Console::EditScript` | **Missing** | Scenario script component editor, save/reload and syntax/error handling. |
| Components / Title | `C4Console::EditTitle` | **Missing** | Title component text editor and persistence. |
| Components / Info | `C4Console::EditInfo` | **Missing** | Info component text editor and persistence. |
| Player / Join | `C4Console::PlayerJoin` | **Missing** | Player selector, join validation/control and errors. |
| Player / dynamic Quit entries | `C4Console.cpp:1410-1461` | **Missing** | Rebuild per active player, permission-sensitive enable state and eliminate control. |
| Viewport / New | `C4Console::ViewportNew` | **Missing** | Detached viewport construction, placement and close ownership. |
| Viewport / dynamic per-player entries | `C4Console.cpp:1203-1266` | **Missing** | Rebuild by player and create player-targeted viewport. |
| Network / dynamic client entries | `C4Console.cpp:1540-1608` | **Missing** | Insert/remove menu, local/remote labels and host-authorized client removal. |
| Help / About | `C4Console::HelpAbout` | **Missing** | Native developer About dialog. |
| Landscape mode selector | `C4ToolsDlg.cpp:262-407,865-896` | **Missing** | Dynamic/Static/Exact modes and enable/update rules. |
| Drawing tool selector | `C4ToolsDlg` | **Missing** | Brush/Line/Rectangle/Fill/Picker, temporary alternate tool and cursor integration. |
| Drawing material/texture/grade/IFT controls | `C4ToolsDlg.cpp:410-1015` | **Missing** | Material and texture combos, preview, grade slider, IFT toggle, defaults and landscape validation. |
| Object Properties display/script input | `C4PropertyDlg.cpp:103-166,398-520` | **Missing** | Selection-dependent property text, definition reload and object-scoped script command entry. |
| Object List tree/selection | `C4ObjectListDlg.cpp:486-805` | **Missing** | Live add/remove/rename updates, definition icons, multi-selection and edit-cursor synchronization. |
| Shared developer notebook/window | `C4DevmodeDlg.cpp:44-130` | **Missing** | Add/remove/switch pages, title, hide/show, transient ownership and remembered position. |
| Edit-cursor context root | `C4EditCursor.cpp:79-104,582-627` | **Missing** | Permission-sensitive native popup and mode-dependent Properties/Tools label. |
| Edit cursor / Delete | `C4EditCursor::Delete` | **Missing** | Selected-object control dispatch and state refresh. |
| Edit cursor / Duplicate | `C4EditCursor::Duplicate` | **Missing** | Duplicate selection/control semantics. |
| Edit cursor / Grab Contents | `C4EditCursor::GrabContents` | **Missing** | Replace selection with contents, update properties and exit objects. |
| Edit cursor / Properties or Tools | `C4EditCursor::OpenPropTools` | **Missing** | Route by Edit versus Draw mode to the correct developer page. |
| Detached/new viewport windows and viewport input | `C4Viewport`, `C4Console::ViewportNew` | **Missing** | Native windows, player targeting, cursor context and teardown. |
| Platform open/save dialogs | console and component callbacks | **Missing** | Native path selection, filters, overwrite and cancellation semantics. |
| Platform message/error/crash dialogs | console/startup/application error paths | **Missing** | Parent/modal ownership, expandable crash log, restart/fatal variants and localization. |

These may be scheduled after player-facing menus, but literal full C++ GUI
parity is not achieved while they remain absent.

## Verification gates

Every row promoted to **Complete** needs:

1. a C++-referenced behavior test for each transition and nested action;
2. input coverage for keyboard, mouse, wheel/drag, touch where C++ supports it;
3. state restoration/focus/selection/scroll boundary coverage;
4. render evidence using real classic resources at audited resolutions/scales;
5. no reachable generic fallback;
6. recursive scenario/script corpus coverage where the screen is data-driven;
7. table-driven failure coverage proving every startup status producer returns
   a typed error before cache replay, base composition or scale-native text can
   write startup pixels;
8. zero-object, no-local-player and ordinary local-player viewport tests proving
   that the app neither draws the navy empty-state fallback nor invents an
   observer selection target;
9. direct coverage for F1 Help, Pause, Flash, mouse control, eliminated text,
   all four upper-board modes and debug/network overlays, whether implemented
   exactly or guarded by the correct typed boundary;
10. explicit documentation and tests for intentional product divergences such
    as recursive scenario search; they are never silently labeled C++ parity.
