[package]
name = "lc-platform"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "staticlib", "cdylib"]

[features]
default = []
ffi = []

[dependencies]
thiserror = "1.0"

[dev-dependencies]
tempfile = "3.8"
