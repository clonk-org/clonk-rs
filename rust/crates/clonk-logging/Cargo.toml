[package]
name = "clonk-logging"
version = "0.1.0"
edition = "2021"

[lib]
test = false

[dependencies]
tracing-subscriber = { version = "0.3", features = ["fmt", "env-filter"] }

[dev-dependencies]
tracing = "0.1"
