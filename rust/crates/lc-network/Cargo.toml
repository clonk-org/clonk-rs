[package]
name = "lc-network"
version = "0.1.0"
edition = "2021"

[dependencies]
lc-engine = { path = "../lc-engine" }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
tokio = { version = "1", features = ["io-util", "macros", "rt"] }
